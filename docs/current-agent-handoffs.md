# Reviewer handoff

Read [HANDOFF.md](../HANDOFF.md), [active state](../ACTIVE_PRODUCT_STATE.md),
[roles](../AGENT_COORDINATION.md) and [workflow](three-agent-workflow.md).

Model120 has120 examples/82 successes with86 registrations,66 living species,70 specimens, four
capture items and cash346. Its exact two-option successor was queried once under seed120091401, but
a wrapper attribute error occurred before the selected index was written. That identity is consumed;
the choice is unknown and there was no gameplay, label or fit.

The corrected Model120 checkpoint still exposes two ordinary identity-free candidates—routed
restoration and trainer resupply—and no fishing supplement. Review only the repaired V2 freeze. It
must write a pre-query tombstone and retain the choice plus selected-option content hash before
later validation. Do not propose a redraw, manual target, teacher fallback or consumed-step retry.

Claude and Flash both approved the no-redraw adjudication and found the pre-query crash window;
Claude also requested selected-option content identity. Those findings were accepted in the
unexecuted V2 script. Claude direct print-mode CLI access is restored.

Pete's explicit gate is a fresh start-to-finish model-directed Red run with story completion
and the full local Red Pokédex before any ROM hack. Version/trade/event dependencies cannot
be dropped, and shared credit is not local ownership. The older checkpoint-story demonstration
does not meet this gate. After full Red: compatible hack, Crystal, at least Emerald.

Next external work, if useful: narrowly review any change to the V2 receipt/admission seam. No broad
audit, gameplay or private-save access.
[Latest session](work-sessions/2026-09-14-model120-freeze-instrumentation-failure.md).
