# Closed: final Agatha resource batch

All four declarations executed once. Two reached Agatha completion; two heal/heal
sequences hit the decision limit. All seven attempted returns, including the
original controller stop, entered one shadow refit. No replacement cohort.
The optional updated-head probe was deferred, not passed: all four new first
choices were healing, and the reusable mechanics correction changes the frozen
controller contract. Continue with bounded model-led story composition, not more
within-Agatha collection. See the [closeout audit](audits/red-resource-fit-and-fixed-damage-2026-09-09.md).

## Historical prospective plan — do not execute again

## Why this is the next useful step

The new Agatha head has made one successful first choice, but all three fitted
returns began at the same resource context. Healing success also depends on the
frozen tail actually choosing story. More greedy-head demonstrations could all
heal and would not supply admissible contrast for the existing fitter.

Collect a small, fixed batch of real sampled choices at the already authenticated
changed-resource post-Bruno endpoint. No new model architecture is needed.
The [preparation receipt](evidence/red-agatha-resource-coverage-preparation-2026-09-09.json)
verifies source/plan compatibility with zero actions, frames or episode claims.
Preparation is not training progress and the batch has not executed.
Prepared declarations do not schedule execution. Start the next work session and
revalidate them before input; Pete's standing development permission does not
waive the no-retry or evidence rules and does not require another routine approval.

## Bounded sequence

1. Revalidate the four prepared declarations, complete saved ancestry, absence of
   execution claims, and current two-option menu. Use the exact published original
   collector `669cefa3`, not whichever checkout happens to be current. It binds
   the same Agatha plan `c453661c` as the original batch. Newer source has a
   different continuation identity and cannot silently reuse that contract.
2. Run the four prospectively fixed TRAIN attempts, seeds 2026090941–2026090944,
   with native82 sampling both the first choice and frozen tail. Each gets two
   macros, 60,000 actions, 6,000,000 frames and at most two field items. Start from
   the declared successful historical checkpoint; these are disclosed correlated
   resets, not retrying the original failed trial or manufacturing independent roots.
3. Stop on the first execution exception. Retain every attempted terminal and
   cancel never-claimed slots without replacement. Do not shop seeds or force the
   missing action to obtain a desired outcome balance. Record actual context-by-
   first-option coverage; four samples may still provide insufficient contrast.
4. If every attempt has admissible evidence under the exact same goal/controller
   contract, combine all three original attempts with all newly attempted returns
   for one separate shadow refit, ridge 1 and importance cap10. Include original
   stopped 03; preserve original cancelled 04 as cancelled. Unknown interruption is
   never zero: if the whole-batch loader cannot represent it, retain it and stop
   fitting rather than dropping it. Neither first-head probe is a training row.
5. Audit the whole dataset and fitted behavior before deciding on one bounded
   own-first-choice probe. Do not promote from training loss or clipped estimates.
   If new data still support only healing, say so; the result is not evidence that
   healing is always necessary. Reorient before spending another batch.

## Mission check and stop

- Capability: choose useful recovery before a finite story goal at varying resources.
- Learned authority: old82 retains collection authority; a refit is shadow until a
  separately declared first-choice probe. No recursive deployment or full-player promotion.
- Transfer test: changed resources in known correlated training history, not held-out
  evaluation, a compatible hack, or Crystal.
- Cheapest falsifier: source-bound preflight already passed; next is the small
  selected-action return batch, with no promised class balance or success rate.
- Time box: target one 60–90 minute session including collection, at most one fit,
  and audit. If a mechanical failure blocks learning, retain it and reorient once.
- Stop: first execution exception, incompatible whole-batch provenance, missing
  terminal meaning, or the declared resource budget. No boss-support patch cascade.

This is the next local learning experiment, not the whole remaining Phase4 plan.
**This is the final planned small within-Agatha coverage batch, not a new stage
gate.** After its bounded result—or justified cancellation—return to reusable
mechanic support and declared model-led composition toward concurrent
Champion/Hall-of-Fame completion. Insufficient contrast, unimproved training loss
or uncalibrated estimates do not authorize a replacement cohort. Unknown evidence
blocks the affected fit, not all subsequent product development. The optional
own-choice probe depends on an admissible useful refit; it is not a prerequisite
for addressing a real gameplay blocker.

The known move101/move82 switch-entry limitations can make these returns teach
controller-bug avoidance rather than useful game strategy. A narrow cartridge-
grounded correction has direct product value and must not wait for reliable bug
avoidance. Preserve old returns under their old controller contract; do not pool
repaired-controller data under the unchanged old source digest. Inspect fixed-
damage semantics rather than pretending every damage move is an ordinary typed
attack. No new boss-specific route or unbounded repair chain is implied.

The remaining Phase4 work is bounded composition toward story completion, honest
authority and current Champion/Hall-of-Fame evidence, not proof of optimal recovery.
The current first-choice heads cannot simply
be called recursively: their fitted return assumes the declared frozen tail and
fixed horizon. Any expanded authority needs its own measured consequences.

## Read-only mechanics reconnaissance for the following slice

The current code already has a narrow constant-damage bound for moves49/82 in
`red_battle_catalog.py`, and `red_trainer_damage.py` consumes it before ordinary
damage arithmetic. Do not build a second general battle simulator. The ordinary
party controller reaches a different path: `red_trainer_party.py` asks only for
attack types, and `switch_entry_attack_type` deliberately rejects fixed damage.
`RedTrainerPartyController.choose` invokes that screen only when a reserve could
materially improve the matchup; it is not simply screening every unused reserve.

A useful correction must represent the fixed-damage hazard explicitly at that
entry boundary, preserve observed health and existing eligibility/ranking, and
keep unknown/indirect effects rejected. Returning `None` as though fixed damage
were a harmless status turn is not a repair. The current screen does not receive
opponent level; any level-dependent bound needs an explicit observed-level input
and verified cartridge semantics. Do not infer move101 behavior merely from its
label or reuse ordinary type/STAB/critical arithmetic for fixed HP loss.

Existing ROM-free tests live in `test_red_battle_catalog.py`,
`test_red_trainer_party.py`, `test_red_trainer_damage.py` and
`test_red_trainer_control.py`. Test the real controller seam, not only a catalog
constant: supported fixed hazards, HP equality/lethality, missing/invalid level,
ordinary coverage preservation, unsupported effects and revalidation before input.
Only then consider the separately declared bounded model-led continuation. No
mechanics code, old failed state or old training contract changed in this closeout.

Phase4's exit and all later living-Dex, compatible-hack and Crystal stages remain
unchanged. Do not replace the difficult exit with a count of fitted boss-specific
heads. The permanent product is a reusable player, not a library of teacher victories.
