# Six-hour Red learning cycle — final audit

## Outcome against the mission

The product remains a model that plays and learns Pokemon, completing stories and a living
collection across games. Red is the first environment; a compatible unfamiliar Red hack is
the intermediate adaptation test before Crystal. None of those later exits changed.

This cycle's retained learner grew **45 to58 examples**, with every earlier row preserved.
Six of13new eligible outcomes succeeded; this adaptive same-lineage mix is not a benchmark
success rate. The saved collection grew **16 to22living species**,18 to25specimens. Required
duplicates are preserved for evolution, rather than counted as extra species.

The bounded collect/fit/continue checklist closes **3/3**. This percentage is for that local
integration checklist only, not sustained Red, full Red or the entire project.

## What actually ran

Q model55 selected Route24, found five non-target encounters across64grass displacements,
failed its finite search and automatically fitted56. Q stopped as declared. Its failure and
all resource costs remain recorded; Q02 is unclaimed and Q must not be restarted.

Fresh R then executed under source `c1d4fb557eb0730826e52a3ec96b37eb61285871`:

| Step | Actual behavior model | Selected source | Outcome | Retained fit |
| --- | --- | --- | --- | --- |
| R01 | 56examples | Route5 | Mankey12 caught; one verified status effect | 57examples |
| R02 | 57examples | Route24 | Abra12 caught; original Abra10 retained | 58examples |

R02's recorded continuation chain names R01's exact checkpoint, and its behavior model is
the exact R01 fitted model. Both fits retained all prior rows.998controller actions and
60588emulator frames cover both played steps. The native parent steps added zero learning
rows; two real source choices added two. No duplicate parent/source credit.

Both R steps succeeded, so the optional safe-search-failure continuation branch was **not**
exercised live here. Tests cover it. Do not turn that option's presence into a gameplay claim.

[Final result and fit](../evidence/red-incremental-learning-completion-2026-09-07.json).

## Exact saved endpoint

Read-only restoration verified22living species,25specimens,27registered,zero losses,
one ball,109money and one free current-box slot. Drowzee13 has28/39HP and16HypnosisPP;
the protected party remains alive. Abra10 and Abra12 coexist, as do the saved originals
needed for earlier evolution work. The final58-example model has not played after its fit.

Only Route5 remains a supported ordinary capture source, with one of two required Mankey
specimens retained. A new destination-only cycle cannot honestly supply two alternatives.
Do not repeat the current source-only loop or reset its resources.

## Engineering and validation

- Existing source choices, save admission and fitting were composed into a finite cycle.
  Actual outcomes precede fits; actual saves and updated models precede the next choice.
- Optional ordinary-search replanning requires the existing typed search-exhaustion failure,
  unchanged collection and zero losses. Other failures stop; default old behavior is retained.
- A read-only diagnostic joins executions to verified snapshots. Repeated identical encounters
  count as separate transitions; missing state or inconsistent chains fail instead of guessing.
- The existing local viewer shows hash-bound last-verified saved collection separately from
  live gameplay. It never opens a private save/model or fabricates live frames. API verification
  and view-only tests passed; no browser screenshot audit was performed. The updated roadmap's
  headline/checklist area was visually inspected through a rendered thumbnail.
- Broad ROM-free regression: **7504passed,1skipped,1expected failure**,1025.28seconds.
  This collection began before the final13command-test additions; those additions were covered
  by the final focused set, **207passed**. The groups overlap and must not be added together.
- 409package modules and three modified command scripts pass typing. Lint, documentation,
  focus and public-artifact checks pass. No new CI workflow or CI-wait gate was introduced.
- No external reviewers or subagents were invoked during this cycle. This is a Codex self-audit.

## Remaining limitations and next session

The final action-free mixed-parent inspection exposes `restore_team` and `acquire_species`,
but still returns `deterministic_unsupported`: restoration is not a learned option, leaving
only one scoreable kind. No episode, frame or input was created. A proposed Abra-to-Kadabra
target change then failed **before input**: changing the global boxed-evolution argument
also changed the reconstruction of an earlier supply-only profile transition. The existing
guard correctly rejected the unrelated historical edit. This does not demonstrate that
Abra evolution is unsupported; it identifies an ordering seam in the continuation command.
The first repair should be an explicit prospective evolution-target transition after old
profiles are restored, not a weaker supply guard or a rewritten old episode.

Training is real, but narrow: the fitted option-value scorer ranks supported semantic goals
and destinations; deterministic mechanics still execute movement, battles and captures.
These correlated train outcomes do not establish independent advantage, a learned Red win,
vision-only understanding or transfer. No sealed Red, Crystal or full-game run was performed.

The next smallest useful implementation is **mixed-goal continuation**, not another teacher:

1. Add an ordered prospective evolution objective that preserves every historical profile.
   Then `run_red_regional_goal_step.py`, which currently requires a multi-source proposal before its
   native parent chooses a task. Permit zero/one-source contexts to reach genuine native goals
   without inventing source-choice targets. Keep source proposals unfitted.
2. Reuse current resource/storage/party/evolution providers at the actual R endpoint. Inspect
   feasibility before choosing which missing binding to connect; preserve living duplicates.
3. Extend incremental inventory assembly to actual native-parent outcomes and zero-row support
   episodes. Do not count a forced restoration as a learned choice or fit one decision twice.
4. Run a bounded mixed-goal chain and report collection gain, funds/balls/storage, failure costs
   and intervention burden. Then vary contexts before expanding authority or claiming advantage.

This is a meaningful step toward sustained play because it removes manual per-example launches
and demonstrates actual model/save continuity. It does not remove the remaining need to connect
resources, storage and evolution into the same learner-controlled loop.
