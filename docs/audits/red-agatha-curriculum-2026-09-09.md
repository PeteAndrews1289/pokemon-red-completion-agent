# Agatha curriculum learning: audit and reorientation

## Outcome

The prospective Agatha lesson succeeded in241 controller actions and22,693 frames.
The saved event flag and5,940 payout were verified after reopening with zero input.
All30 specimens and the final Full Restore remain; the current save isdc12195a.
The run and its fit are consumed; neither may be repeated from the old endpoint.

Model78 became model79:78 prior comparative-choice outcomes and1 separately
declared guided story outcome. All prior fingerprints were retained. The stored
model, mixed dataset hash and curriculum admission were independently reopened.
The new outcome records success, dependency progress, actual costs and party damage.
It is not a model-selected victory, independent validation or authority promotion.
In-sample weighted error changed0.01279416 to0.01227443 on the same expanded corpus;
this is fitting behavior, not a measured gameplay improvement.

Evidence: [run, model and independent audits](../evidence/red-agatha-curriculum-2026-09-09.json).

## Findings and decisions

- The narrow script parser could not prove arbitrary called routines' behavior.
  Exact supported Red revision binding now precedes decoding. No ROM-hack claim.
- Curriculum metadata could otherwise acquire false choice probabilities.
  Admission uses exact fields, type-sensitive identities and distinct stream counts.
- Unreadable post-execution observations are explicitly censored, not successes.
- The first fit report used a stale objective label although the actual serialized
  model used the correct mixed objective. Preserve the original receipt; publish
  the erratum and fix reporting. Do not refit or rewrite the trained model.
- The dashboard displayed an unperformed choice comparison as zero changes.
  It now uses an unknown value and states that comparison was not performed.
- A preexisting historical Cave-source test omitted battle_runtime changes from
  commit8d9dcc06. The actual diff was reviewed and the explicit drift list updated;
  this does not make the old Cave measurement current-source authority.

## Verification and review

542 combined focused tests passed on the final implementation. Targeted typing,
lint, documentation, product-focus
and public-artifact checks passed. The overview was visually inspected and its API
verified at79 outcomes,37 successes and the Agatha checkpoint, explicitly not live.
Broad ROM-free suite status is recorded at closeout below; no full-suite pass is
inferred from targeted checks and no hosted CI retry loop was used.

The broad diagnostic was stopped at its25-minute budget:5,737 passed,1 skipped,
1 expected failure and1 failure, with3 integration tests deselected. Its sole
failure was the already-repaired Cave drift expectation; the repaired test passed
in the focused suite. The remainder was not executed: this is not a full-suite pass.

Flash3.8 High provided a24.06-second advisory:16,174 reported total tokens,
including11,761 input and4,413 output. It was not a source audit. Advice to retain
stream separation and censoring was accepted; reweighting historical choices or
asserting relative rankings without unchosen outcomes was rejected. A separate
read-only code reviewer found concrete boundary and display issues, which Codex
fixed and tested. Claude was not dispatched. Fresh five-hour/weekly service quota
readings were unavailable; token counts are not substituted for quotas.

## Next session

The income-to-model-led-story milestone stays2/3. The missing exit is a genuine
bounded story choice and fit, not another forced victory. Phase4 remains open.

1. Start only from saved Agatha state/model79; preserve resources and all79 rows.
2. Qualify Lance's distinct entrance script. An action-free parser check refused
   the Agatha grammar; do not carry that movement assumption forward.
3. Expose a legal story-versus-recovery choice, with the last-item reserve intact.
4. Execute one bounded model-policy lesson, retain the real outcome and reassess.

Long-term: finish Red with useful learned decisions, build the living collection,
then test an unfamiliar compatible Red hack before Crystal and later games.
Current narrow Red mechanics and sparse story-choice coverage remain limitations.

The [reference Lance script](https://github.com/pret/pokered/blob/master/scripts/LancesRoom.asm)
uses a multi-segment run-length encoded walk and a separate door-lock trigger,
unlike Agatha's single-direction movement buffer. Next qualification should decode
the real cartridge's movement list and event-dependent terrain, verify the settled
arrival, and retain the ordinary drift guard. Reference inspection is not live
qualification and no Lance input was issued in this session.
