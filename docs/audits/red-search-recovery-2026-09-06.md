# Search-recovery audit and reorientation

## Verdict

The bounded player can now preserve an ordinary unsuccessful search instead of losing its endpoint
to a fatal recovery exception. The new known-state regression executed the same **215 actions /
8,244 frames**, caught nothing, returned `search_exhausted` and `recovery_goal_repeated`, and retained
an authenticated terminal save. A separate read-only restore verified the original semantics,
collection ledger and unchanged saved bytes with **zero input and zero frames advanced**.

This is a small engineering unblock, not improved playing competence. The model still contains
**31 examples**. No fitting or new training examples occurred. See the
[result](../evidence/red-search-recovery-result-2026-09-06.json) and
[prospective plan](../work-sessions/2026-09-06-unsuccessful-search-recovery.md).

## What changed and what did not

- The Red survey reports ordinary overworld search exhaustion with actual encounter/capture/cost
  accounting. Other executor errors and exhaustion while an encounter is active still raise.
- The independent skill verifier labels verified zero-acquisition exhaustion as failure, never
  capture success. Partial real acquisition can still be verified by the existing ledger rules.
- The anti-loop guard remains. A rejected same-goal recovery proposal now produces a structured
  terminal result before recording or executing another goal. Unrelated errors remain fatal;
  rejection that changes metered state is also fatal.
- The runner requires an action-free, input-ready, out-of-battle checkpoint boundary. Existing
  held-button, ledger, saved-byte and completed-trajectory checks remain intact.
- The original failed episode is preserved, not repaired or relabeled. This new identity is a
  disclosed changed-code regression from its known parent, not an independent evaluation.

## Evidence and authority accounting

The one executed choice was `deterministic_unsupported`: only acquisition was model-supported at
the starting shop. At the destination, exploration became available too. The model then ranked
acquisition above exploration, but the recovery guard rejected that proposal **before input**.
That is one model proposal, **zero executed model-ranked choices**. The summary now explicitly
distinguishes unexecuted proposals from settled authority steps; raw policy-call counts alone
must not be presented as learned gameplay.

All 51 snapshot hashes and all action/frame joins verify. There were no component exceptions.
The fresh ledger stayed at 14 living species, 19 registered species, 17 specimens and 106 required
specimens remaining; no undeclared losses. Party and resources were unchanged, including 649 money
and 19 capture items. This time the actual endpoint, not just the earlier parent, survives.

## Useful alternatives: qualified availability is not useful execution

After restoring under the original profile, an existing mechanical profile expansion was inspected
read-only. It exposes **local development alongside acquisition and exploration** at the saved
Mansion endpoint. No new route, teacher, travel or healing was added; the existing qualified skill
uses a fixed four-battle dose. Its availability passed, but it was **not executed** in this session.
The original profile binding was not weakened or silently replaced.

Evolution and storage remain unavailable in this configuration. Center-based/boxed evolution
requires its genuine boundary and executor; free storage does not justify a pointless box switch.
The party is already levels 63/55/55/55/55/55. Therefore development is an available controlled
contrast, not justification to grind the whole party to a higher arbitrary target. Long-term
collection needs training/evolution of required precursors, not more levels on an established team.

The current scorer does not incorporate a durable unsuccessful-search history as a learned input.
Its acquisition proposal remained optimistic after no find. Safe stopping solves evidence loss,
not the decision of when to search again, switch sources or develop a required specimen.

## Qualification and scope

- 311 focused player, checkpoint, acquisition, routing, registry and focus tests passed.
- 46 subsequent runner/checkpoint-boundary checks passed after the final runner changes.
- 399 source/script files type-checked; lint, public-artifact, documentation and focus checks passed.
- The two unused registry source bindings were mechanically refreshed. Structural review found
  only source and derived execution hashes changed; assignment rosters/contracts did not change.
  Their identities were not executed, reauthorized or counted as progress.
- No new full-suite pass, hosted-CI pass, external audit or training improvement is claimed.
- No sealed Red evaluation, ROM modification, Crystal or full-game replay occurred.

The subsequent compatibility sweep initially found four old-dataset failures: the new player
failure enum had implicitly changed the frozen V1 collection contract. V1 now pins its original
six failure labels and retains its independent golden digest. No old registry or dataset was
rewritten to accept the new player vocabulary. This compatibility repair occurred after the
gameplay qualification; the run's recorded executable source remains unchanged.
After the fix, all **97** compatibility/runtime/provider checks and **144** registry/focus checks
passed. Hosted run `34056536283` on the pre-fix head failed on the same historical-contract issue;
its exact failing historical-loader test also passed locally after the repair. No new full-suite
or final-head hosted pass is claimed. The dashboard work note was shortened to its validated field
limit; an overlong note had caused the read-only observer to retain its last safe display.

## Next session: return to learning, not another recovery/CI loop

1. Reuse this authenticated endpoint and its original train lineage. Add the smallest explicit
   old-profile verification to separately declared expanded-profile transition; never loosen
   checkpoint matching or relabel the saved state as an independent root.
2. Connect it to the existing prospective, full-support selected-outcome collector. Use a short
   capture-versus-local-development contrast, retain unsuccessful searches and costs, and preserve
   all 31 prior examples. Do not retrofit this greedy regression as training data.
3. Audit and fit only settled sampled outcomes, then run a short new continuation. Report real
   executed learned choices, required-specimen gain, cost and stopping separately from proposals.
   Stop at the declared dose; do not grind to 60 or enlarge search bounds for a favorable result.
4. Prioritize required-precursor evolution/storage and persistent search history next. Expose real
   reachable alternatives and evaluate continue-versus-switch behavior before a longer Red run.

Maintenance has now addressed the named lost-state failure. Another session with only guardrails,
CI or receipts and no measured learner outcome should trigger the anti-drift review. The delivery
sequence remains useful Red play, model-directed story/living collection, unfamiliar compatible
Red modification, then Crystal—not a new teacher campaign or immediate transfer promise.
