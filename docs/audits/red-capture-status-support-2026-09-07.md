# Capture preparation engineering — September 7

## Actual N result and reorientation

N completed a model-selected Route10 acquisition: Voltorb16,1263actions76296frames.
The exact terminal restored20living species/22specimens/25registered,zero losses,
zero balls and1109currency. One PC preparation and three status attempts executed;
**zero successful status observations**. Hypnosis PP20→17 and helper HP15 are observed.
The catch used all four balls: no capture-efficiency improvement is established.

Fit53→54 retains all53 earlier rows,19successful outcomes total. The final model has
not played. The small resource-aware checklist closes3/3, while affordable continuation
starts0/3; sustained-play and later stage exits are unchanged.
[Path-free evidence](../evidence/red-capture-status-result-2026-09-07.json).

Next qualify a smaller affordable purchase from actual funds, restore helper readiness,
and try another useful model-selected continuation. Do not retry N or inject resources.
The following section preserves the pre-execution engineering checkpoint.

## Scope and claim boundary

This is deterministic skill maintenance unblocking model53's existing acquisition and
resource choices. It is not a new learned capture policy, a second training target, or
evidence of independent advantage. The resource-aware checklist remains **2/3** until a
productive post-fit continuation actually plays. No further unchanged ball-only search
follows the two-failure stop recorded in the [resource audit](red-regional-resource-goals-2026-09-07.md).

## Verified observation

The exact M save restored without controller input. It retains19 living species and21
specimens,24 registered species,4 ordinary balls and1109 currency. A separate action-free
inspection of the actual current-box move structures found a level13 Drowzee with Hypnosis
and20 PP. The boxed Oddish and Pikachu do not currently know a qualifying status move.
The diagnostic checked unchanged save bytes, frames and released input; no prediction ran.

## Implementation under qualification

- Select a usable pure sleep/paralysis move from actual move effects, PP and health.
  Species-swapped tests distinguish capability selection from a helper-species allowlist.
- Read enemy status through the revision adapter. Do not add a field to historical raw
  checkpoint serialization. Only an observed status change earns a successful-status count.
- Limit status attempts to three per encounter across all ball throws. Preserve target HP,
  species, party membership and bag through each attempt; no poison, burn or damaging move.
- Retrieve an actual stored helper using the existing PC controller and cartridge router.
  Preserve the living multiset, strongest living escort and every last field-move carrier.
  Re-observe and rebind the already selected acquisition at the new location; do not ask the
  policy to select a different goal or silently change its destination.
- Keep all setup travel/menu costs in the selected goal. Preparation has zero standalone
  learning rows. Forward only allowlisted status/preparation counters to the player summary.
- Opt in with an explicit ordered execution-profile transition; old profiles and outcomes
  remain unchanged, and subsequent regional retargeting retains the declared capability.

The runtime still uses deterministic navigation, low-level battle/status and ball control.
It does not yet optimize catch probability or choose a capture-specific lead before encounters.
Switching may let a fleeing opponent escape or consume sleep turns. These remain real costs,
not successful captures. Absent safe support masks acquisition, not unrelated resource goals.

## Qualification checkpoint

446 targeted ROM-free tests passed across capture, PC, profiles, routing, player continuation,
source choice and shared battle control. A further overlapping259-test protocol/dashboard/
continuation check passed in108.31 seconds. Lint, documentation, product-focus and public-artifact
checks passed. Type checking covers408 source modules. Live status-assisted gameplay has
**not yet run** at this checkpoint; the next falsifier is one prospectively bounded fresh
continuation from M, not a replay of a consumed trial.

The earlier broad suite on the prior executable was intentionally interrupted after5210
passes,1 skip,3 deselections and1 expected failure in1154.25 seconds. That is not a full-suite
pass. The separate earlier complete suite is historical, not qualification of this change.

Reassess at09:45UTC if preparation is still not executable; overall work window ends10:57UTC.
