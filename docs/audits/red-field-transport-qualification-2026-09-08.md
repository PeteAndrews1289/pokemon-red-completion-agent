# Live field transport closeout — 2026-09-08

[Path-free evidence](../evidence/red-field-transport-qualification-2026-09-08.json).

## Verified

- The new storage component uses the existing deposit/change-box/withdraw operators.
  It revalidates its whole plan before input and compares retained party members, every
  box's species/levels, bag, money, position and story flags after the operation.
- The prospective transport heuristic preserves unique field moves, usable capture
  status and a qualified escort. It minimizes ordinary offensive move capacity among
  eligible deposits; this is not a learned or boss-optimal roster policy.
- Actual Snorlax was stored and Farfetch'd55 withdrawn with139HP. Surf/Strength/Dig,
  sleep support and electric offense remain available. Complete species counts match
  the original30-specimen collection.
- Computed travel reached the PC and returned to Cerulean. The initial flight admission
  stopped at the actual open menu; no flight completed in that attempt.
- Source5cc6b29c fixed the observed header boundary and supports exact-menu resumption.
  The one successor observed destination IDs0,10,9 and confirmed one flight.
- Final state5c66ec39 is Indigo(map9,x9,y6), input-ready, battle0, no held buttons,
 619 currency,0balls. A zero-input/zero-frame reload verified it and the stored collection.

## Failure and cost accounting

Attempt1:229 actions/18948frames. Its full report writer failed on a frozenset in
route details, after terminal.state had been saved. The original exception text was
not recovered; the saved tile row directly demonstrates the rejected blank-tile
assumption. A new read-only audit records only supported facts, not reconstructed
unobserved metrics. The action trace remains available. Do not rerun its script.

The separately declared exact-terminal successor used7actions/792frames and succeeded.
Total236actions/19740frames. No rewind, repetition of deposit or repeated flight.
The diagnostics are **not** native training episodes or independent lineages.
Their terminal, parent identity and both costs must enter any future continuation
honestly; do not start another run from the old parent to disguise this work.

## Verification and scope

325 focused tests cover field retrieval, PC, capture-party, Fly, observation and
other field moves. Full-source lint and typing pass (426 source files).
Another153 dashboard/product/roadmap tests pass. Public-artifact, documentation,
active-state and registry-freshness checks pass. The saved dashboard card now identifies
Indigo explicitly as a diagnostic terminal, not an admitted training checkpoint.
No broad external review ran. Hosted CI is not a gameplay gate.

Model76 retains76 fitted outcomes,34successful,66distinct rows; no new fit.
The income-to-story checklist stays1/3: travel alone does not make Lorelei executable.
Phase4 is not complete; no independent generalization or Crystal claim.

## Next bounded session

1. Carry terminal5c66ec39 and both diagnostic costs into the existing native continuation
   mechanism; do not relabel support as a training outcome or manufacture a new root.
2. Reuse the generic adaptive trainer-battle machinery for a flexible-party boss contract.
   Verify its actual entry, opponent identity, protected resources and completion event.
   Do not restore the teacher's exact party/order, moves, stock or fixed walk strings.
3. Expose an honest story-versus-preparation choice, record its result and fit incrementally.
   If the boss adapter is unsafe, retain that exact reason rather than cycling through
   collection/healing or starting a full replay.

Next setting: Astra High, Fast off. OpenAI Docs was consulted for task-appropriate
verification guidance; no comparative usage or speedup was measured.
