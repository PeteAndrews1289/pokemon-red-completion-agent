# Mixed-goal integration — September 7

## Product outcome so far

The mission is a learned player that completes stories and a living collection,
starting with Red before a compatible unfamiliar Red modification and Crystal.
This session implements the first three items of the prepared five-item mixed-goal
plan. It has produced **no new gameplay, training example or model fit**. The
58-example model and R's verified22living species/25specimens remain unchanged.

## Implemented and checked

- An explicit ordered `--evolution-objective SOURCE:TARGET:LEVEL` transition can
  change the future goal after earlier regional/supply profiles are reconstructed.
  Historical global `--boxed-evolution` behavior stays unchanged. Reordering the
  new objective before an old supply profile still triggers the non-supply guard.
- The mixed-parent command now reaches native goal selection with zero/one source.
  A unique source is a binding, not a sampled learned choice. With none, the native
  profile remains in control. Neither case samples a source or creates its target.
- Native incremental fitting reconstructs previous native and regional inventory,
  authenticates actual new native outcomes and rejects duplicate/source credit.
  Zero-row support alone does not fit; the caller must carry its retained results
  alongside the next eligible goal. No new automatic native gameplay chain has run.

136 focused checks passed, including real recorded-episode admission and support
exclusion.409package modules and the three changed implementation modules/commands
pass typing. Lint, documentation, public-artifact and focus checks pass. The
regression run initially found stale generated source bindings; regeneration and
the affected focused checks preserve those checks, rather than disabling them.
Broad regression completion and actual saved-menu results are recorded below when
available. No external reviewer or subagent was used; this is a Codex self-audit.

## Pre-execution concern

Source inspection shows that the native evolution skill still assumes a trainee
with a safe direct-combat venue. Its battle move selector rejects status-only
moves; the legacy trainer also treats missing damaging PP as a restoration need.
Switch-assisted fallback will not fight with an escort at the old parity cap.
These are three separate assumptions, not a reason to remove one check and run.
The source-level capability check gives level12 a level7 direct-opponent ceiling;
all three existing venue bands have zero fightable share at that ceiling. Their
minimum encounter levels are9,15and28. The legacy escort parity cap is55, below
the retained starter's level63. These checks use code configuration, not a new run.

The previously demonstrated Ponyta evolution does not qualify low-level Abra
development. The next action-free inspection will preserve all historical profile
hashes, restore the exact R endpoint, bind its unique source, and check the native
opportunities. If no useful mixed choice exists, the declared stop condition applies.

## Reorientation candidate if that concern is confirmed

The smallest next capability is **bounded development of a non-attacking specimen**:
separate the XP recipient from the battle finisher, check genuine switch and attack
safety, and stop at the declared evolution target. Reuse observed party switching,
damaging-move selection, recovery, cartridge routing and specimen-preserving PC work.
Keep legacy equal-level/capped-escort behavior intact for its original callers.

Qualify a short safe battle/XP quantum before advertising the complete evolution
goal, then preserve the actual save and continue only if throughput is practical.
No species-specific direction script, rare-item shortcut, unlimited carry grinding,
new teacher run, reset, sealed access or Crystal execution is the next step.

The next learner result must be an actual useful mixed-goal choice and retained
update. This engineering checklist is not the sustained-Red stage exit and does not
establish independent learned advantage.
