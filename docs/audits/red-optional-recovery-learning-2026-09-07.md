# Optional recovery learning — September 7

## Outcome

The bounded session produced one new actual training example, not another teacher
campaign. From V's retained save, the exploratory model policy selected healing
over evolution with probability 0.7960547254964198. It succeeded in 48 controller
actions / 4,056 frames. Metapod remained level 4, but HP improved from 5/18 to
18/18. All six party members are healthy and at full HP. All 25 specimens,
23 living species and 28 registrations survived. No capture, XP or evolution
was claimed. Capture supplies remain zero and money remains 109.

This was **field-item healing**, not a Pokemon Center visit. Exactly one Hyper
Potion was consumed (5 to 4), for 13 HP restored; aggregate recovery stock fell
from 21 to 20. The actual item cost is in the fitted outcome (1/21), but the
prospective restore feature advertised resource cost zero and had no known-spend
quote. The deterministic skill chose the item, not the model. This is a resource
efficiency/representation limitation, not evidence of optimal recovery planning.
Preserve W's original features; any improved quote must be prospective.

The update retained all 60 original example hashes, added exactly the executed
recovery outcome, and preserved V as zero-row support under its original behavior
model. Model61 has 61 outcomes, 24 successful and 51 distinct selected rows;
the corpus records 37 native episodes and 12 regional choices. It has not played.
The exact W save reopens with zero input/frames and is ready for a **forced
evolution bridge**, not another learned choice.

[Path-free result](../evidence/red-optional-recovery-learning-2026-09-07.json).
Executable source: `960fac1606f05e2a4eeee0e354786c42e38f7164`.

## What changed

- Explicitly froze the 24-column v1 and 29-column v2 layouts. V3 appends recovery
  identity and its interaction with semantic party pressure; it does not insert
  a new column into old one-hot weights.
- Preserved eight-kind historical campaign menus and their literal fixtures.
  Growing the portable enum no longer silently rewrites those contracts.
- Gave optional recovery its own prospective behavior identity, model schema,
  strict codec round trip and training-plan binding. Historical excluded choices
  remain excluded; mandatory recovery still bypasses sampling and consumes no RNG.
- Initialized new columns to zero without a fit, then fitted only after actual
  gameplay. The mixed-version reader replays each episode with its original model.
  V was not relabelled to the bootstrap identity to bypass a same-model helper.

## Verification

The real 60-row corpus reopened. All 35 pre-session native episodes replayed;
24 previously excluded decisions remained excluded. Upgrading changed predictions
on every historical candidate by exactly 0.0. Controlled ROM-free outcome reversal
also shows that the recovery-pressure effect can be learned from outcomes, not a
fixed preference; this synthetic check is not game-training evidence.

Final active checks: **215 passed** across value features, historical replay,
sampling, strict codecs, native trajectory admission, fitting, continuation and
live-runner callers. Another 50 source-registry checks passed after regeneration;
54 provider/native-recovery checks passed. Counts overlap with earlier focused
runs and must not be added as a unique-suite total. Lint, typing across 412 source
modules, docs, product-focus and public-artifact checks passed.

The broad legacy pass reached 1,135 passes and one stale fixture expectation,
which passed after correction. It was deliberately bounded at about twenty
minutes, not called a complete-suite pass. Three legacy journal-to-fitter cases
also passed before bounding that slow campaign-fixture pass. No CI alarm was
disabled or weakened. GitHub run 34152570644 was pending at live execution;
the development run did not wait for a redundant full remote test cycle.

At closeout that remote run finished with 5,958 passes and one failure: the
completion-dose test's fake model lacked the newly required feature-version field.
The fixture now declares it explicitly; completion-dose preservation is tested
across versions 1, 2 and 3. All 45 related completion/command/regional-choice checks
passed locally. Another 153 dashboard/focus/roadmap/work-status tests passed.
This correction changes test data only, not W's executable source or outcome.
The replacement remote run remains separate verification, not a replay of W.

## Claim boundaries and reorientation

This proves a newly supported choice can be executed, audited and learned from
without rewriting earlier work. It does **not** prove good general recovery timing:
there is only one real recovery example. Training-set MSE changed from
0.010723573982138449 to 0.010700155033064129 on the same 61 rows; that is an in-sample
diagnostic, not independent improvement. Mechanics remain deterministic.

The post-withdrawal evolution repair is still ROM-free qualified. W exercised
the ordinary restore provider, not the native evolution trainer, so it cannot
live-validate that repair by association.

The storage-safe varied-collection milestone stays **2/3**. Its useful varied
collection outcome is not replaced with an easier healing criterion. The full
Red story, living collection, unfamiliar compatible Red modification and Crystal
stage exits are unchanged. No full replay, sealed access or Crystal run occurred.

The next product bottleneck is **automatic selection of useful collection
objectives**. Current high-level goals still hide a manually declared evolution
target and deterministic recovery-mechanism choice. Finish at most one bounded productive bridge from the healthy save, then
expose competing owned-precursor objectives. Do not turn the next session into
another sequence of hand-picked target changes or forced support-only fits.
[Next session plan](../work-sessions/2026-09-07-owned-precursor-choice-plan.md).

Codex performed this audit; no Claude, Antigravity or subagent review was requested
or represented as completed. No gameplay is left running after this closeout.
