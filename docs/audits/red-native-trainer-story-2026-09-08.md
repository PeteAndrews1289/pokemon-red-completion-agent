# Native trainer-story integration — September8

257 focused tests pass; full-source lint and typing pass (430 modules). This is not a
full-suite or live battle qualification. [Evidence](../evidence/red-native-trainer-story-2026-09-08.json).

The new profile is opt-in and preserves legacy profile parsing and historical menus.
It plans from cartridge terrain/trainer objects, prepares the observed party, rechecks
the interaction and invokes the existing identity/payout/event-checked battle wrapper.
The bounded controller reuses observed move and switch execution; six switches maximum,
no repeated switching without a move decision, no item spending or fainted party, and
no learned battle-policy override. Preparation is still heuristic, not a survival forecast.

Real saved-state preview verifies the original native semantic hash and full specimen
counts before inspecting Indigo. It caught a strict enum-versus-int map-ID boundary;
the repair is now tested. The resulting Indigo menu has only the story binding. No
input or model query ran. Its first rehearsal must be labelled forced support.

Recorded-support checkpoint tests use the real private writer and opener. They preserve
native predecessor hashes, split, search memory and continuous states/traces. Mutated
costs, state bytes, profile, partition, added decisions, observer mode and ancestor fail.
An import has zero new actions/frames, separate historical costs, no training plan and
no model steps. Retained local diagnostics are not promoted to native gameplay evidence.

Remaining: publish and import the existing two support segments; one bounded native
boss rehearsal; preserve exact outcome; expose genuinely useful competing goals before
claiming a new story-choice fit. No external audit/quota query ran. North Star unchanged.
