# Native trainer-story integration — September8

## Live closeout

Published source2beef485 ran two forced-singleton attempts,301actions/27684frames total.
The administrative support import is verified; its separate236actions/19740frames and
first missing report remain preserved. A post-publication operator verifier typo was
recovered by reading the existing import, with no new import or gameplay.

Attempt01 stopped after scripted entrance displacement, retaining native checkpoint4890baa7.
Attempt02 continued from that exact settled room state; it defeated the first three quoted
opponents and stopped against Jynx. Exact failed stateef23d074 remains at trainer MAIN,
party HP76/61/71/12/34/139, all30specimens and bag stock retained. It is not a safe checkpoint.
The first three opponent zero-HP observations and subsequent roster transitions support
the progress claim; an unrelated transient intro species value is not counted as a defeat.

Only two observed switch targets occurred: slots2 and4. The last switch took Dugtrio from
118HP to12HP before its next move. Reconstructing those controller flags reproduces the
unfit-active-member refusal. This is posthoc diagnosis, not a recovered original nested
exception. Future diagnostics retain bounded explicit cause chains; forced UI commitments
no longer set the model-prediction flag.55 focused follow-up tests pass.

No policy query, label, fit, reset, consumed-choice retry, item spending or fainted party.
Model76 and checklist1/3 remain unchanged. Next qualify switch-entry risk and explicitly
budgeted recovery from the exact failed battle, then meaningful competing story goals.
Do not resume from a pre-battle checkpoint to erase damage or repeat the entrance script.

## Initial engineering checkpoint

Final combined local verification:451 focused tests pass, full-source lint and typing
pass (430 modules), registry/docs/public-artifact/focus checks pass. The infographic
was regenerated and visually checked. This is a targeted suite, not all repository tests.

257 focused tests pass; full-source lint and typing pass (430 modules). This is not a
full-suite or live battle qualification. [Evidence](../evidence/red-native-trainer-story-2026-09-08.json).

The new profile is opt-in and preserves legacy profile parsing and historical menus.
It plans from cartridge terrain/trainer objects, prepares the observed party, rechecks
the interaction and invokes the existing identity/payout/event-checked battle wrapper.
The bounded controller reuses observed move and switch execution; six switches maximum,
no repeated switching without a move decision, no item spending or fainted party, and
no learned battle-policy override. Preparation is still heuristic, not a survival forecast.

Real saved-state preview verifies the original native semantic hash and full specimen
counts before inspecting Indigo. It caught a strict enum-versus-int map-ID boundary;
the repair is now tested. The resulting Indigo menu has only the story binding. No
input or model query ran. Its first rehearsal must be labelled forced support.

Recorded-support checkpoint tests use the real private writer and opener. They preserve
native predecessor hashes, split, search memory and continuous states/traces. Mutated
costs, state bytes, profile, partition, added decisions, observer mode and ancestor fail.
An import has zero new actions/frames, separate historical costs, no training plan and
no model steps. Retained local diagnostics are not promoted to native gameplay evidence.

Remaining: publish and import the existing two support segments; one bounded native
boss rehearsal; preserve exact outcome; expose genuinely useful competing goals before
claiming a new story-choice fit. No external audit/quota query ran. North Star unchanged.
