# Regional source learning — audit and reorientation

This is an intermediate closeout within the04:57–10:57UTC work window, not a claim
that six hours elapsed. [Evidence](../evidence/red-regional-source-learning-2026-09-07.json).

## What changed

The existing learned value scorer now samples a capture destination from real reachable
ordinary-grass options below the one-option-per-kind goal manager. Four destinations
passed the action-free cartridge/routing/resource checks. The operator did not select
the played source. Navigation, battle and capture execution remain deterministic skills.
Sampling retains25%uniform exploration; a sample is not necessarily the greedy choice.

| Attempt | Behavior model | Actual selection and result | Actions / frames | Fit |
| --- | --- | --- | ---: | --- |
| D |45 | Route24; search exhausted, four balls retained |356 /16,836 |45→46 |
| E |46 | Route24; search exhausted, four balls consumed |506 /36,552 |46→47 |

All45 previous examples remain. The latest model has47 settled examples,16successful;
both new source outcomes are failures. No unplayed candidate becomes a target. The
deterministic parent goal contributes zero rows, avoiding double-counted gameplay.
E is fresh play after the D fit. Model47 itself has not played yet.

E's model preferred Route10, but exploration sampled Route24 with probability0.0941.
That recorded choice was not replaced after seeing it. D's failed search cost appeared
in E's source history:1attempt/1exhaustion/356actions/16,836frames. The initial read-only
E inspection exposed position-dependent binding keys; the correction reconstructs source
history from authenticated prior regional choices without rewriting the old native ledger.

## Evidence and limits

113 focused checks,402-file typing and explicit new-driver typing passed, plus lint,
documentation and private-artifact publication checks. Tests execute real durable parent
trajectories; distinguish pre-input choice commitment, altered distribution/profile/cost,
missing terminal bindings, parent double counting and missing retained fit rows. Explicit
ordered revisits are accepted; undeclared profile rollback still fails. No full-suite or
outside-audit claim is made. CI is not a prerequisite for these development fits.

Current lineage:18living species,20specimens,23registered,zero specimen loss. Zero balls
remain and129currency is insufficient for another Poké Ball. The two correlated failures
do not demonstrate independent superiority, multi-source success, autonomous Red completion
or transfer. Candidate differences currently emphasize effort/history, not catchability.

Regional integration checklist:4/5 (80% of this small checklist only). Multiple real choices
and a retained-data fit/fresh continuation are demonstrated. Varied source play with practical
resource costs/interventions remains open; the overall sustained-Red exit is not complete.

## Next bounded section

Inspect E's legitimate inventory/funding options and the reason its four capture attempts
failed. Prefer an existing verified resupply/funding mechanic. If a narrow surplus-sale skill
is needed, protect required/unique items and party-recovery reserves, quote actual proceeds,
and verify exact inventory/currency deltas. This is support for the named learned destination
loop, not another teacher route. Never inject money, reset to restore balls, retry D/E, or
invent a second available capture option. Reorient after90minutes without a feasible path.

Feedback: the loop now produces model updates, but resource efficiency—not more protocol
work—is the useful next problem. Learning from failure is necessary; calling repeated
failure useful forever without restoring practical collection progress would be drift.
