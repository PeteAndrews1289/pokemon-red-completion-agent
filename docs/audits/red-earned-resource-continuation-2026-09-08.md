# Earned supplies, one learned recovery choice, and an Oddish retained

The finite **resource-backed progression checklist is 3/3**. Phase 4, model-led
Red story completion, is not complete. This report does not claim an autonomous
economy, learned battle control, independent advantage or transfer.

## Measured result

| Step | Actual outcome | Actions / frames | New learned rows |
| --- | --- | ---: | ---: |
| Earned purchase | Bought one Poké Ball for 200; money294 to94 | 287 / 13,452 | 0 |
| Optional recovery | Model chose team restoration over capture; succeeded | 55 / 3,072 | 1 |
| Post-fit continuation | Only capture was executable; caught a spare Oddish | 434 / 29,856 | 0 |

The learner is now **model76**, with76 fitted outcomes,34 successful and66 distinct
selected feature rows. All75 earlier outcomes remain. The optional HP/PP restoration
choice had logged probability0.779731; the source proposal itself was a unique
binding, not a learned destination choice. Capture was forced singleton, not a
second learned decision. More fitted data is not evidence of improved generalization.

Three exact terminal reloads verified94 currency, four protected Full Restores,
all earlier species counts retained, and30 final specimens /28 living species /
33 registered species. Oddish now has two specimens, allowing future evolution
without consuming the only retained base form. No evolution was performed here.
All terminal party members are alive; the audit sent zero input and advanced zero frames.

Session cost is776actions/46,380frames. Including both earlier funding failures and
the trainer recovery, the funding-to-capture chain costs880actions/55,921frames.
Those earlier failures remain immutable; no resets or protected-item sales occurred.
Funding01 and the purchase are authenticated zero-row entries in the new fit's
inventory. Failed funding02/recovery remain linked support provenance, not fit rows.
The post-fit capture is pending zero-label support for the next genuine fit.

[Path-free evidence and exact identities](../evidence/red-earned-resource-continuation-2026-09-08.json).

## CI: repeated trigger, one stale test interface

The latest failed runs34243450288 and34242012944 both reached the same TypeError:
the historical restore test's observer substitute rejected `trainer_funding`.
The corrected test explicitly accepts and verifies both trainer options. Execution
enables them while old snapshot restoration must keep them false. A companion
preflight test checks that the current execution options are forwarded correctly.
No production code or safety assertion was removed. 111 related tests plus 108
roadmap/product checks pass locally (219 total). Lint, registry freshness and
documentation checks passed before publication.

Repair commitd9e8c6e2 is pushed to PR236. Its hosted run34244100279 passed the
previous failure point and was still running the full tests when this report was
prepared; no full-suite green claim is made. We did not manually rerun old failures,
disable CI, change notification settings, or add another workflow. Future iteration
should batch changes at useful publication boundaries, rather than push every patch.

Two private fit-wrapper setup calls were rejected before any fit because they tried
to reuse gameplay readiness for a consumed output/episode. The wrapper now loads
the completed records directly through the existing authenticated fitter. One actual
fit occurred. The read-only audit's first profile encoding was also rejected; canonical
serialization corrected that without gameplay input. These were operator errors,
not hidden new trials or missing model outcomes.

## Reorientation: the next gap is not another healing trip

The latest exact checkpoint4a609569/state77c4d264 is on Route24, with94 currency
and zero balls. A no-input inspection exposes **only restore_team**. It sent no
prediction query or controller input and claimed no episode. We stopped there.

Next session should qualify one bounded reachable income option beyond the current
map using the existing cartridge trainer data, safe computed routing and battle
skill. Then expose meaningful resource/story alternatives to the model. Do not
keep repeating optional healing, one-ball capture loops, teacher replay, or catalog
work that never reaches a real decision. The previous one-map income success does
not establish that the player can replenish resources wherever it runs out.

What still remains for Phase4:

1. Reach useful income and prerequisites from varied retained locations; give the
   goal model actual resource/story alternatives rather than only a heal option.
2. Compose dependency-legal quests, travel, HM/puzzle skills and battles from the
   actual world state. Existing teacher coverage does not grant the learner authority.
3. Show useful model-directed story progress across bounded varied situations with
   failures and intervention costs visible, then satisfy the unchanged full-run gate.
4. Demonstrate model-led completion with concurrent Champion and Hall-of-Fame evidence.

There is no defensible whole-phase percentage or completion date yet. Three of
three describes only the named finite resource-integration checklist above.

No external agents were needed for this narrow continuation; no quota was consumed
on another routine audit. Current external quota was not queried; the prior session's
figures are historical. Suggested next setting: Astra High, Fast off for ordinary
integration; escalate for a specific difficult design decision, not all waiting time.
The OpenAI Docs skill informed supported
[Astra effort settings](https://developers.openai.com/api/docs/models/gpt-6-astra),
not a measured account-usage comparison.
