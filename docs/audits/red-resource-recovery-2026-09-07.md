# Resource recovery — engineering checkpoint

The mission remains model-directed Red play and a preserved living collection,
then a compatible unfamiliar Red modification and Crystal. This repair unblocks
the next collection decision; it is not additional learned authority.

Cross-box evolution is now opt-in for native completion-dose play. The provider
selects an actual stored precursor, prefers the current box, and requires room
for the existing deposit/withdraw exchange. Before input it rechecks the complete
request. Computed walking reaches the PC; the box switch must preserve every
observed specimen, level, party member, bag item, currency and story flag. The
existing evolution engine resumes from the actually observed PC boundary, not a
synthetic reset. Its budget includes all preparation.

Storage costs now survive the routing and episode summaries as a bounded,
path-free diagnostic. Preparation creates zero extra learning rows. Old summaries
without recorded costs stay unknown rather than becoming fabricated zeroes.
New verified zero-ball search failures have a distinct capture-items-exhausted
reason; U remains its original historical search-exhausted failure.

145 focused tests passed before the broader compatibility pass. Prior source
18535c2a completed7604 local tests and GitHub34143810607 is green. These are
different claims: the older full-suite result does not validate the new source.

Broader verification:386 cases passed initially; three source-fingerprint checks
found stale generated identities. Those identities were refreshed, and all three
then passed inside a158-test follow-up. Repository lint,412-module typing, public
artifact safety, documentation and focus checks pass. No new full-suite claim.

Live feasibility is pending. Model60/60examples and the exact U checkpoint remain
unchanged; no new input, fit, capture or evolution has occurred. The local
storage-safe varied-collection milestone remains2/3, not complete.

Next: publish the locally checked repair and inspect U without input. If only
evolution is available, any played bridge must stay deterministic support with
zero learning credit. No empty-bag capture, funded reset, full replay or new teacher.
Scarce-resource feature redesign is deliberately separate: changing old feature
interpretation without a compatible migration would corrupt historical labels.
