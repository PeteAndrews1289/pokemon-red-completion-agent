# Resource learning and final-story qualification

## What actually learned

The [practice receipt](../evidence/red-dependent-story-practice-2026-09-09.json)
records two new model-selected restoration outcomes on one explicitly dependent,
historical post-Lorelei training branch. Both selected healing over the available
Bruno story option. They consumed two Full Restores,80 actions and6960 frames.
The third preflight had only a story option after reserving the last heal; it
stopped before input. That branch is closed, not resampled for a preferred choice.

Model81 retains all81 rows:80 comparative choices and1 guided Agatha outcome.
The distinct selected feature count remains69. Both healing transitions repeat
known behavior, and the second endpoint equals a previous post-healing state.
This is actual incremental fitting, not new independent coverage or evidence of
improved play. On the same81-row corpus the previous model's weighted error is
0.0120857873 and the new model's is0.0120884649: slightly worse, not an improvement.

The separate continuous Lance chain remains intact, including all368 actions and
32869 frames of retained support costs. Its six members and30 living specimens
remain; it has23983 currency, no Full Restores and one owned Elixir. The earlier
statement that no useful resource alternative existed was too broad: the provider
had omitted PP restoration. No inventory was replenished by the practice branch.

## Small engineering change with a named learning purpose

The next experiment exposes a genuine choice at that actual Lance endpoint:
spend the owned Elixir, or advance to the Champion. The PP planner computes actual
benefit for every living party member, caps each slot at its PP-Up-adjusted maximum,
and uses the best total restoration rather than assuming the lead or a species.
Read-only cartridge/state qualification found22 PP restored in party index3.

The provider owns exactly one use, verifies the changed target PP and one-item
decrement, closes menus and independently rechecks preserved state. Tests cover
the game's two-stage ordering: PP changes before success text finishes; inventory
decrements afterward. No further confirmation follows observed consumption.

Historical goal-observation v1 bytes and outcomes remain unchanged. Explicit PP
profiles produce v2 observations and include Elixir stock in the existing resource
cost outcome. The nine learned heads and feature width are unchanged. **Exact PP
benefit is recorded evidence, not a new learned feature or target.** The model can
learn success/cost of this goal; it does not yet value individual restored moves.

## Champion adapter, not the old winning recipe

The final room is not an ordinary trainer-header interaction. Its revision-pinned
script supplies a starter-dependent trainer set and its own automatic movement.
The [Champion script](https://raw.githubusercontent.com/pret/pokered/a1a22aaf84d1675bcdbaeb194592379d586d838e/scripts/ChampionsRoom.asm)
and [Hall-of-Fame script](https://raw.githubusercontent.com/pret/pokered/a1a22aaf84d1675bcdbaeb194592379d586d838e/scripts/HallOfFame.asm)
were checked against actual cartridge operands. The adapter does not replay those
directions over the game's queue or use the legacy boosted fixed-party strategy.

The actual saved rival selector qualifies opponent243, normalized class43, set2.
The route is four computed steps; the specialist exclusively owns the last warp.
Availability and immediate execution checks bind the same scene, released controls,
no pending ordinary trainer, clear dialogue, current terrain and living party.
Combat uses the existing observed deterministic party controller, not the rejected
shadow battle head. No items, RNG scheduling, sacrifice or automatic entry retry.

An explicit verified battle-exit callback hands control to the scene specialist
before generic overworld-settlement inputs. Default battle behavior is unchanged.
The final checkpoint exception requires the declared Champion profile, actual Hall
map/stage0 or1, released controls, zero observed input and all concurrent referee
facts. It is a terminal scene snapshot, not generally input-ready overworld.
Current Champion evidence must remain present; no historical latch substitutes.

## Tests, review, and honest scope

ROM-free suites cover relocated script pointers, three starter branches, asymmetric
reversed movement, event operands, exact observation addresses, PP capacity/stock,
two-stage consumption, recording-to-dataset reconstruction, one-shot entry,
wrong trainer/payout/resources, stale scene/text/pending battle, battle-exit handoff
and the narrow concurrent terminal exception. An initial broad run was stopped
after26 minutes with6083 passes and21 failures because source/configuration edits
had overlapped its imports. It is not a valid frozen-build verdict. The failures
were a transient registry/source mismatch, stale runner fixtures, and an in-flight
active-state projection. The current product/registry suites pass141 tests and
the updated runner suite passes59. A full frozen-tree run remains separate from
these focused passes.

The operational contract golden changed because it fingerprints the entire battle
runtime module, including the new explicit exit callback. Its fixed digest was
updated after reviewing that source delta and testing unchanged default settlement;
the assertion remains exact rather than comparing a value against itself.

A read-only actual-state check caught a fixture assumption missed by the first
pure planner review: the observation adapter returns319 event bytes, not320.
The planner now uses the adapter's length contract and literal tests reject both
short and oversized data. No controller input or item was consumed in this check.

An internal bounded helper supplied pure tests and review. Its fresh-scene and
pending-trainer findings were accepted. Flash's short conceptual review usefully
challenged teacher-only drift; unsupported claims about passive reads consuming
input queues and a proposed latched completion flag were rejected. This was not
an independent source audit. Claude was not used; fresh external subscription
five-hour/weekly quotas are unavailable.

A final internal integration review found a genuine retention gap: execution
exceptions were saved, but a verifier, training observation or terminal serializer
could fail after gameplay and escape after emulator closure. The runner now
retains that exact state inside the live emulator scope, de-duplicates an already
saved identical boundary, and propagates the original error. A failed diagnostic
write is attached as an error note; it never creates an admitted training outcome
or checkpoint. This is a narrow repair for the pending real choice, not a new
campaign or permission to retry consumed inputs.

No Elixir execution, Champion victory, new story label, unseen evaluation,
cross-title transfer or Phase4 completion is claimed by this qualification.
Next: one sampled choice, retain its actual outcome, audit and fit only what it
supports. Reorient by the private two-hour mission deadline; no full replay.
