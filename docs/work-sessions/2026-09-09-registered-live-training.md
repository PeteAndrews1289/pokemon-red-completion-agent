# Registered live training — execution plan

- Capability: persist shared registrations through the actual collect/fit/continue loop.
- Learned authority: model-selected destinations and native collection/support goals produce
  actual registered-objective outcomes; deterministic execution is not learned combat.
- Transfer test: inherited registration and independent local stock in synthetic integration;
  the live batch is correlated Red training, not a transfer or held-out evaluation.
- Cheapest falsifier: authenticated saved endpoint plus two settled real choices, distinct
  new-objective corpus, reloadable fitted model, and exact terminal ledger agreement.
- Time box: reassess after two hours of integration; continue the bounded live objective if
  ready, without starting a full replay or expanding the architecture.
- Stop condition: lost specimens, unverifiable checkpoint/ledger, objective mixing, unsafe
  control, or exhausted bounded attempt. Preserve failures and never replay consumed attempts.

Scope: finish the regional outcome and live-launcher connections, import the authentic
saved endpoint, collect at least two eligible outcomes, fit and reload the new scorer.
The old114 corpus remains immutable. Initial behavior may use it, but new fitting does not
pretend its living-Dex labels used the revised objective. No sealed, Crystal or full replay.

## Integration checkpoint

The opt-in launcher now freezes an authenticated shared-registration snapshot, protects
actual party stock, writes verified terminal observations to a durable ledger, and
reconciles already-completed terminal imports after interruption without replaying them.
Regional choices have separate registered-objective schemas. The incremental loop waits
for two settled choices before its first fresh-objective fit and retains that objective's
own corpus on subsequent updates. Historical rewards are not pooled or relabeled.

Validation before gameplay: 212 targeted tests passed, including versioned regional
success/failure reconstruction, duplicate zero-novelty, session serialization, terminal
evidence tampering, stable read-only observations, and historical runner compatibility.
Targeted typing (four modules), lint, public-artifact, documentation and focus checks pass.
No real new-objective training is claimed at this engineering checkpoint; the next action
is a bounded live continuation from the retained native114 endpoint.

The first authenticated zero-input inventory imported38 registrations and42 specimens,
but exposed no eligible next goals. Read-only diagnosis found the registered demand
projection could suppress directly catchable missing evolved forms merely because a
precursor could eventually cover them. Direct missing entries now remain alternatives;
redundant base captures are still suppressed. The live adapter also carries the same
registered projection used to bind routes, preventing initial/fresh hash disagreement.
These follow-up changes passed120 targeted tests before repeating zero-input inventory.

The repeated inventory still found no choice. Its location list was also limited to
canonical preferred acquisition sources, excluding alternate cartridge locations.
Registered-mode enumeration now considers named ordinary land-encounter tables,
requires real reversible encounter terrain, and preserves route feasibility checks.
Noncanonical profile continuations require matching cartridge evidence; Safari remains
excluded because its capture rules differ.120 targeted tests passed for this extension.

## Actual live result and closeout

The three requested objectives are complete: live shared-ledger binding, registered
regional outcomes, and an actual collect/fit/reload/play loop. The integration checklist
is3/3; this is not a whole-project percentage or a completed Red Pokédex.

| Attempt | Actual result | Training |
| --- | --- | --- |
| Route7 search | Exhausted survey; no capture,519 actions/25,656 frames | Retained pending first fit |
| Route15 search | Captured Pidgeotto;1,333 actions/81,666 frames | First fresh fit:2 outcomes |
| Native capture choice | Model2 chose among2 goals; helper preparation failed after177 actions/10,896 frames | Refit:3 outcomes |

Total:2,029 actions/118,218 frames in957.494 seconds;38 to39 registrations,
42 to43 specimens, no specimen loss. The durable ledger contains baseline sequence0
and verified terminal sequences1–3. Model2's actual behavior hash matches the first fit;
the final model3 is saved but has not played. Neither older114 rewards nor parameters
were reused for fitting. Failures are observed selected-action targets, not successful
teacher demonstrations. Two regional labels and one native-goal label are not duplicated.
All three observations are correlated training, not independent validation.

[Path-free live evidence and fit](../evidence/red-registered-live-learning-2026-09-09.json).

### Failure and next bounded session

The last component raised `RedCapturePartyError`: the withdrawn capture helper was
not actually ready. It stopped in capture preparation before continuing the capture.
The terminal ledger and checkpoint agree on preserved inventory. Automatic resume is
false; do not assume a published terminal authorizes replay or a safe battle entry.

1. Restore and inspect the terminal helper's actual HP, moves and PP, comparing the
   preparation plan with the readiness predicate. Preserve the recorded failed outcome.
2. Fix the smallest general readiness/recovery mismatch with a ROM-free regression;
   use legitimate healing or a different eligible helper, never save edits.
3. Qualify recovery from the retained endpoint and let model3 make another real choice.
   Aim for12 new-objective examples as an initial collection budget, not a significance
   claim; retain successes, failures, search costs and collection changes.
4. After stable capture throughput, measure useful evolution-to-missing-registration
   choices, then Blue ledger sharing. Keep ROM-hack and Crystal transfer deferred.

### Audit and publication

The hosted suite exposed a legacy callback-identity regression after8,359 passing tests.
The registered adapter now implements its own projection, so the training callback can
again be the actual adapter observation method.72 focused tests passed after that repair.
Known-failing duplicate CI runs were cancelled; no unchanged failing run was rerun.
The dashboard now references the separate registered model, not historical114 training.
The final model3 was also reloaded read-only and its expected hash and objective verified.
Closeout validation passed212 focused tests, including72 runtime/learning checks and140
reporting, roadmap and compatibility checks. This is not a claim that hosted CI finished.
The active board keeps its older counters immutable and separately verifies the new
registered-example count against the hash-pinned fit receipt.

No external-agent audit was needed for this bounded integration. Their quotas were not
queried or consumed. The mission and North Star requirements are unchanged. Next-session
recommendation: Astra High, Fast off; focused recovery/debugging does not need Max by default.
