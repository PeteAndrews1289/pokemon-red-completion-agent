# Fresh-state goal-chain session — September 6

## Mission check

1. **Capability:** turn useful preparation and captures into successive completion decisions,
   observing the actual party, inventory and retained collection after every skill.
2. **Learned authority:** reuse the saved 29-example option model through its existing safety
   shell. Count only model-selected goals as learned choices; safety and forced actions are separate.
3. **Transfer test:** this first chain is a training-context integration diagnostic, not a new
   held-out result. Preserve the portable question/action vocabulary. Crystal remains deferred.
4. **Cheapest falsifier:** the first authenticated training catalog context with at least three
   recorded local options, excluding the retired train and paired roots, gets at most four steps
   per actor through the existing bounded player. A stale/exhausted menu or failed skill is a result.
5. **Time box:** two hours to the first terminal check and reorientation. Prefer the existing
   executable to a new routing or training framework.
6. **Stop condition:** consumed-trial reuse, stale state, unverified collection change, lost
   diagnostics, hidden teacher replacement or the fixed step/action/frame ceiling. Preserve the
   first result; never rerun it as the original four-root comparison or fit its diagnostic rows.

## Starting evidence and prospective scope

The four-root comparison is complete: three successful model-selected side goals, zero new model
captures, one setup failure and one control exception. The [audit](../audits/red-routed-model-play-2026-09-06.md)
explains why this does not yet demonstrate collecting. The fixed 29-example model is unchanged.

The catalog's first qualifying training entry is `red-goal-v1-010-acquire_species-train-01`, with
acquisition, field restoration and exploration available. This is a different training-context
integration question, not a replay of the eight retired lessons or four paired scenarios. Verify
its original catalog/plan/profile identities and physical exclusion before opening the game.
If excluded, stop and report that admission result; do not silently select a better-outcome root.

The read-only check found this training root had previously supplied a setup capture. That fact
does not make it a fresh independent root, and its old claim remains unchanged. It does **not**
overlap the eight retired-bank physical roots. NORTH_STAR permits restoring authenticated
training contexts under a prospectively bounded curriculum; the existing player is explicitly a
repeatable same-state development tool. This four-step integration uses that scope, not the old
setup-capture or one-shot causal plan. Pass `--context-origin training`, retain the original catalog
identity, emit no fitting rows, and make no independence claim. Do not delete, rewrite, recover or
re-execute any old assignment to do it. A global used-before marker is not a claim of new
independence and is not being erased to manufacture one.

Run the existing `run_paired_red_bounded_player.py` with the living-Dex artifact, four-step limit
and explicit continuation after first progress. Budgets remain 6,000 actions / 600,000 frames per
step and 24,000 actions / 2,400,000 frames per actor. The separate fixed control remains diagnostic.
Training-root provenance must accompany any public result; no independent superiority claim or
model update comes from this check. All four retired paired outcomes and the reserve stay intact.

One minimal pre-input repair is justified: this older bounded loop still collapses executor
exceptions into empty evidence. Preserve the existing path-safe diagnostic in private trajectory
evidence and hard-stop terminals without changing policy choices, recovery limits or public
result claims. Verify that containment with targeted tests, then publish once and execute.

The dashboard should show actual frames, actor, ranked choices, party state, collection and
freshness. It is a passive viewer, never a replacement actor. After the result, decide whether
the next necessary work is goal availability/continuation or additional train-only contrasts;
do not assume that four steps imply three genuine learned decisions.

## Pre-input engineering result

The existing loop now forwards ordinary skill and budget exceptions to a private diagnostic
recorder before recovery. Logging cannot act on the game, and a failed diagnostic write stops
further recovery. Hard-stop terminals retain the same path-safe cause. Public goal results do
not gain private messages. The live-arm wiring is exercised with a recording sink, not only
inspected as source text.

The command also records declared context origin and explicitly disclaims independent
generalization. `--context-origin training` reaches both private arm headers and public summary;
it does not alter the model, safety shell or budgets. The current focused set passed **99 tests**;
all 389 source files passed typing, and full lint/docs/active-state checks passed. No three-choice
production result or new fit is claimed at this pre-input checkpoint.

The separate registry/product-state/viewer group also passed 154 tests: **253 focused tests**
across the two non-overlapping file groups. Public-artifact verification passed. PR 232 is merged;
PR 233's CI passed, but its squash-parent history needs alignment. Keep that publication detail
separate from source-tree qualification and the next real integration run.

## Closed: one learned capture, then the resource-continuation seam

Both actual arms terminated after two of the permitted four decisions. The model made one
genuine acquisition choice, gaining one living/registered species, then deterministic exploration
failed after the last capture ball was spent. Both five-stream trajectories authenticated and
all 704 actions / 31,344 frames reconcile. No new fit or independent superiority follows.
The next [resource-aware session](2026-09-06-resource-aware-goal-chain.md) connects existing
supply and transport skills; this original pair and result remain terminal.
