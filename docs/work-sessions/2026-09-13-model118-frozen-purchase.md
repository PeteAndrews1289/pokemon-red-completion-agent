# Model118: exact frozen purchase succeeds

## Outcome

Model117's exact frozen resupply candidate ran once from its authenticated parent after an
action-free reconstruction proved the same model, menu, private binding and state. The composite
made one verified Fly and bought three Ultra Balls for exactly1800. Cash changed2146 to346 and
the bag changed only by those three items. It used134 controller actions /7716 emulator frames,
kept85 registrations /65 living species /69 specimens and issued no teacher or execution-time
policy query.

The choice was not redrawn or retried. Exact-source CI run34780233067 passed before controller
input. The terminal was retained and independently re-observed before fitting.

## Learning and continuation

The verified resource success became row118. Model118 has **118 settled examples /80 successes**,
including14 measured-source and39 economy-qualified rows, while preserving all117 prior rows. The
fit reduced in-sample weighted MSE from0.0311424461 to0.0120639699. This is training-only evidence
without an action trace, independent evaluation or authority promotion.

The terminal reopened as a zero-input restart. An action-free Model118 menu then exposed two kinds:
one remaining fishing acquisition and team restoration. Model118 froze candidate1, restoration, at
probability **0.6782507836124833**. The menu build and choice used zero controller actions, emulator
frames, teacher labels or training examples. That choice is unexecuted; gameplay is stopped.

## External contribution

Gemini3.8 Flash High completed a read-only audit of preflight effects, binding integrity, exact
purchase accounting, failure retention and measured-choice admission, returning PASS with no
blocking finding. Claude Sonnet independently returned PASS with two non-blocking advisories. Its
request to confirm the hosted run against the exact execution commit was accepted and satisfied
before input; its observation that preflight failures intentionally create no gameplay receipt was
informational. Neither reviewer edited the repository or ran gameplay. Current service quota was
not available from either CLI.

## Verification boundary

The exact preflight passed with zero input and unchanged state. The focused admission gate passed84
tests, style and changed-source type checks. Hosted CI passed the exact execution commit. The broad
local gate passed11567 tests with one expected failure after excluding one pre-existing machine-only
assertion: this Mac's installed PyBoy2.7.0 `RECORD` metadata hash differs from the repository-pinned
runtime identity. That local environment drift did not affect hosted CI, the exact private runtime,
or the measured terminal, but it remains an explicit local qualification limit.

## Product gate and next session

The required finish line remains unmet: one authenticated non-deterministic, model-directed Red run
must complete the story and full local Red Pokédex before any ROM hack. Version, trade and event
dependencies remain explicit; the current39-entry remainder is only the124-entry native scope.

Next, execute Model118's exact frozen restoration once from its authenticated purchase terminal,
retain the real result and fit only eligible evidence. No redraw, retry, policy requery or teacher
fallback. Estimate: one bounded session,60–120minutes.

Recommended model: **GPT-5.6 Sol, High effort, Fast off**. This is another exact frozen execution,
recovery verification and measured fit; Astra is not needed unless the work expands into fresh-run
authority or dependency architecture.

[Evidence](../evidence/red-model118-frozen-purchase-2026-09-13.json) ·
[Roadmap decision](../roadmap-decisions.md)
