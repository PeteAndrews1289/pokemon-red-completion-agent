# Capture prerequisites, not more failed throws

BatchK reached the model-selected Tower3F, spent18 ordinary balls, added no
registration, and retained/fitted its failure once (model28). Actual1071 actions
and85140 frames are retained. A zero-input authenticated terminal inspection
confirms Tower3F(y8,x8), ready field, no balls,3503 money and no Silph Scope.
51 registrations and46 specimens remain; no successful indoor capture yet.

The pinned [battle implementation](https://github.com/pret/pokered/blob/1e96034092686d006e863cace09e87273051a3d8/engine/battle/core.asm#L3228)
classifies wild Tower encounters without the Scope as unidentified ghosts; the
[ball implementation](https://github.com/pret/pokered/blob/1e96034092686d006e863cace09e87273051a3d8/engine/items/item_effects.asm#L144)
bypasses capture calculations for them. RAM still exposes an underlying species;
recognizing that species does not establish capturability.

## Mission check

- Capability: qualify acquisition prerequisites before travel/spending and reject
  impossible throws at the controller boundary.
- Authority: existing learned destination choice remains; deterministic legality
  screening is maintenance unblocking useful collection, not a new learned skill.
- Transfer test: source location versus actual location, different Tower floors,
  item absent/present/zero count, non-Tower sources and changed pre-throw inventory.
- Cheapest falsifier: a Tower offer with balls but no Scope must be unavailable;
  no input may be issued by an impossible throw.
- Time box:45minutes to qualification/tests and an actual-state check.
- Stop: invented inventory, retroactive relabeling, consumed retry, resource reset,
  or claiming that this limited guard solves every special encounter.

New offer filtering will be an explicit prospective profile transition. Historical
menus keep their original semantics. Subsequent recovery must buy supplies through
existing legitimate mechanics; no refund of the18 wasted balls. General special
encounter/event dependency solving and item withdrawal are not completed here.

## Integration audit

The explicit `capture-access-requirements` transition adds destination item
requirements while retaining old menu meanings. The controller independently
rejects unidentified ghost throws before bag/controller access and after any
pre-throw preparation. No RAM addresses were added to strategic code.

489 relevant tests passed, including absent/present Scope, floor boundaries,
non-Tower sources, trainer versus wild battles, runtime-factory opt-in, profile
retargeting, and a Scope disappearing during preparation. Source type checking
passed for468 files. The tests do not prove item retrieval or all special captures.

The unchanged model28 endpoint authenticated with zero input before publication.
After publication, verify it again with the prospective profile and start a fresh
bounded continuation. Existing recovery must purchase balls legitimately; the
next capture source remains the model's choice. No more agent quota was consumed
in this repair; Flash's stone draft remains isolated and unpromoted.

## Authenticated batch closeout

One retained failed source choice /one fit: model28,51 registrations,46 specimens. The actor reached Tower3F without Silph Scope and spent18 balls on uncatchable ghosts. 1071 actions /85140 frames retained;0 balls and3503 money remain. No new registration.

Next: Publish prospective capture prerequisites and final throw guard, authenticate model28, then use legitimate resupply and fresh supported collection choices. No resource reset, consumed retry or forced destination. Item withdrawal and general puzzle solving remain unfinished.
