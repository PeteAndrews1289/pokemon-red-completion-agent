# Connect the prepared party to a real story option

## Mission check

1. **Capability:** reusable, observed trainer approach and bounded party-aware combat,
   connected to the existing goal menu rather than another fixed chapter recipe.
2. **Learned authority:** the goal model chooses story versus preparation. Battle controls
   stay deterministic; the learned battle scorer remains shadow-only.
3. **Transfer test:** varied trainer identities, party order, low HP/PP, changing opponents,
   wrong battle identity, stale route targets and exhausted switch budgets.
4. **Cheapest falsifier:** ROM-free controller/provider tests followed by action-free native
   enumeration from the retained Indigo state. Preserve the story opportunity until the
   model can actually select it.
5. **Time box:** one two-hour integration session. Reuse existing route, trainer identity,
   matchup, menu and checkpoint mechanisms. No new preparation research or outside audit gate.
6. **Stop condition:** unsupported route/battle boundary, loss of a living specimen, hidden
   item spending or inability to preserve exact support ancestry. Retain failures; no rewind.

Implementation order: opt-in profile-bound story controller; then truthful import of the
recorded zero-label support terminal into native continuation; then a genuine story choice
when both are verified. No sealed Red, Crystal, full replay or claimed-trial retries.
The current terminal remains5c66ec39, with236actions/19740frames of prior support, model76.

## Action-free result and bounded adjustment

The real runtime exposes exactly one executable Indigo goal: advance story. The old
profile remains unchanged, and no action or model query ran during enumeration. Do not
invent a preparation alternative or claim the forced option is a learning example.
After the zero-input historical import, use one recorded native singleton rehearsal
to qualify actual boss control. Preserve any failure without retrying its choice.
Story-versus-preparation learning remains open until multiple useful options really exist.
