# Model111 cross-box capture support

## Outcome

Capture preparation can now inventory all twelve verified PC boxes and select a helper from actual
sleep/paralysis moves with usable PP. It does not select by species identity. If the party is full,
the planner excludes full helper boxes, preserves the strongest living escort and refuses to
deposit the last carrier of any field move. Execution uses the existing checked box-switch,
deposit and withdrawal primitives.

After preparation, the complete party-plus-storage specimen multiset, bag, money, battle state and
input readiness must match their protected expectations. The originally selected acquisition is
rebound from the new PC origin without another policy query. Setup creates no training row.

## Verification

- 304 focused observation, storage, capture-party and routed-support tests passed.
- Ruff and mypy passed for the changed source.
- Exact source `e9fb7cc83dba84e1ca966a2dae3e740c2ed6a4b5`, bundle
  `ff562c7dad41eb8ac4d93c5a797ab16f7db98fd025350148221b446f4c101cf5`, passed
  GitHub CI run `34733442341` attempt 1 in 30m47s.
- The exact recovered model111 checkpoint was then rebuilt under a read-only controller: zero
  actions, zero frames, byte-identical state, unchanged model and 83 registrations.

The local full suite also reached 1,806 passing tests before one unrelated Mac-only historical
runtime-identity check rejected locally installed PyBoy metadata. Linux CI skipped that
host-specific branch and passed all repository checks. This did not affect the focused feature
qualification or the recovered checkpoint.

## Falsifier result

The exact menu remained forced-only: `restore_team` was the sole executable goal and acquisition
remained `no_legal_target`. This is not a cross-box failure. The current profile is pinned to
Seafoam Islands 1F and all eight species in that source are already registered. Cross-box support
can prepare a selected acquisition; it cannot manufacture missing demand in an exhausted source.

The same action-free inspection still found 18 productive fishing maps and four reachable ones.
The repository already contains identity-free fishing destination features, a model selector and a
mixed-goal menu, but their live executors are assembled in private run scripts. That is now the
active product gap.

## Mission check

| Question | Answer |
| --- | --- |
| Reusable capability | Retrieve capture support by observed capability from any verified Red box while preserving the collection and original goal. |
| Learned authority | Unchanged: model111 remains at 111 settled examples, 76 successful. |
| Product relevance | Removes active-box locality as a hidden assumption, which matters on arbitrary retained saves and later collection runs. |
| Correction | The prior handoff overstated helper locality as the immediate menu blocker. Exact inspection proved source exhaustion is immediate; both facts are now separated. |
| Cheapest next falsifier | The reusable runtime cannot expose at least two reachable productive fishing candidates beside ordinary goals without identity leakage, state change or a second policy query. |
| Stop condition | No destination-specific profile, private-only executor assembly, fake learning row, consumed-state replay, Blue/Crystal work or full replay. |

## Next session

Move cartridge-derived reachable fishing candidate construction and its bounded executor out of
private orchestration into a tested reusable runtime. Join those candidates to the existing mixed
option set, wrap the selected acquisition with cross-box capture preparation, and rebuild model111
action-free. Only then allow one fresh model-selected bounded outcome.

[Evidence](../evidence/red-model111-cross-box-capture-support-2026-09-13.json)
