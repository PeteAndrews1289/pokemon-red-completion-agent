# Recorded economy decisions and replay

Status: engineering connection complete; real model initialization published
privately. Live funding execution remains next, not an accomplished income result.

## Mission check

1. Capability: recognize useful supply shortfalls and retain actual earnings/losses.
2. Learned authority: the existing sampled goal policy receives current cash facts;
   only its actual eligible choices can teach the new economy head.
3. Transfer test: change cash, useful supply demand and observed proceeds without
   changing map/species identities; replay must reproduce the same decision facts.
4. Cheapest falsifier: stale preselection cash, altered budget or altered endpoints
   must reject admission; old journals must still replay unchanged.
5. Time box: one bounded integration session, at most two hours, then reassess.
6. Stop condition: missing evidence, changed historical contracts, fabricated labels
   or unsafe live activation. No new campaign or full replay to test this seam.

The repeated no-live-learning alarm is acknowledged: connect the existing runtime,
not another standalone model. AM01/model65 is preserved until the complete seam
passes. Flash's objective is already integrated; no duplicate draft or standing
external review is needed for these tightly coupled authority/evidence changes.

## Delivered

- Explicit `--economy-training` requires a registered continuation and a v4
  model. Its new registered plan/event versions retain the bound capture reserve,
  purchase limit, item and price; older plans and journals keep their meaning.
- Before each policy selection, the recorder observes current cash and inventory
  without controller input, derives useful supply demand and records the context.
  Policy inputs contain cash/target only, not inventory identities. A stale cash
  quote or a changed observation between selection and execution stops admission.
- Replay reconstructs that same budget and policy probabilities, validates the
  registration binding and controller interval, then derives actual cash gain/loss.
  Missing observations remain unknown, interruptions have no targets, forced and
  unsupported decisions remain excluded. Expected income never becomes a payout.
- The regular update path fits, publishes and reopens a v4 economy model in
  synthetic end-to-end tests. Existing model baselines upgrade correctly for v4.
- A corpus-authenticated initialization enables the new inputs with zero weights,
  retaining the registered objective and historical predictions; it is not a fit.

## Actual saved artifact, not synthetic learning credit

Authenticated the actual AM model and all65 retained examples, then initialized
the v4 record `6cd88d0f3acb08ae66e72b18cf0e3bad0edefeedc58a7c128653aa144b837e92`
from `fad0b1bce9887862fb1214a6b95d1d43cc96457f8029f50715f189c72a03238b`.
The corpus remains `9f9a3a86a9315d8f702468468aff32c492b9e0de7a2f0e92a6322858471b7997`.
Executed source: `3d508eb3570605c467ba510d448b62a8be594cd0`.
[Initialization receipt](../evidence/red-economy-initialization-2026-09-11.json).

Zero new examples, fits, controller actions, registrations or authority promotions.
The economy head is absent until eligible real outcomes exist. AM01 remains the
same Fuchsia Center game save:64 registrations,54 specimens,593 money,zero balls.
The new model record is ready for opt-in continuation; it has not played a goal.

## Verification and findings

314 targeted tests passed, including19 new economy recorder/replay tests and the
native runner, old journals, registered fitting, codecs and policy regressions.
Ruff and configured mypy passed for477 source files. No full-suite pass is claimed.
Tests cover changed cash across consecutive decisions; altered budget, cash,
question and outcome; missing context; unknown money; interruptions; failure loss;
stale postselection state; real-parent rejection; model publication/reopening;
and preserved initialization predictions and corpus identity.

The first tests exposed that registered observations nest their resource facts
under `semantic_observation`; the integration now reads that explicit projection.
Fixture fixes retained safety gates and refreshed the second decision's quote
instead of weakening the stale-cash check. Synthetic tests stub only their absent
private parent; a separate test confirms the v2 schema still rejects that parent.

No Flash or Claude calls were made this session. The previous Flash objective was
reused directly; duplicating this tightly coupled recorder/replay work would have
added integration cost. No external quota reading or new savings claim is made.

## Reorientation and next session

The missing software seam is now connected, so do not begin another objective
design or audit campaign. Use the initialized v4 model with exact AM01, verify the
opt-in plan and supply contract without input, then qualify legitimate Center
departure and one bounded income attempt using existing trainer/battle mechanics.
Keep actual costs, failures and any forced support visible. An opening matchup
quote is not proof of League survival or profit; do not assume a solo sweep.

Budget60–120 minutes for that qualification/attempt, not guaranteed sustainable
funding or Red completion. Stop and diagnose an observed execution failure rather
than replaying the game or inventing another experiment. No sealed Red, Crystal,
full replay, reset or consumed retry. The resource-recovery checklist remains2/3;
neither initialization nor tests complete its income/collection item.

Recommended next session: Astra High, standard speed. The task is bounded live
qualification and diagnosis, not a new architecture; higher reasoning or Fast is
not justified by this session's findings. This is a recommendation, not a settings
change. [Official usage guidance](https://learn.chatgpt.com/docs/pricing).
