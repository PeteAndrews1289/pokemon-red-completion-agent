# 2026-09-15 fossil/evolution bridge

Status: closed — exact fossil target unavailable; no gameplay

## Mandatory mission check

| Question | Answer before implementation |
| --- | --- |
| Reusable capability | Let an already-implemented, observation-derived fossil revival occupy the portable `acquire_species` family beside an executable evolution, without exposing fossil or species identity to the goal model. |
| Learned authority now | Model121 has 121 settled examples, 83 successes and 86 local Red registrations. Fossil execution exists as deterministic support; it grants no goal-selection authority by itself. |
| Learned authority target | If and only if the exact terminal exposes both executable families, Model121 may choose once between portable acquire and evolve; only the settled observed outcome may change its development evidence. |
| Transfer test | Preserve the title-neutral acquire/evolve policy surface so later games can replace Red fossil mechanics below the same high-level choice. No cross-title result is claimed here. |
| Cheapest falsifier | The exact Model121 terminal has no executable unregistered fossil target, or binding it requires controller input, gameplay preparation, or policy-visible fossil/species identity. |
| Time box | One session / two hours. |

## Plan

1. Add an internal trusted fossil inventory/binding seam to the full-Pokédex proposal while keeping the public proposal closed to caller-supplied executors.
2. Preserve executable wild capture, then reachable fishing, ahead of fossil fallback; replace only an unavailable acquisition family.
3. Run ROM-free contract tests and an exact action-free Model121 terminal scan.
4. Execute one bounded model-selected lesson only if acquire and evolve are simultaneously executable at that unchanged terminal. Otherwise publish the falsifier and stop gameplay.

## Immutable boundaries

- No reset retry, protected-root scan, source-four use, route patch, teacher fallback, sealed Red evaluation, Crystal execution or full replay.
- Qualification performs zero model queries, claims, controller actions and emulator frames.
- Fossil and species identity remain below the portable `acquire_species` policy row.
- Gameplay remains stopped unless the unchanged two-family gate passes.

## Result

The exact committed Model121 terminal hit the cheapest falsifier before any integration
work was justified. Native evolution remains executable, but the observation-derived
fossil inventory contains zero executable targets. The two-family prerequisite is false.
The scan preserved the exact state byte-for-byte and produced zero model queries, reset
claims, controller actions, emulator frames, outcomes and fits. No gameplay ran and no
source adapter was added.

Private record `model121-fossil-evolution-bridge-gate-20260915-v1` seals the result with
record SHA-256 `a588d3d87a4a66d84d88d3d6b825b942ff13233b90581d69981a25e7bad0f766`
and manifest SHA-256 `f46b09907e271522136f2930840b052314f5d385f280c82562dbf3474c81696b`.
Path-free public evidence is
[`red-fossil-evolution-bridge-gate-2026-09-15.json`](../evidence/red-fossil-evolution-bridge-gate-2026-09-15.json).

The unchanged fossil and full-Pokédex proposal contracts pass 36 focused tests. Flash 3.8
High independently recommended ending the serial provider-probe loop: execute the already
ready evolution as a deterministic forced singleton with no fit or authority claim, then
rebuild the menu from its fresh retained terminal. Claude CLI connected but returned no
completed verdict.

## Decision

Do not implement a fossil bridge for an unavailable exact target and do not substitute a
different acquisition mechanic in this session. The next session must inspect the ready
evolution's resource type and preservation constraints action-free, then freeze and execute
it once only if it is a safe level-triggered transition. Its outcome advances the local
Pokédex but is not a learned choice. Rebuild the heterogeneous menu from the retained fresh
terminal and stop if the transition consumes a finite item or protected physical precursor.
