# Phase5: collection and incremental training are running

The accepted Flash/Claude catalog package was pushed at `24e0f796`.
This session reused the existing native runner and fitter: **85 → 86 → 87
examples**, preserving every prior row. It did not build another model or
training-admission framework. [Measured evidence](../evidence/red-phase5-native-learning-2026-09-09.json).

## Mission check

- Capability: sustained living collection with legitimate supply and physical retention.
- Authority: sampled high-level recovery/acquisition choices; deterministic mechanics remain.
- Transfer: changed Red stock, destination and post-game context in the same correlated lineage.
- Falsifier: one bounded played choice with actual collection and resource outcomes.
- Time box: two hours for the first new lesson; stop at the first runtime failure.
- Stop: preserve that failure, fit its eligible observed outcome, then diagnose without replay.

## What actually happened

Normal credits, save and Continue returned the existing game to Pallet. All30
specimens, inventory and30,418 currency were preserved. These288 actions and
17,172 frames are deterministic support, not learning. Two administrative
imports failed on path-free prose and missing checkpoint-anchor metadata; both
remain preserved. The successful import normalizes prose, records its original
digest and explicitly marks the verified anchor added at import. No game input
was repeated, no external reset occurred and no resources were injected.

| Step | Played outcome | Actions / frames | Fit rows |
| --- | --- | --- | ---: |
| Legitimate supply | Bought10 Poké Balls for2,000 at Viridian | 171 / 10,164 | 0 |
| Model chose recovery | Restored at Center with capture also available | 49 / 2,928 | 1 |
| Source proposal + forced capture | Reacquired Spearow;3 balls,9 encounters | 639 / 42,900 | 0 |
| Model chose capture over resupply | Storage preparation succeeded; Route3 replanning failed | 615 / 33,421 | 1 |

Native85 played recovery; native86 played both subsequent episodes. Native87 is
fitted but has not played. Supply safety, the forced capture and the uncredited
source proposals were not relabelled as training examples. The actual failed
capture choice and its costs were fitted, not discarded. Total native gameplay:
1,474 actions /89,413 frames. There was no teacher fallback.

The collection now retains31 specimens across29 species, versus30/28 initially.
Registrations remain33 because Spearow was already registered but no longer held.
There are91 missing living targets under the unchanged120-target contract.
Current stock:7 balls,28,568 currency,20 spaces in the newly selected box.
This is not full Red collection, learned combat or fresh-game autonomy.

## Catalog integration and unresolved boundaries

The accepted catalog was exercised against actual, zero-input checkpoint
observations:156 declared acquisition options plus physical stock and
cartridge-derived level edges. Drowzee→Hypno and Oddish→Gloom are useful surplus
transformations. They are **not** proven currently executable merely by this
report. Unknown reachability and executor qualification remain blocked; the
catalog is an explanatory projection, not a substitute for native bindings or
a new policy authority. Native Drowzee evolution was exposed prospectively,
but not offered at these boundaries. Do not claim it ran.

The local collection-start checklist is **2/3**, not Phase5 completion: supply
and real alternatives were demonstrated, and a model-selected collection
attempt was measured and fitted. Full current attainability/one-shot/dependency
coverage remains unfinished. The measured collection attempt failed; this
checklist does not imply successful learned collection or policy advantage.

## Failure and next action

The first runtime failure stops this session's gameplay. Its exact state and
typed cause are retained. The replan raised `route replanning found no candidate`
with a nested Cut-capability error. A fresh **zero-input** route computation from
the actual terminal, including observed NPC occupancy, finds a57-step walking
route to the same target. Thus “needs Cut” is not an established diagnosis.
Inspect the executor's accumulated blocked cells, trainer interruption and
replan state. Do not add a Cut workaround or a hard-coded Route3 walk.

Next: reproduce that request without controller input, add a ROM-free regression
for the actual stale-blocker/interruption defect if confirmed, and qualify the
smallest safe repair. Only then declare a bounded native87 continuation. No
rerun of the consumed episode, new boss cohort, full replay or sealed access.
Where capture is the only native goal but multiple destinations are executable,
reuse the existing destination-choice/fitter lane prospectively. Do not
retroactively credit this session's discarded proposals.

## Validation and delegation

**145 targeted ROM-free tests passed** across catalog/demand/edges, support
imports, incremental fitting, native goal proposals and regional choice learning.
Both fits passed full retained-row and outcome admission checks. The final fit
reduces in-sample weighted MSE from0.01185223 to0.01156980; no held-out result or
policy promotion follows. No full test suite was rerun this session.
The updated native87 dashboard and roadmap then passed51 additional focused
tests. An overlong saved-location label was caught locally and shortened; no
UI limit or assertion was weakened. The regenerated infographic was visually
inspected. The overview server now reads this active T7 checkout and labels
the saved failure as stopped rather than pretending training is still running.

No additional Claude/Antigravity session or quota query was needed. Their
accepted package was reused rather than commissioning another architecture
review. Hosted CI34374039795 was in progress at the mid-session check; neither
gameplay nor fitting waited for it. It finished green before closeout. Main
runtime source was unchanged throughout.

Private resume material is in the existing saved-endpoint-learning session
directory: `phase5-collection-20260909-04-declaration.json`, the paired goal/fit
and catalog-audit results, and `phase5-collection-20260909-04-route-diagnosis.json`.
Use authenticated model87 and the explicit failure context; no automatic retry.

Next-session recommendation: **Astra High, Fast off**. The named task needs
careful state/replanning diagnosis; extra inference speed does not shorten
local emulation, source enumeration or fitting.
