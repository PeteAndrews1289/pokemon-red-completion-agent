# Resource-cost learning session — September 6

## Mission check before implementation

1. **Reusable capability:** choose useful collecting goals while accounting for real purchase
   costs and reserve needs. Known prices are adapter facts, not outcomes the learner must invent.
2. **Learned authority:** preserve the existing learned success/progress ranking and deterministic
   safety shell. Add an explicit, opt-in economic contract beside the unchanged learned projection;
   this repair alone is maintenance, not new training or promoted authority. The named next
   learning output is a small prospective train-only outcome batch and an actual retained-data fit.
3. **Transfer test:** the economic interface contains only numeric costs, funds and resource
   counts/targets, never maps, species, ROM identity or private bindings. Changed-budget and
   changed-reserve cases must differ without controller input. Later Red outcomes remain bounded
   development evidence; no Crystal transfer or independent superiority is implied.
4. **Cheapest falsifier:** identical available goals with different budgets/reserve deficits must
   produce distinguishable public quotes and economically different adjusted scores. Legacy
   unquoted questions, model features, outcomes and all 29 old training rows must stay unchanged.
5. **Time box:** one session, at most four hours from 15:19 UTC. Prefer existing player, collector
   and fitter interfaces. CI remains a background publication check, not a development dependency.
6. **Stop condition:** stop rather than build a new campaign factory, invent targets, relabel
   diagnostic outcomes, retry a consumed trial, or expose private identity to the policy. Record
   a concrete blocker if useful prospective training cannot proceed within this session.

## Sequence

- Add and test the smallest optional semantic quote and policy-cost integration.
- Bind quotes to fresh Red observations and actual executable Mart quantities/prices.
- Qualify legacy compatibility and economic contrasts before any controller input.
- Inspect the existing training contracts for a legitimate small prospective batch; preserve
  original lineage, real selection probabilities, negative outcomes and every prior example.
- Run and fit only when that path is actually supported; otherwise report the precise missing
  seam rather than disguising engineering checks as learning.
- Close with measured outcomes, handoff, narrative and a short next-step reorientation.

No previous completed campaign is reopened. No sealed Red, Crystal or full-game replay is in scope.

## Bounded implementation decision

The existing player writes diagnostic goal trajectories; the legacy fitter admits a separate
setup/candidate-trial journal. Do not fabricate those older setup attestations for player actions.
The minimal remaining connection is a prospective train-only player trajectory adapter: log
full-support model-guided sampling before input, reuse the exact existing nine outcome definitions,
and admit only complete authenticated new training episodes alongside the unchanged prior corpus.
Safety and forced choices remain visible but are not full-support fitting examples. Any same-root
episodes stay one training lineage, never independent evaluation samples. This is a connection
between the existing player and learner, not a new route generator or sealed campaign protocol.

## Prospective native training batch (before controller input)

- Two new episodes only: `red-native-econ-20260906-a-causal` and
  `red-native-econ-20260906-b-causal`, sampling seeds **17** and **23**, respectively.
- Both start from the original authenticated train-010 capture, under its existing catalog
  assignment and lineage. These are two correlated curriculum resets, not two independent roots.
  They are new player-training episodes, not retries of any previous diagnostic pair or trial.
- Use the existing routed capture/Mart profile and unchanged 29-example behavior checkpoint.
  Enable economic quotes, continue after progress, retain terminal checkpoints, and stop after
  at most four goal decisions per episode. Existing action/frame/route bounds remain unchanged.
- Sample supported non-safety choices from 75% model-softmax plus 25% uniform exploration.
  Record the exact distribution before input. Forced, unsupported and safety choices remain
  visible but excluded from full-support fitting. Retain failures and interruptions explicitly.
- No baseline arm or superiority claim. Include both complete episode manifests in admission;
  do not omit an episode because its outcomes are unfavorable. If an episode is incomplete,
  stop and retain it rather than retry it or silently select a successful subset.
- Fit the existing nine-head selected-outcome learner only after admission, retaining the full
  prior corpus byte-for-byte. The money quote is a deterministic rule beside predictions;
  the old consumable-only target is unchanged. In-sample prediction error is not a gameplay win.

The native fitter writes a separate model/corpus format and never fabricates legacy setup,
one-shot claims or CI attestations. Its model can be read by the bounded-player loader. The small
fit CLI currently supports a batch collected under one behavior checkpoint; mixed-checkpoint
history requires explicit typed episode/model inputs, not inferred provenance.
