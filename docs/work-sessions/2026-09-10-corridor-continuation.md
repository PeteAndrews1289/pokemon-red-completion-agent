# Resume the exact chosen corridor

## Mission check

- Capability: retain model-selected collection choices across saved-state steps.
- Learned authority: preserve the capture-source proposal and native goal choice;
  this repair must not replace either or invent a training target.
- Transfer test: changed warp coordinates, alternative lane positions and both
  registered and legacy reconstruction paths.
- Cheapest falsifier: profile reconstruction excludes the same warp/object cells
  as source enumeration, producing the same exact profile hash.
- Time box: one hour inside the overnight window ending12:41UTC.
- Stop condition: a true checkpoint identity mismatch, unsafe restore or need to
  bypass scope verification. Preserve two completed outcomes; never replay them.

BatchH completed two model-selected evolutions (Nidorina and Nidorino) and fitted
both: model21 to23,47 to49 registrations. Before step3, checkpoint scope validation
stopped the cycle. The existing per-step outcomes/fits remain durable despite the
missing aggregate report. Source inspection identified inconsistent warp-coordinate
exclusion between prospective source enumeration and reconstruction; verify against
the actual chosen profile before changing it. No checkpoint guard may be weakened.

## Authenticated partial closeout

Local validation:140 continuation/regional/indoor tests and112 focus/roadmap tests
passed. Lint,465-file type checking, documentation and registry checks passed.
The prior indoor implementation's hosted CI34444173577 succeeded; no rerun was
needed. This narrow repair keeps checkpoint guards unchanged and introduces no
new learner authority, reset or unplayed-example claim.

2 completed model-selected evolutions /2 fits: Nidorina and Nidorino. Model23,49 registrations,46 specimens. 7741 actions /677718 frames. Step3 stopped before input on a profile-reconstruction mismatch. No indoor capture yet.

Exact-ROM comparison confirmed MansionB1F reconstructs the same lane, but Mansion3F chose (9,13) without warp exclusions versus the committed (9,14). The mismatch explains why step2 restored but step3 did not. Reconstruction now uses the same registered-only exclusions; checkpoint identity checks stay unchanged. Each future completed step also saves its full preparation/outcome/fit record before progressing.

Next: Verify exact model23 terminal restoration with matching corridor exclusions, then a fresh bounded continuation. Preserve two completed attempts and their fits. Let the model choose among capture and remaining evolutions; no forced destination, reset or replay.
