# Planning throughput and fresh model7 continuation

## Mission check

1. Capability: practical repeated planning for missing-registration destinations.
2. Learned authority: unchanged by maintenance; unblock fresh model7 choices and
   fit only their actual played outcomes.
3. Transfer test: ROM-free maps with different warp layouts, observed starts,
   borders and graph mutations between calls; preserve route semantics exactly.
4. Cheapest falsifier: compare the optimized projection against the previous
   semantics across synthetic grids; unrelated exits must remain untouched.
5. Time box: two hours including a bounded fresh continuation and closeout.
6. Stop: changed safety semantics, unrecoverable input boundary, stale evidence,
   or insufficient time for another bounded attempt. Never replay consumed work.

The prior measured hotspot rebuilds every tile's outgoing edges merely to make
unrelated warps absorbing and restrict departure from an observed warp start.
Use a fresh shallow mapping with replacements only at affected coordinates;
immutable edge tuples can be shared. No global cache or cross-state reuse.
Flash High receives a narrow read-only correctness review; Codex owns the patch,
measurement, controller execution and publication.

Current distance: seven registered-objective examples toward the next twelve-row
review threshold,40 verified registrations, and no full-Pokedex or cross-title
competence claim. The completed three-item integration checklist is not Phase5
completion; the remaining work is useful throughput, broader acquisition and
evolution coverage, sustained collection and measured generalization.

## Initial qualification

The fresh projection passed43 routing/adapter tests and full-source typing
(463 files). The isolated synthetic benchmark used1,600 tiles and300 projections
per version:0.276 seconds before,0.037 after (7.47x for this function only).
Twelve varied start-coordinate comparisons matched the previous implementation.
This is not an end-to-end speedup or a learning result.

Flash High reviewed the exact algorithm in24.68 seconds and found it equivalent,
highlighting absent warp coordinates, preserved tuples and caller immutability.
Codex independently covered those cases. Its suggestion to evaluate bounds lazily
is deferred: the current small patch intentionally retains existing empty-graph
behavior and avoids combining another semantic change with this measurement.

## Actual continuation and review

Unprofiled preparation on published source `b2485773` completed in73.81 seconds:
24.45 preparation,49.36 source enumeration, two destinations, zero predictions
and controller inputs. Earlier timed-out cProfile runs are not an equivalent
baseline; no end-to-end speedup ratio is established.

Two bounded batches completed five fresh choices and five fits, model7 to12,
in848.992 seconds combined runner time. They executed1,529 actions /93,564 frames.
This excludes the standalone preparation measurement and work-session overhead.

| Step | Actual learned choice | Outcome | Capture items |
| --- | --- | --- | --- |
| A1 | Recover team instead of capture | Succeeded; model8 |14 to14 |
| A2 | Route15 destination | Search exhausted; model9 |14 to14 |
| B1 | Route15 destination | Search exhausted; model10 |14 to9 |
| B2 | Recover team instead of capture | Succeeded; model11 |9 to9 |
| B3 | Route15 destination | Ordinary balls exhausted; model12 |9 to0 |

No new registrations or specimens:40 registered /44 physical throughout.
The total twelve-row corpus has five successes and seven failures. Recovery
success is not collection gain. The destination choices were exploratory:
later Route15 selections had probabilities near0.5, even when Route7 scored
slightly higher. Repeated selection alone does not prove search memory was ignored.

### Failure and exact continuation

The final component diagnostic is `SurgeChapterError`: ordinary capture balls
exhausted. The routine fled before raising. The authenticated terminal snapshot
reports no active battle, `input_ready=false`, zero capture items, money15,503,
and all six party members alive. The terminal is durable, but automatic capture
resumption is not admitted: settle/verify control and resupply first.

- Episode: `red-registered-throughput-20260910-b-03-causal`
- Checkpoint SHA: `d3ce03c528af8cd21cfe22a8dcde8c3db852ea8241543575b20109b6024b7f54`
- Registered model12 SHA: `fc22fbf718c41452973db9e4279af4d4ad5f9a33d66c9e280b9d1a9d4191207b`
- Corpus SHA: `e61e5627d597486cd90ccef14f54ba12fcd82e40a8077890edf62c20c0963760`

Both batch declarations and all five consumed outcomes remain immutable. Never
restart either launcher or return to a fuller bag by restoring an older save.
The gameplay process has exited; the dashboard presents saved evidence.

### What the twelve-example review says

The data threshold is met, but useful collection yield is not. The current Red
contract lists124 targets:40 credited,84 outstanding. This is entry coverage,
not an engineering-completion percentage; fresh-run and Blue continuity also remain.

The current utility gives verified subtask success weight4 and completion gain
weight4. Registered novelty is divided by124, so a single new registration adds
about0.0323 beyond ordinary success, before costs. Reliable support can therefore
look attractive against uncertain collection. This arithmetic is a design risk,
not proof that it caused each sampled decision; no counterfactual training labels
or retrospective reward edits were made.

The bounded survey hit its leg limit after four and seven encounters in the two
clean search failures. The last two attempts consumed fourteen balls with no new
registration. The status-support branch currently goes directly to throwing,
whereas the non-status branch has weakening logic. Neither observation proves
a capture bug, but both deserve a focused efficiency test before a larger campaign.

Next sequence: verify/settle retained control, legitimate resupply, then test
registered utility balance and capture/search efficiency with explicit budgets.
Measure new registrations per minute and per ball alongside failures, and expose
useful evolution alternatives. Do not scale an unproductive narrow loop merely
to raise sample counts. No whole-game replay, sealed evaluation or Crystal run.

Flash review quota snapshot after its completed call:62.15% five-hour /86.91%
weekly remaining. Claude was not used. Codex accepted the small projection change
and deferred lazy-bound scanning and global caching. Source qualification passed
139 focused tests, whole-source typing, lint, registry and documentation checks.

Final closeout validation:274 focused routing, reporting, focus and dashboard
tests passed; whole-repository lint, public-artifact and documentation checks
passed. The infographic and live dashboard projection were inspected. GitHub CI
run34426055788 passed for executable source `b2485773`; the two preceding runs
also completed successfully. No full-suite local pass is claimed.

Recommended next session: Astra Extra High, Fast off for the bounded utility and
capture-efficiency review, with Flash High drafting isolated tests after the
invariants are specified. Keep higher effort focused on the design decision,
not routine monitoring or another broad audit.
