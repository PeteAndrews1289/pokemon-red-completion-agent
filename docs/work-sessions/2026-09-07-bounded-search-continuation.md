# Bounded useful-search continuation — September 7

Start23:27UTC, stop by00:57UTC or the natural two-choice limit.
Retain the actual healthy Route24/model64 terminal; do not replay recovery.

## Mission check

1. Capability: continue safely after ordinary no-catch exhaustion, using observed
   remaining resources and preserving the living collection.
2. Learned authority: existing goal/destination policy selects each real attempt;
   fit its outcome, not the preparation or unselected source.
3. Transfer test: vary outcome flags, inventory costs, collection changes and bad
   observations in ROM-free tests. Live continuation is same-lineage practice.
4. Cheapest falsifier: one fresh learned destination from this actual healthy
   state; can finite search retain a useful specimen?
5. Time box:90minutes, reassess after the first actual result, at most two newly
   declared choices with retained fitting between them.
6. Stop condition: unsupported goal, unsafe party or recovery reserves, specimen
   loss, malformed evidence or repeated execution fault. Ordinary verified safe
   exhaustion may proceed only within the prospectively declared two-choice cap.

## Division of work

Flash3.8High drafts a small optional public search-summary projection with tests
in its isolated branch. No gameplay, private data, policy or source-budget changes.
Codex independently verifies the projection while inspecting the existing
safe-exhaustion learning cycle and current saved-state feasibility.

No new teacher, reset, sealed evaluation, Crystal execution, fabricated labels
or broad architecture rewrite. Source1a2ff13f CI is now green; documentation-head
40eac0f6 CI remains running. These checks are not learning outputs.

Initial external quota23:28UTC: Gemini84.33% five-hour and96.20%weekly remaining.
Actual draft time plus Codex correction/testing time will be recorded separately;
this single assignment cannot establish an overall acceleration multiplier.

## Integration checkpoint

The existing safe-exhaustion loop wrongly required no semantic state change,
which excluded a verified safe search that spent a ball or changed location.
It now preserves exact collection equality and the typed safe-exhaustion verdict,
with the original finite opt-in and fresh source-readiness checks. Non-boolean
state flags, other failures, changed collections and fit errors still stop.

Actual survey-leg exhaustion now retains its literal cause and counts in an
explicit optional capture_survey section. No generic report fields, exception
text, source identities or private paths become model features or labels.
The marker is absent for other skills and old step serialization stays unchanged.

Flash's initial draft passed55tests with one inconsistent None-default assertion.
Codex also found that its flat-field detector would reject real generic exploration.
The revision uses an explicit nested marker, fixes the test and validates relational
bounds. Raw reviewed draft f76cc46d is retained in its isolated branch. Codex appended
the optional field after existing positional fields and connected actual Red reports.
Combined221tests pass, including real provider counter/cause assertions; all417source
files type-check and lint passes. The existing prospective registry was regenerated
without changing its run inventory. No old run or sealed identity was reopened.

Provider-reported cumulative Flash duration for the two turns:466seconds; Codex
review/tests and live preparation overlapped, so this is not an end-to-end speedup.
Latest23:38UTC quota: Gemini79.78%five-hour/94.86%weekly remaining, resetting02:11UTC
September8/23:22UTC September11. Account-wide readings, not per-task attribution.
Claude was not used. Stop further drafting revisions and perform the actual trial.

## Result and reorientation

Completed one of at most two actual source choices. Model64 selected Route5 at
probability0.784165 and retained a second Mankey12 using one ball,334actions and
20,628frames. All26specimens remain,25distinct living species/30registered;
four balls209currency and six full-HP party members. Model65 fitted the outcome,
retaining all64priorrows:65total,26successful,55distinct,44native/15regional entries.
The second trial was never claimed: only Route24 remains a capture source.

Exact checkpoint5135f211 and full inventory restored without input or frame advance.
Cartridge graph confirms Mankey-to-Primeape at28; two Mankey now allow retaining one
while developing the other. This closes safe acquisition3/3, not Phase3. No Primeape,
independent evaluation, post-fit model65 gameplay or new species is claimed.

Post-run audit found the routed composer dropped the capture_survey marker. Narrow
typed forwarding and regression tests repair it prospectively; consumed c99d18ae
evidence stays unchanged. Final261targeted checks include39registry tests; separate
119fitting/continuation tests passed.403package files type-check and lint passes.
The current source CI remains running; previous40eac0f6 and1a2ff13f are green.

Natural stopping point: saved outcome/fit audited, next mixed acquisition/evolution
step defined. No second forced source question, reset or automatic replay. See
[closeout audit](../audits/red-bounded-search-continuation-2026-09-07.md).
