# Model111 zero-label recovery

## Outcome

The exact model111 failure terminal recovered ordinary field control through the qualified generic
scripted-dialogue handler. The single attempt used eight controller actions and 576 emulator
frames across four bounded dialogue pulses. It ended input-ready and outside battle with all 83
registrations intact.

The recovered terminal was published and independently reopened as
`red-model111-gate-recovery-support-20260912`, checkpoint record SHA-256
`8c358a3a5dd8346fc62a247e19aed0caee461eb77e9ce0f562dfa7c3f65b79f3`. This is
deterministic support: it added no teacher label, training example, model fit or authority.
Model111 remains at 111 settled examples, 76 successful.

## Next-menu result

The broad menu was rebuilt action-free from the recovered checkpoint. It exposed only one
executable goal, `restore_team`, so the committed stop condition fired before another gameplay
choice. Acquisition remained unavailable as `no_legal_target` even though the save holds four
capture items, 179 free storage slots, 18 productive fishing maps and four independently reachable
fishing maps.

A second action-free diagnostic found the reusable cause. The party is healthy but contains no
usable sleep/paralysis move. The active box contains no suitable helper, while seven capable
helpers exist across other boxes. The existing capture-party planner searches only the active box.
The next engineering gate is therefore cross-box capability discovery and safe retrieval, not a
named-species route or another dialogue patch.

## Verification

- Exact source `23dadf37a59ad5f018d3d227921770174be108c7` passed GitHub CI run
  `34730511619` attempt 1.
- The one recovery attempt retained its declaration and no-retry claim before controller input.
- Recovery ended after 8 actions / 576 frames and preserved 83 registrations.
- The restart reopened against the same ROM, profile, model and parent lineage.
- Menu and party diagnostics used zero controller actions and zero emulator frames and left the
  recovered state byte-identical.

## Mission check

| Question | Answer |
| --- | --- |
| Reusable capability | Recover a typed scripted-dialogue route interruption and publish its terminal as zero-label support. |
| Learned authority | Unchanged. Model111 still ranks goals; deterministic adapters recover control and execute them. |
| Product relevance | Preserves failed experience and resumes the same collection lineage without retrying or manufacturing a success. |
| Cheapest falsifier | Cross-box capability retrieval cannot safely expose a useful acquisition alongside support goals from this exact checkpoint. |
| Stop condition | The rebuilt menu was forced-only, so no heal, capture, resample, fit, Blue/Crystal execution or full replay occurred. |

## Next session

Extend capture preparation from the current box to all cartridge-observed boxes. Select a helper by
capability and resource condition, switch boxes through the existing PC primitive, preserve every
specimen and field-move dependency, and rebind the original acquisition without re-querying the
model. Qualify it action-free and with focused tests before allowing one fresh supported choice.

[Evidence](../evidence/red-model111-zero-label-recovery-2026-09-12.json)
