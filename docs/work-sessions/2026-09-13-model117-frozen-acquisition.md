# Model117: exact frozen acquisition succeeds

## Outcome

Model116's exact frozen candidate2 ran once from its authenticated terminal. A read-only preflight
first reconstructed the same model, menu, private binding and parent state with zero controller
actions or emulator frames. The execution then used979 actions /59232 frames, made six casts,
encountered two targets and captured one missing species. Registrations rose84 to85; living species
rose64 to65 and total specimens68 to69. Cash stayed2146.

The choice was not redrawn or retried. It used zero teacher labels and zero execution-time policy
queries. Fresh verification observed exactly one new registration before the terminal was retained.

## Learning and continuation

The verified success became row117. Model117 has **117 settled examples /79 successes** and
preserves all116 prior rows. This is training-only measured evidence without an action trace,
independent evaluation or authority promotion.

The terminal reopened as a zero-input restart with85 registrations,65 living species and69
specimens. Building Model117's next action-free menu then failed closed before a selection or
artifact write: the existing live-fishing inventory requires at least two reachable productive
destinations, and fewer than two remain after this capture. No further gameplay occurred.

## External contribution

Gemini3.8 Flash High completed a read-only review and passed the exact menu/model/checkpoint,
zero-input, no-resampling, no-retry and verification guards. Claude Sonnet4.6 independently found
that the canceled menu CI reference and later green execution qualification needed distinct audit
fields. That finding was accepted through a dedicated source-binding receipt; Claude otherwise
returned PASS. Neither reviewer edited the worktree or ran gameplay. Current service quota was not
available from either CLI.

## Verification boundary

The action-free private preflight passed with exact hashes and zero effects. The public declaration
admission passed91 focused ROM-free tests. Model117 fitting and checkpoint publication each observed
zero controller actions. This remains bounded development evidence, not full-game or transfer proof.

## Product gate and next session

The required finish line remains unmet: one authenticated non-deterministic, model-directed Red run
must complete the story and full local Red Pokédex before any ROM hack. Version, trade and event
dependencies remain explicit; the current39-entry remainder is only the124-entry native scope.

Next, qualify identity-free degradation when live fishing has one or zero reachable productive
destinations, then rebuild and freeze Model117's mixed menu without gameplay or a teacher fallback.
Estimate: one bounded session,45–90minutes. Stop on controller input during menu construction,
identity-bearing policy features, manual target selection or any retry/redraw of Model116.

Recommended model: **GPT-5.6 Sol, High effort, Fast off**. The task is a bounded option-construction
contract and focused integration change; Astra is not needed unless the remaining-acquisition design
expands into a new authority boundary.

[Evidence](../evidence/red-model117-frozen-acquisition-2026-09-13.json) ·
[Roadmap decision](../roadmap-decisions.md)
