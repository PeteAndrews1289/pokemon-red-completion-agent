# Registered runtime binding — work plan

- Capability: make live capture surveys and evolution readiness use shared registrations.
- Learned authority: maintenance unblocks `red-shared-registration-learning-v1`; no new authority
  until checkpoint and reward admission bind the new objective and a bounded lesson executes.
- Transfer test: synthetic inherited Red/Blue credit must suppress redundant demand without
  changing local flags, physical stock, safety or available resources.
- Cheapest falsifier: a single unreserved precursor is eligible; a protected one remains blocked;
  a globally credited capture chain has no remaining survey demand.
- Time box: one integration session, with a review before any controller input.
- Stop condition: any old checkpoint/reward is silently reinterpreted, or registration mode can
  bypass exact physical-transformation and story/HM protection.

Gameplay remains paused during integration. No consumed run is being replayed.

## Result

340 targeted tests passed, including 14 new runtime-binding tests. Four directly checked source
modules passed typing; source/test lint passed. This is integration engineering, not learner output.
No ROM, production ledger, checkpoint import, controller input or model fit occurred. Native114
and its old-objective corpus remain unchanged. No external-agent audit or usage this session.

The immutable `RedRegistrationPolicy` binds a real Red run identity, frozen initial memory,
initial checkpoint, declared target set and explicit physical reserves. Provider configuration and
native evolution training digests now incorporate it when opted in; legacy identities remain
unchanged. Current local owned flags are unioned with the frozen inherited credit, not overwritten.

The actual Red capture survey now consumes registered demand, including previously credited
evolution chains, and recomputes when local registration changes. The native evolution offer and
executor accept a single unreserved precursor. Exact specimen transformation, final registration,
story, HP, input readiness, action limits and per-quantum progress remain checked. A simulated
single-copy party evolution completed through the native training wrapper; the same transition
without the actual target flag was rejected. Larger boxed multiplicities remain unsupported and
are blocked before controller input, consistent with the existing generic boxed engine.

The old bounded-player observer intentionally rejects a registered-policy runtime before any
observation or input. Removing that stop is contingent on the next integration below; otherwise
new-objective actions could enter old living-Dex checkpoints and rewards. This guard does not
block legacy receipt inspection or alter an old fitted model.

## Reorientation and concrete next work

The capture/evolution execution rules are no longer the first blocker. The remaining bridge is
the observation/checkpoint/training boundary:

1. `red_goal_manager.py`: project registered-objective pressures rather than living/level-cap
   quotas, preserving local and physical facts as separate diagnostics.
2. `red_bounded_player.py` and checkpoint serialization: carry a versioned registered checkpoint,
   frozen memory identity and exact physical-loss accounting through save/restore. Do not merely
   reinterpret fields of old `LivingCollectionCheckpoint` receipts.
3. `red_player_training_dataset.py`, `red_regional_choice_learning.py` and fitting: explicitly
   bind registered rewards and decide old114 compatibility. Warm-starting parameters is not
   permission to relabel old rewards or claim all old examples used the new objective.
4. Once those consumers agree, remove the temporary registered-loop stop, import the existing
   authenticated Red endpoint without frames, and run one short new-objective lesson.

This is a second engineering slice required by the user-directed objective change. Reassessing
the anti-drift alarm: no more general ledger or planning architecture is needed before the lesson.
The new integration checklist remains 1/3; the live-runtime item is partially implemented, not
complete. No phase-exit criteria or learning counters changed.

GitHub check during this session: previous head333ba198 CI34402366190 was in progress; preceding
head687e7a83 CI34399893435 passed. No hosted rerun or CI-wait gate was introduced.

[Qualification](../evidence/registered-runtime-binding-2026-09-09.json)
