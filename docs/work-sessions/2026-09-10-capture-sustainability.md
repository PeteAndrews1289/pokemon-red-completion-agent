# Capture sustainability from AJ01

## Mission check

- Capability: sustain missing-species collection with legitimate supplies and safe capture support.
- Learned authority: unblock another real goal/destination choice; deterministic preparation is not a training example.
- Transfer test: inventory-only inspection skips unused ranking history while actual choices still authenticate it; resource and helper guards remain valid with different stock and health.
- Cheapest falsifier: inspect the exact AJ01 continuation without inputs for a legitimate funding/recovery offer before spending money.
- Time box: 60–90 minutes, then stop and reorient if no sustainable collection attempt is available.
- Stop condition: no legal resource offer, unsafe support, stale ancestry or unverifiable results; never replay AI/AJ or lower party protections.

Starting point: 64 registrations, 54 specimens, 63 examples; Seafoam B3F,
zero balls, 593 money, capture helper 36/73 HP. These counters change only after
verified gameplay and an eligible fit. No reset, sealed Red, Crystal or full replay.

## Preparation repair

The cycle's inventory-only inspection no longer rebuilds a ranking history whose
menu it discards. Actual choice calls retain authenticated history by default;
both paths still restore and verify the exact state and reject input side effects.
90 focused inventory, cycle, source-runner and learning tests passed. This is
maintenance to unblock collection, not new learning or a measured live speedup.

Two initial read-only invocations stopped at clean/published-source checks before
opening the continuation. No episode, prediction, gameplay or fit was created.

## Exact-state result and reorientation

The successful read-only preflight took 33.508 seconds including preparation.
It offered only explore, created no episode and made zero inputs, frames, model
queries or fits. Separate inventory returned zero regional acquisition candidates
in 4.859 seconds. This is an observed duration, not a before/after speedup ratio.
The route diagnostic found no feasible Center entry at (3,3); it did not execute
nurse interaction. The recovery binder itself also remains walking-only.

The declared no-resource-offer falsifier therefore stopped this session before
live gameplay. The existing trainer-funding scope covers the current/adjacent
ordinary maps, not a cave escape. The fixed Mart offer needs 600 money; only 593
is available. No supported healing surplus can be sold from the actual bag.

A further input-free observation found map 161, allowed escape tileset 17, a
living Dig holder in slot 2 and healing anchor map 7. Existing field-Dig code
already discovers the holder dynamically and verifies protected state. It still
needs a scoped collection-recovery binding, observed eligibility, exact destination
checks and fresh re-planning. No Dig input or recovery result is claimed.

[Path-free diagnostic](../evidence/red-capture-sustainability-audit-2026-09-10.json).
All 64 registrations, 54 specimens and 63 examples remain unchanged. The former
travel-capture checklist is preserved at 3/3; resource recovery starts 1/3, diagnosis
only. The anti-drift alarm for an engineering-only session means the next task must
connect this specific recovery gap, not expand unrelated architecture.

Final focused/regression verification: 157 tests passed, including recovery and
trainer funding; 135 closeout tests also passed. Ruff and registry checks passed;
no full-suite claim. No external
agents ran, and their quotas were not queried.

Next: 60–90 minutes for bounded Dig/recovery integration; adequate funding and a
productive collection lesson may need a follow-up. Recommended setting: Astra High,
Fast off. This is a narrow integration task; higher effort or more agents is not
justified by the current evidence.
