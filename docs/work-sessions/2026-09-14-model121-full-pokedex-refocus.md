# Model121 stops the resource loop and exposes the full Red denominator

## Outcome

Model121 reopened from its published checkpoint without controller input. Its exact menu contained
only trainer resupply and routed restoration; there were no fishing or other acquisition options.
The refocus stop condition therefore fired. No model query was made, no choice was frozen, and no
gameplay, label, fit, or collection change occurred.

The project now has a ROM-free 151-entry Red inventory above the historical 124-entry solo catalog.
It keeps the three required facts separate: local Red registration, shared registration credit,
and current physical possession. Only local Red owned flags can satisfy the completion gate.

## Exact boundary

- Model121 remains at **121 examples / 83 successes**.
- The retained save remains at **86 local registrations**, 66 living species, 70 specimens,
  four capture items, and cash 706.
- The action-free menu had two choices—resupply and restore—and zero acquisition candidates.
- Inspection used zero controller actions, zero emulator frames, zero model queries, and zero
  teacher labels.
- Gameplay remains stopped at the Model121 terminal.

## Full Red inventory

The 65 missing local registrations now have explicit resolution classes:

- 38 have plans in the existing solo acquisition catalog; a plan is not a claim that the option is
  executable from the current state.
- 11 require a paired Blue save plus a link trade.
- 4 are Generation I link evolutions.
- 11 require alternate starter, fossil, dojo, or Eevee choices on a supporting Red save plus trade.
- 1 requires a legitimate event input.

All 151 targets are classified and none silently disappear as “unsupported.” This is planning
infrastructure, not learned progress and not authority to begin the full-game exam.

## Verification and review

Commit `679d2b68` adds the typed inventory and ROM-free tests. The focused suite passed 95 tests;
Ruff, mypy, generated-registry checks, the public-artifact check, and the product-focus check passed.
Hosted CI run `34851396444` passed the exact implementation commit with 11,608 tests passed,
4 skipped, and 1 expected failure.

Gemini 3.8 Flash High completed a read-only review. Its STOP findings about executable-authority
wording, supporting-save evolution coverage, and serialization coverage were accepted. Its request
to distinguish planning from execution was implemented structurally as `solo_catalog_plan`, rather
than through a brittle docstring test. The standalone Claude CLI requested login, but Claude Sonnet
4.6 completed a read-only audit through `agy` and returned GO with P0/P1 zero. Its two advisory P2
notes were rejected: the evidence already records target count 151, and its Eevee note treated the
cataloged gift species as an excluded branch. Neither CLI exposes remaining quota percentages.

## Next bounded objective

Connect the full-151 inventory to the goal-proposal layer and qualify one independent, action-free
menu spanning at least two acquisition families. Prefer an item evolution versus an in-game trade
or another genuinely heterogeneous pair. Stop without gameplay if two supported executable
families cannot be proven; do not return to the Model121 restore/resupply loop.

Next-session recommendation: **GPT-5.6 Sol, High effort, Fast off**. The work is a bounded planner
integration with important invariants; Astra is unnecessary unless the dependency/authority model
must be redesigned after the falsifier.
