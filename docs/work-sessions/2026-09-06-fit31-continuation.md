# Session: continue real Red play with the 31-example model

## Mission check, recorded before implementation

- **Capability:** preserve collection progress across bounded player sessions rather than resetting
  to the same capture. Reuse the existing observation, skill execution and checkpoint code.
- **Learned authority:** run the fitted 31-example goal selector on its predecessor's endpoint.
  Deterministic mechanics, safety and unsupported-menu choices remain explicitly reported.
- **Transfer test:** this is same-lineage Red integration, not independent generalization. Later
  compare Red-trained initialization with an otherwise identical untrained learner on a compatible
  unfamiliar Red modification before Crystal. No hack or Crystal execution belongs to this session.
- **Cheapest falsifier:** fresh checkpoint/ledger agreement before input, followed by one chain of
  at most four goals. Count needed specimens, spending, supplies, interventions and failures.
- **Time box:** one session, at most four hours; targeted local checks before actual gameplay.
- **Stop condition:** one completed bounded continuation or a retained terminal failure, then audit
  and reorient. No replay of closed runs, fitting of greedy diagnostic choices or new data factory.

## Prospective execution scope

Use the completed native R1 B checkpoint and the fitted `95f62eaa...edb472` model.
The private operator file binds full identities. Preserve the parent's authenticated training
partition and lineage; a terminal save is never a new independent root. A continuation runs only
the model-directed arm, not another baseline replay. Save its terminal state and report whether
the learned choice actually changes useful behavior. No model fit is authorized by this diagnostic
plan. The original 31 retained learning rows remain unchanged.

## Agreed product sequence

1. Sustained Red play with useful model decisions and durable collection progress.
2. Repeatable model-directed Red story completion, not completion by a fixed teacher route.
3. Red living collection under a declared version/trade/event scope, including evolution/storage.
4. A compatible unfamiliar Red ROM modification as the first transfer demonstration. Measure
   initial performance separately from adaptation, and compare with learning without Red training.
5. Crystal and subsequent games, retaining reusable strategy and explicit game-interface adapters.

The modification is a later evaluation environment, not a new implementation lane. Beating Red
does not guarantee beating a hack; success means measurable benefit from prior learning without
writing a new walkthrough. Cross-title living-Pokedex completion remains the long-term product.

## Closed result

The consumer was implemented and published as `e1dd3af81f0bbbb89b0996df7a9c3fded7a68e61`.
The one live attempt authenticated its restore, travelled to the capture source, entered three
wild battles and found no capture within 64 survey legs. Recovery selected the failed goal again
and terminated with a guard exception: **215 actions / 8244 frames, zero captures, no spending**.
The selected goal was an unsupported-menu deterministic bridge; the 31-example model did not earn
a new learned decision. The failed archive is retained, with no replay or fit. Original save is
safe; no new terminal save or complete fresh terminal collection ledger was produced.

See the [audit](../audits/red-fit31-continuation-2026-09-06.md) and
[verified aggregate](../evidence/red-fit31-continuation-result-2026-09-06.json).
Next session is typed no-find/recovery stopping, safe state retention and useful existing alternative
mechanics. This session stops at its declared failed-attempt/audit boundary, not another repair/run loop.

87 focused tests passed. Full lint, types, docs/focus, registries and public-artifact checks passed.
The broader suite finished with 7102 passed, one stale registry fingerprint expectation failure,
one skipped, three deselected and one expected failure. Reviewed fixed expectations were updated;
the audit distinguishes this bookkeeping correction from gameplay changes and a full-suite rerun.
The follow-up registry/continuation/focus suite passed all 161 tests; no further gameplay occurred.
