# Model119: forced fishing adds Poliwhirl without a false model choice

## Outcome

The sole Model119 fishing acquisition executed exactly once under explicit
`forced_singleton` authority. It captured national species061 (Poliwhirl) in **905 controller
actions /45,564 emulator frames**. Registered species advanced **85→86**, living species
**65→66**, total living specimens **69→70**, and the native-scope remainder fell **39→38**.
Cash remained346 and four capture items remain.

The step used zero model queries, model-authority decisions, teacher labels or training examples.
Model119 therefore remains **119 examples /81 successes**. The step was durably recorded before
execution, its success and child checkpoint were retained, and retry is false.

## Forced-choice boundary

The reusable bridge requires zero ordinary executors and exactly one available supplemental
acquisition. Its public record contains only title-neutral candidate/context features and declares
`forced_singleton`, zero model/teacher queries and zero training examples. ROM-free tests reject
zero or two supplements, ordinary executors, non-acquisition candidates, storage pressure and
incomplete economy context.

Source commit `18ae5bb7` passed exact hosted CI run34794131554 before private state was opened. The
frozen Model119 reconstruction then proved one candidate and zero actions/frames. The generic
bounded player kept its minimum learned-menu width at two, allowed only the initial forced bridge,
recorded one forced step and executed it once without redraw.

## Checkpoint correction and next boundary

The execution episode and sealed child checkpoint are valid. A read-only audit caught a private
summary-label error: `result.json` placed the checkpoint document hash in a field named as the
sealed record hash. The original result remains immutable; a separate correction binds the document
hash, record identity and correct sealed checkpoint hash. The corrected checkpoint reopened with
state `b2a560ca…b2f72` and record `e7621f2a…161b4`. This correction used zero actions/frames and did
not authorize a retry.

The changed-state inventory was then rebuilt action-free. Fishing now has zero candidates, while
two ordinary identity-free choices are available: **resupply** and **restore**. Their menu hash is
`abb5d536…a2696`. No choice was sampled or executed. Gameplay is stopped at this genuine Model119
decision boundary.

## External contribution

Gemini3.8 Flash High passed the bridge contract. Its runner review incorrectly raised three P0s
that contradict the tested forced-singleton semantics, but it also identified a valid observation-
purity issue: the controller-started claim should not be written inside observation. Codex accepted
that finding and moved the claim into the sole executor wrapper, after durable forced-decision
recording and immediately before possible input. Earlier Flash attempts were denied read permission
or timed out without output; none changed files or touched gameplay.

Claude Sonnet passed the conceptual design and identified two load-bearing requirements: use
`GoalSelectionMode.FORCED_SINGLETON` and keep `min_available_goals` above one so the singleton cannot
be recorded as learned authority. Both were accepted. A later exact-file read-only review timed out
without a finding. Current service quota was unavailable from both CLIs.

## Verification and product gate

The source gate passed186 focused runtime tests,150 documentation/focus tests, Ruff, changed-source
typing, registry regeneration/check and exact hosted CI. The private freeze, execution checkpoint
reopen and post-terminal menu inspection all passed. No ROM, save, checkpoint or private path was
added to the repository.

The required finish line remains unmet: one authenticated fresh-start, non-deterministic,
model-directed Red run must complete the story and full local Red Pokédex before any ROM hack.
Version, trade and event dependencies remain explicit; the38-entry native remainder is not the
full-Pokédex denominator.

Next, freeze Model119's exact resupply-versus-restore menu, sample and execute it once, then fit only
an eligible measured result. Estimate: one bounded session,60–120minutes.

Recommended model: **GPT-5.6 Sol, High effort, Fast off**. The next task follows the established
two-option freeze/execute/fit loop; use Astra High only if the corrected checkpoint cannot join the
measured-choice admission contract without weakening it.

[Evidence](../evidence/red-model119-forced-singleton-fishing-2026-09-13.json) ·
[Roadmap](../development-roadmap.md)
