# One training source selected; direct-start integration remains

## Outcome

A metadata-only selector now chooses a training source by immutable catalog assignment and
account-wide claim availability, before opening its saved state or evaluating its menu. It rejects
state aliases, ambiguous captures, mismatched registries and incomplete availability snapshots.
Its fixed order does not use historical goal preferences or outcomes.

The authenticated catalog contains 54 train entries. The account claim census found 42 claimed and
12 unclaimed. The earlier 18-registration diagnostic is claimed and excluded. This does not revoke
its engineering evidence; it cannot supply the new lesson. The selector froze the first unclaimed
slot once, under the account registry's shared lease, in private record
`full-local-source-20260914-v1`. No additional source may replace this selection after inspection.

Only the selected source's configuration was then read. It declares story, restoration and control
recovery, with neither wild capture nor native level evolution. Its saved payload remains unopened.
This is a configuration gap, not evidence that the actual state cannot support two acquisition
families. There were zero root claims, ROM/save/model opens, gameplay actions, frames, model queries,
registration sessions, new outcomes or fits. No existing claim or saved state was changed.

The preceding implementation's final hosted run 34876713998 passed on 063ad931. This session's
source-selection change is engineering; Model121 stays at 121 examples, 83 successes and 86 local
registrations. The full Red run and local151 gate remain incomplete.

## Mission check

- Capability: select one eligible source without searching saved menus or reopening claimed roots.
- Learned authority: unblock one prospective capture/evolution choice; selection itself adds none.
- Transfer test: ROM-free permutations, partition aliases, missing census and exhausted supply;
  independent upstream lineage and cross-title performance remain untested.
- Cheapest falsifier: the selected source lacks the execution configuration required by the player.
- Time box: one session, with reassessment at the first decisive readiness gap.
- Stop: no payload or controller authority until direct-start and acquisition configuration pass;
  no replacement selection, historical retry, diagnostic fit or full replay.

## Correction to the previous handoff

The old handoff asked for a newly provisioned catalog source as if the registered collector could
start there. The existing catalog is a fixed 81-assignment contract, not an enrollment service.
There are unclaimed existing training sources, so another catalog or teacher factory is unnecessary
for this first test. But the completion-dose argument guard, training-plan conversion and
registration-session anchor all require an existing continuation. An ordinary new source cannot
reach the integrated full-local mode directly.

The other gap is explicit acquisition configuration: the full-local adapter currently requires a
declared native evolution provider and a declared capture corridor. Its151-target inventory does
not create those declarations. The selected historical profile has neither. Do not mistake a
profile omission for proof that acquisition is impossible in the saved game.

## Concrete next implementation

Use the existing player, ledger, native evolution and cartridge router. Add a versioned direct
catalog origin to registered training and registration-session anchoring. Preserve old continuation
schemas and bytes. A direct origin binds the catalog, assignment, root lineage, exact state and
envelope, execution profile and model before input; it contains no invented predecessor episode.
The new session anchor is the authenticated catalog origin, not a fabricated gameplay terminal.
Continued episodes must retain that origin and reconcile actual terminal registration in order.

Derive acquisition declarations from the selected source's semantic inventory and cartridge
capabilities using existing adapters. Keep the configuration change distinct from historical
profile authentication. Preserve local owned flags, shared credit and physical reserves separately.
Do not require a caller to nominate a species or route to manufacture the desired lesson.

After ROM-free qualification, reopen the frozen selection record, recheck and atomically claim
that exact pair, and inspect only that source. Require actual capture and evolution alternatives
before a model query. If the claim or two-family gate fails, retain that result without another
source. If it passes, freeze one exploratory model choice, execute its binding once, retain the
actual costs and terminal, and admit only that measured outcome. The selection record is not a
reservation: another process may claim the source before this step.

Tests must distinguish direct origin from a continuation, reject forged/missing catalog joins and
state/profile/model swaps, preserve legacy sessions, retain failures after a valid choice, and
prove a stale claim cannot trigger a substitute or payload read. Existing claim-registry tests
cover atomic conflicts; the new selector does not replace that execution boundary.

## Reviews and validation

Claude Opus4.8 High completed a read-only source/test review and found no concrete defect. Its
scope explicitly excluded runtime claim execution and upstream independence. Flash3.8 High
completed a tool-free adversarial test proposal: partition aliases, deterministic first selection
and a concurrent claim. Those categories were accepted. Its suggestion that historical menu
metadata proves present acquisition availability was rejected; the real runtime must establish it.
A preceding bounded Claude inquiry completed without a retained response and supplies no verdict.
Both services' current subscription percentages and reset windows are unavailable from their
headless command interfaces; no credits, resets or billing settings changed.

The source selector and related catalog, claim, player-plan, proposal and continuation tests pass;
the exact count and publication checks are recorded in the linked evidence. No gameplay remains
running. The anti-drift alarm remains active because this session produced no learned result.

Next-session recommendation: **GPT-5.6 Sol / High / Fast off**, approximately 90–150 minutes.
The remaining implementation boundary is now explicit; use Astra for a consequential unresolved
authority decision, and a bounded Claude review for the final direct-origin admission change.

[Evidence](../evidence/red-full-local-training-source-2026-09-14.json)
