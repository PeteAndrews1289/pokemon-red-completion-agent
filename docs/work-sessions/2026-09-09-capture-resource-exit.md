# Capture resource exhaustion and registered training continuation

## Mission check and plan

1. Capability: exit a failed capture safely when ordinary balls run out; recover
   through legitimate actions while preserving registrations, stock and spent costs.
2. Learned authority: maintenance unblocks the existing registered model5 goal
   and destination learning loop. Escape support is not a learned decision.
3. Transfer test: independent ROM-free exhaustion/escape/accounting variants;
   subsequent Red practice is correlated training, not generalization evidence.
4. Cheapest falsifier: no-ball wild battle must settle before a safe checkpoint;
   failed escape or changed inventory must still fail closed.
5. Time box: one session, at most two hours; isolate repair, recover, then run
   a short bounded collection/fitting batch before reviewing actual utility.
6. Stop: unsafe continuation, unexplained inventory change, consumed retry,
   mixed rewards or hidden safety inputs. Preserve every failed attempt.

Flash3.8 High owns an isolated minimal capture-exhaustion implementation and
independent tests. Codex reviews, tests and integrates; separately inspects the
existing failed-state recovery and registered ledger seam. No concurrent edits
to the main worktree, no Flash gameplay or private-artifact access.

## Integrated review before input

Flash supplied two isolated implementation/test packages, each with one correction
pass. Codex ran them: capture first had4 fixture failures, registered recovery
had19 fixture errors and2 failed tests. Flash corrected capture; Codex repaired
remaining registered observation/support-fixture API mistakes and rejected copied
header/ordering assertions as decorative. This is not a claim of measured speedup.

The no-ball branch preserves the original failure after bounded verified escape,
not another sequence of futile capture searches. Registered recovery has a distinct
checkpoint schema and verifies its immutable failed origin, exact costs and fresh
registration row. Codex requires explicit ledger/session/sequence, restores the
already committed destination without resampling, and leaves recorded-support
imports forbidden.249 combined targeted tests passed before live work; typing of
the four checkpoint/ledger/profile modules passed. No learning count advanced yet.
