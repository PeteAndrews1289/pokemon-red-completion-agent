# Capture resource exhaustion and registered training continuation

## Mission check and plan

1. Capability: exit a failed capture safely when ordinary balls run out; recover
   through legitimate actions while preserving registrations, stock and spent costs.
2. Learned authority: maintenance unblocks the existing registered model5 goal
   and destination learning loop. Escape support is not a learned decision.
3. Transfer test: independent ROM-free exhaustion/escape/accounting variants;
   subsequent Red practice is correlated training, not generalization evidence.
4. Cheapest falsifier: no-ball wild battle must settle before a safe checkpoint;
   failed escape or changed inventory must still fail closed.
5. Time box: one session, at most two hours; isolate repair, recover, then run
   a short bounded collection/fitting batch before reviewing actual utility.
6. Stop: unsafe continuation, unexplained inventory change, consumed retry,
   mixed rewards or hidden safety inputs. Preserve every failed attempt.

Flash3.8 High owns an isolated minimal capture-exhaustion implementation and
independent tests. Codex reviews, tests and integrates; separately inspects the
existing failed-state recovery and registered ledger seam. No concurrent edits
to the main worktree, no Flash gameplay or private-artifact access.

## Integrated review before input

Flash supplied two isolated implementation/test packages, each with one correction
pass. Codex ran them: capture first had4 fixture failures, registered recovery
had19 fixture errors and2 failed tests. Flash corrected capture; Codex repaired
remaining registered observation/support-fixture API mistakes and rejected copied
header/ordering assertions as decorative. This is not a claim of measured speedup.

The no-ball branch preserves the original failure after bounded verified escape,
not another sequence of futile capture searches. Registered recovery has a distinct
checkpoint schema and verifies its immutable failed origin, exact costs and fresh
registration row. Codex requires explicit ledger/session/sequence, restores the
already committed destination without resampling, and leaves recorded-support
imports forbidden.249 combined targeted tests passed before live work; typing of
the four checkpoint/ledger/profile modules passed. No learning count advanced yet.

## Actual recovery and local-supply qualification

The exact failed capture was escaped and healed in174 actions/8,724 frames,
preserving39 registrations and43 specimens. Registered recovery checkpoint
`dde0491a6ed4a2362c7a0f4123c2e7c2b3602a653113aca5b4e5b2a9edea70ed`
advanced the shared ledger to sequence6. No model decision, retry or label.
The next automatic batch stopped with zero steps/input: no executable native
goal, because the sole configured shop was distant Viridian while the healed
player was in Fuchsia with zero balls and27,503 currency.

The smallest prospective repair is an explicit local shop using the existing
generic routed-Mart skill. The Fuchsia map identifier, clerk and item order were
checked against cartridge bytes and the [upstream map definition](https://github.com/pret/pokered/blob/master/constants/map_constants.asm),
[objects](https://github.com/pret/pokered/blob/master/data/maps/objects/FuchsiaMart.asm),
[stock](https://github.com/pret/pokered/blob/master/data/items/marts.asm) and
[prices](https://github.com/pret/pokered/blob/master/data/items/prices.asm).
No new walk string, Fly controller or learned shop-discovery claim is added.

Flash supplied the isolated map constant and four test groups. Codex corrected
immutable tuple comparison, invalid fixture cardinality, resource headroom and
binding API assertions; all four initial tests failed before those corrections.
The unmodified draft is preserved locally as`a2a457ae`.91 focused shop/profile/
router tests passed after integration. The prospective purchase is up to20
Great Balls at600 each, bounded by actual funds and stack capacity; execution
must still prove exact inventory and money changes. Live result follows below.

## Live results and safe closeout

The purchase succeeded in149 actions/7,836frames. A separate zero-input restore
confirmed20 Great Balls and15,503 currency: exactly12,000 spent, no specimen loss.
The forced purchase added no example. Model5 selected Route7 from two destinations;
seven encounters produced no useful capture within the survey limit. That settled
failure cost966 actions/41,580frames and was fitted, increasing the separate
registered corpus from5 to6. No prior rewards were relabeled or mixed.

Model6 reloaded and chose Route15 from two destinations. Its attempt stopped
after576 actions/18,372frames: Ditto's displayed species changed during the switch
to Hypno, while opponentHP63, party identities and20 balls were retained.
The capture-status guard interpreted the species change as a changed target.
This attempt is retained but excluded from fitting and normal continuation.
The fourth scheduled step was never claimed; the launcher stopped, not a full
four-step completion. Recovery then escaped/healed in165 actions/8,028frames.

Final recovery checkpoint:
`ecc303c5d5c60dc4522c397f4d27de6ec454a6b15c5d88461f696c8622b4c251`.
Its state is`9c2683d4199b67bacea5f12ed357d50141daf05c36b6ab4721f9c8d4c00b7099`.
Model6 is`8cececd18cc24d4447cf55a08aebb139fe4b916fcc38a1a8876ac458e85e62ed`.
Final ledger sequence9 retains39 registrations/43 specimens. Both forced recoveries
added zero labels. This session recorded2,030 actions/84,540frames including
the new failure and recoveries, excluding costs already counted in the prior session.
No new registration or independent advantage was demonstrated.

[Path-free evidence](../evidence/red-registered-resource-exit-learning-2026-09-09.json)
pins the actual fit, support costs, failed state, and recovered continuation.
The live viewer now labels the registered scorer and uses the declared124-entry
target, distinguishing missing registrations from physical specimens. Its23 prior
viewer/reporting checks plus one new live registered-view test passed before the
final receipt-specific test was added; final validation is recorded below.

## Flash review and Codex decisions

Three isolated implementation drafts, two correction passes, and one final
80.43-second read-only Transform review were used. The final review did not edit
source or access private artifacts. Flash correctly identified the distinction
between persistent encounter identity and mutable battle appearance, plus typing,
move and capture-attribution consumers. Codex accepts that design direction.

Codex does **not** accept blindly assigning captured stock to the original species.
The [cartridge Transform routine](https://github.com/pret/pokered/blob/master/engine/battle/move_effects/transform.asm)
copies species/types/catch rate while leaving HP, and the
[capture routine](https://github.com/pret/pokered/blob/master/engine/items/item_effects.asm)
contains a Gen1 quirk that treats a transformed wild opponent as Ditto when
reloading caught data. This matters for non-Ditto transformations. Actual received
stock and Pokédex flags remain the verifier's source of truth. Flash's proposed
original-identity address still requires a pinned adapter qualification; its
recommendation is not implemented evidence.

Latest refreshed Gemini quota:71.15%five-hour /88.31%weekly remaining, displayed
resets approximately3h33m/47h4m. Initial cached values were superseded; do not infer
task-specific savings from these shared windows. Claude was not used or queried.
All external tasks are finished. Original drafts remain in isolated local branches;
only reviewed Codex integration is published. Several draft tests needed correction,
so there is no measured overall speedup claim.

## Next session and reorientation

1. Qualify original wild encounter identity versus transformed battle appearance,
   including mid-switch/pre-transformed cases and cartridge-correct capture stock.
   Preserve HP, party, bag, wild/trainer and bounded-input guards.
2. From the final recovered checkpoint, collect a **new** useful collection choice;
   never retry either consumed capture or revert to an earlier successful endpoint.
3. Expose more useful missing-registration/evolution alternatives. The current
   two-source menu and stale completed evolution binding are narrow coverage,
   not a model that already knows all habitats or mechanics.
4. Measure and reduce repeated immutable preparation overhead without hiding
   controller effects or skipping current-state checks. No broad audit prerequisite.

The mission, North Star, stage IDs and exit criteria are unchanged. The three-item
registered integration milestone remains closed; the current measurable corpus
target is6/12, not50% of the entire project or proof of competence. Resource
recovery is live-qualified; Transform capture support is the next concrete gate.

## Final validation

490 combined targeted tests passed after closeout, including the capture/recovery,
registered ledger/checkpoint, shop/router, map graph and dashboard/reporting seams. The live
viewer module passed typing after correcting a subtype-narrowing issue. Changed
source/test lint, public-artifact, documentation/focus checks and prospective
registry regeneration/check passed. This is not a full-suite or independent audit
claim. The infographic was regenerated and visually inspected.
The final whole-project typing pass also found no issues in463 source files;
whole-repository lint passed. The running dashboard reports the actual5-to6 fit.

Hosted CI run34419559966 for the local-shop publication failed one historical-map
comparison after6,108 passes and3 skips. The archived named-map subset predates the
new Fuchsia Mart enum entry; its full recorded graph already contains the shop.
The test now traverses that independent graph from the recorded start, checks all
current names are reachable, retains the historical147-name and220-map counts,
and pins the Fuchsia-to-Mart edge separately. Historical evidence is unchanged.
All21 map tests passed locally and are included in the490 above. Final publication
will receive a fresh hosted check; the prior failure is not presented as green.

Next session recommendation: Astra High, Fast off for focused semantic integration;
Flash3.8 High can draft the bounded adapter tests, with Codex adjudication. A broad
multi-agent audit or higher reasoning setting is not itself the next deliverable.
