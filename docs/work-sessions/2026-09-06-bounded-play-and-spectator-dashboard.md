# Overnight work: bounded Red play and the spectator dashboard

The owner requests continued work across natural session boundaries, with reorientation after
each, toward a learned Pokemon player and eventual multi-game living Pokedex. Red remains first.
The hourly continuation is limited to this eight-hour window; it must check live jobs before
starting work and must never duplicate a campaign or refit a completed dataset.

## Six-part mission check

1. **Capability:** successive supported Red objectives, verified living-collection progress,
   and a spectator view that explains the actual actor, decisions, resources and learning state.
2. **Learned authority:** use the existing 29-example living-Dex option scorer for bounded
   development choices. Deterministic skills enforce mechanics and safety. Label their choices
   separately; the observer cannot silently substitute a preferred action. No production promotion.
3. **Transfer test:** the four declared disjoint Red paired roots are descriptive development,
   not a powered generalization claim. Retain portable semantics; defer Crystal execution.
4. **Cheapest falsifier:** authenticate the saved model with the existing bounded-player loader
   and test refreshed state between three choices. For the view, changing the evidence, stopping
   the producer or disconnecting must not leave a false training count or LIVE indicator.
5. **Time box:** two-to-four hours per development session; audit and reorient before continuing.
   Dashboard work is scoped to the existing local observer, not a second website or hosting stack.
6. **Stop condition:** duplicate/consumed input, false ledger progress, private data exposure,
   stale policy state, lost failure diagnostics, or unavailable authenticated inputs. Preserve
   a terminal and the precise blocker, then choose another safe in-scope step without retries.

## Order of work

1. Inspect the existing paired/bounded player for reuse with the actual saved 29-example artifact.
   Do not replace it with the older goal manager or create a second learning system.
2. Connect the updated scorer and the frozen, still-unexecuted paired roots to the supported
   descriptive check. Any missing seam gets focused behavioral tests before real input.
3. Redesign the existing local viewer around an authentic game screen and current decision, a
   legible six-member party, collection metrics, learning evidence and a session activity stream.
   Replace hard-coded old counts with receipt-backed data and explicit unknowns. Keep the view
   loopback-only and read-only; do not send game frames, ROMs or private paths to hosted services.
4. Integrate a short goal chain with fresh state and ledger verification, retaining negative and
   interrupted outcomes and identifying every safety substitution. Do not claim a three-choice
   episode unless three actual model choices ran.
5. Audit, update state/roadmap/handoff/narratives, publish a coherent change, then proceed to the
   next useful train-data or model-feedback step. Reuse qualified executables when contracts permit;
   CI runs and dashboard improvements do not count as learning.

The earlier eight-slot batch, its two setup censors and the 29-example fit remain completed and
non-retryable. Four paired roots and one reserve are not replacements for its failures. No sealed
Red access, full-game replay, Crystal execution or public hosting is part of this session.

## Implemented and checked in this session

- Spectator redesign of the existing local viewer; no new website runtime or cloud deployment.
- The current model's chart and counts load from a hash-bound saved receipt, with strict accounting.
- A loopback-only live-feed relay, explicit unknown/stale/disconnected states, presentation focus
  mode, reduced-motion support and responsive layout. HTTP feed failures return to the overview.
- The actual immutable 29-example model loaded through the existing strict player model loader.
  This was an action-free load, not a prediction or new fit.
- The existing bounded-player command gains a view-only feed and opt-in continuation after a
  progress signal. Its previous stop policy, source checks and limits remain the default.
- Viewer notifications follow durable selection/outcome records. Existing observations supply
  party, location and collection; frame updates never ask the policy or re-observe the cartridge.
- Focused tests: **205 passed** after the final review of evidence relay and terminal costs.
  A separate 146-test viewer/legacy-dashboard group also passed; the sets overlap and must not
  be added together. Full typing covers 387 source files; lint, docs and public-artifact checks pass.
  No screenshot/browser interaction audit was performed; embedded JavaScript syntax was checked.
- The broad local regression reached 6,433 passes before finding one unnecessarily regenerated
  historical navigation registry. Its frozen bytes were restored, along with the other unrelated
  historical goal registry; no production test assertion was weakened. The affected protocol now
  passes in the focused set. That interrupted run is **not** a full-suite pass. The remaining
  end-of-suite group subsequently passed **553 tests**. These groups overlap. Required publication
  CI remains the full amended-tree check; do not report the interrupted run as fully green.

Qualification: [path-free engineering receipt](../evidence/red-bounded-player-spectator-qualification-2026-09-06.json).

## Reorientation: preserve the declared question, reuse the mechanics

The four frozen development slots contain construction routes and routed acquisition/storage/
resupply providers. Their raw catalog starts are not interchangeable with those conditioned menus:
two raw starts expose only one local option. The generic paired player accepts the model format,
but it is not yet a consumer of this bank's frozen routed menu or paired pre-outcome commitment.
Do not execute the raw starts as an easier substitute.

The next bounded implementation should consume only slots 8–11 from the authenticated retired-bank
binding, leaving the eight train terminals and reserve alone. Reuse the production setup resolver,
validated capture and causal selected-arm executor. Record both first policy choices before either
outcome; preserve exact menu/recipe identities, source, model and control configuration. Keep the
four-root result descriptive and retain every failure in its denominator. Do not weaken the
existing train-only assignment or call the older single-model development journal a paired check.

The smallest useful subsequent player episode chains supported goals with fresh state and living
ledger checks, using the new bounded-chain/viewer interface. Three steps are not automatically three
learned choices: the safety and singleton counters remain separate. Expand missing Red experience
only after actual behavior identifies the needed capability; Crystal remains later.

This session changed no experimental outcomes, consumed no game input, did not refit the completed
dataset, and grants no production authority. The next true progress counter is model-selected Red
behavior, not dashboard polish or CI throughput.
