# Resource-aware goal chain — September 6

## Reorientation: the first chain is closed

The known-training-context integration from `7762e591` terminated normally after two
decisions per actor, not four. The saved 29-example model chose acquisition once, and the
deterministic capture skill added one living and registered species (13 to 14; 18 to 19).
That used the last ordinary capture ball. The refreshed menu correctly masked acquisition
as `missing_resource`, but resupply remained `missing_capability` because this profile only
offered local skills. Exploration was the deterministic unsupported fallback, not a second
model choice; it exhausted its 64-leg bound. Both actors produced the same result.

The existing private reader authenticated both five-stream trajectories, joined their terminal
results and counted all 704 actions / 31,344 frames. The new private diagnostic retained the
real `RedAreaExecutionError`. No model fitting, new train rows, independent superiority,
Crystal, sealed access or full replay occurred. Keep the pair terminal and unchanged.
See the [result](../evidence/red-fit29-known-train-chain-result-2026-09-06.json) and
[read-only audit](../evidence/red-fit29-known-train-chain-readonly-audit-2026-09-06.json).

## Mandatory mission check for the next implementation

1. **Capability:** continue collecting when supplies change, by offering a reachable supply
   destination and returning to a supported capture source through the existing semantic router.
2. **Learned authority:** keep the same saved goal scorer and safety shell. Supply emergencies
   remain visibly deterministic; only genuine model-ranked goals count as learned choices.
3. **Transfer test:** mechanics stay in Red providers and router adapters; semantic goal selection
   stays identity-free. First falsify this seam in synthetic resource/location changes, then a
   prospectively declared training-context integration. This is not independent generalization.
4. **Cheapest falsifier:** the existing bounded player receives opt-in routed capture/supply
   offers from refreshed state. At most four steps per actor; no new campaign factory, policy
   reward tuning, teacher route or emulator memory editing. One supply visit plus up to three
   model choices is sufficient to expose whether the loop composes.
5. **Time box:** two hours to implementation qualification and an actual bounded check, then
   reassess. GitHub checks run in the background; do not wait for duplicate main qualification.
6. **Stop condition:** unsupported field move, no feasible route, changed destination need,
   stale origin, failed verification or current hard action/frame limits. Preserve the first
   result and diagnose it; do not extend the old search limit or replay old consumed trials.

## Scope

Reuse `RedSemanticTransportRoute`, `RedFreshGoalDestinationBinder` and the existing title-neutral
route/goal composer. The new seam is the refreshed opportunity enumerator, not a navigation
rewrite. It must plan without controller effects, rebind the actual skill after arriving, and
retain transport costs under the selected semantic goal. It may not forge a destination
observation to advertise feasibility or convert walking into an exploration target.

Capture and Mart resource prerequisites should have one implementation shared by local and
remote offers. The first opt-in mode is walking-only; unsupported HM mechanics remain masked
instead of being advertised to an executor that cannot perform them. Old local profiles and
one-shot experiments keep their behavior. Private profile extension and any replay use are
declared as known-training integration, not new evaluation roots or fitting data.

The dashboard already has its spectator redesign. Keep real frames, actual actor/goal,
collection, party and work status visible; never keep a finished producer labeled live.

## Pre-input qualification

The opt-in `--routed-resource-goals` path is implemented through the same observer in preflight
and real arms. Walking transport is private; local resource checks are shared, arrival binds a
fresh skill, and the existing hard per-step/episode limits are unchanged. Field-move actions
remain unavailable in this walking-only integration. No weights or safety thresholds changed.

Two non-overlapping focused groups passed **306 tests** (139 gameplay/composition and 167
registry/state/profile/viewer tests). All **390** source files passed typing, and full lint,
documentation and product-focus checks passed. A read-only live inventory of the known train
capture found acquisition, restoration, exploration and a feasible resupply destination with
enough money. It used zero controller actions, frames, model predictions and fits.

The new private profile retains the original five provider specifications unchanged and adds
the existing Cinnabar Mart contract for ten Great Balls; it does not edit inventory or the save.
Profile SHA-256: `2b74125f55cd8245b2406b4f540d4574ecb46fefce284362e6a2a2409f30264b`.
The prospective pair is `red-fit29-resource-chain-train010-v1`, four steps per actor, original
training capture, unchanged 29-example model, explicit continuation and routed-resource opt-in.
Any resulting rows remain diagnostic and do not enter fitting. The old pair stays terminal.
This is a different resource-continuation integration, not a replay of an old frozen assignment
or an independent root. Supply safety and single-supported-choice steps do not become learned
decisions simply because the multi-step loop runs.

PR 234's original diagnostics-only head passed CI. Its new implementation is locally qualified
as above, with background publication checks to follow. The main tree exactly equals the old
PR 232 head despite its squash ancestry; consolidate the two dependent PRs without discarding
executed source commits. This Git history repair is not a gameplay or learning milestone.
