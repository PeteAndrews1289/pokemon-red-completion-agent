# Route-feasible capture candidate gate — September 14, 2026

## Working mission check

| Question | Answer |
| --- | --- |
| Reusable capability | Select a cartridge-derived missing-registration capture corridor by actual route executability while keeping species, source, coordinates and route identity outside the learned policy. |
| Learned authority | Maintenance grants none. If the unchanged gate exposes capture and evolution, a separately declared one-decision successor lets Model121 choose the acquisition family and control the played attempt. |
| Transfer test | ROM-free worlds with several missing corridors must choose the first executable candidate under changed origins and capability sets, reject unreachable candidates, and never expose the chosen target through portable features. |
| Cheapest falsifier | The exact Model121 terminal cannot expose both acquisition families without a species-specific exception, coordinate-bearing policy feature, state mutation or a protected-root scan. |
| Time box | One session, at most two hours, including a conditional played lesson. |
| Stop condition | Stop without gameplay if generic candidate qualification fails, if evolution remains unexplained/unavailable, or if source publication and zero-input exact qualification are not clean. Never retry the consumed reset. |

No model query, controller input, emulator frame advance, reset/source claim, fit or training
admission is authorized before the exact two-family gate passes. The current work is bounded
maintenance for the already-named heterogeneous collection lesson.
