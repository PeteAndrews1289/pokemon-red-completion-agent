# Model119: frozen restoration succeeds, singleton acquisition stops

## Outcome

Model118's exact frozen restoration ran once from its authenticated purchase terminal after an
action-free reconstruction proved the same model, menu, private routed-Center binding, state and
executable source. The party reached full HP and PP in **153 controller actions /5,424 emulator
frames**. Species, levels, moves, status, cash346 and the full inventory—including five Ultra
Balls—were unchanged. Registrations stayed85.

The choice was not redrawn or retried. Exact-source CI run34787154347 passed before controller
input, and execution made zero policy queries or teacher labels. The terminal was independently
re-observed before fitting.

## Learning and continuation

The verified recovery success became row119. Model119 has **119 settled examples /81 successes**,
including15 measured-source and40 economy-qualified rows, while preserving all118 prior rows. This
is training-only, in-sample evidence without an action trace, independent evaluation or authority
promotion.

The terminal reopened as a zero-input restart with85 registrations /65 living species /69
specimens. Its next action-free inventory contains exactly one executable candidate: a supplemental
fishing acquisition. The existing mixed-option contract correctly rejected a one-candidate menu.
No gameplay, model query, teacher label or fit followed. Gameplay is stopped at Model119.

That candidate must not be presented as a learned ranking decision. The next implementation should
reuse the established forced-singleton semantics, label it separately, exclude it from option-value
training, and retain its real success or failure without retry.

## External contribution

Gemini3.8 Flash High passed the admission review and caught a real runner mismatch: the first draft
compared readiness to the old menu-era source bundle rather than the newly qualified execution
bundle. Codex accepted and fixed that blocker before gameplay. Flash also suggested explicit
restore-kind and routed-Center binding checks; both were accepted. It unexpectedly invoked the
action-free preflight during a review-only assignment, but issued zero actions and frames, then
timed out after delivering its finding.

Claude Sonnet passed both bounded reviews. Codex accepted its missing-export finding and clean
worktree guard. Both reviewers suggested separating registry changes; that was rejected because
the registry is mandatory executable-source identity. Neither reviewer edited the repository or
ran gameplay. Current service quota was unavailable from both CLIs.

## Verification boundary

The exact execution preflight passed with zero input. The development admission slice passed94
tests and the wider focused gate passed137, with style, changed-source typing, product-focus,
documentation, public-artifact and registry checks green. Exact hosted CI passed all stages.

A duplicate local broad run was deliberately stopped after hosted CI became authoritative; it had
reached **7,742 passed /3 skipped /1 deselected /1 expected failure** at66% with no failures. The
deselection is the pre-existing machine-only PyBoy metadata identity check on this Mac, not a
failure of this change.

## Product gate and next session

The required finish line remains unmet: one authenticated non-deterministic, model-directed Red run
must complete the story and full local Red Pokédex before any ROM hack. The current39-entry
remainder is only the124-entry native scope; version, trade and event dependencies remain explicit.

Next, publish and qualify a forced-singleton bridge for the sole fishing acquisition, prove that it
uses zero model or teacher queries and cannot enter choice training, then execute it once from the
Model119 checkpoint. Estimate: one bounded session,60–120minutes.

Recommended model: **GPT-5.6 Sol, High effort, Fast off**. The bridge is bounded integration with a
known precedent. Escalate to Astra High only if the supplemental binding cannot reuse existing
singleton accounting without weakening the learned-choice boundary.

[Evidence](../evidence/red-model119-frozen-restore-2026-09-13.json) ·
[Roadmap decision](../roadmap-decisions.md)
