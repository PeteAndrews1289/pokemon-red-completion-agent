# Full-local Red shared-departure redesign

Status: redesign implemented and action-free qualified; no new gameplay or learning claim.

## Mission check

- Capability: reusable selection between travel-capable capture and native level evolution
  from one observed departure, with all151 local Red registrations as the completion target.
- Learned authority: retain model choice of the actual executable family; no teacher target,
  route identity feature, unexecuted success label or fallback action.
- Transfer test: ROM-free changed-map, missing-route, changed-stock and inherited-credit cases;
  actual unseen-lineage and cross-title performance remain untested.
- Cheapest falsifier: the real router cannot expose both acquisition families action-free.
- Time box: one session, at most two hours, then reassess against measured evidence.
- Stop: no input without two genuine families, coherent local/shared/physical views and a
  retained measured-outcome path. No sealed roots, consumed trial retry or full-game replay.

## Correction to the prior diagnosis

The prior inspection used only the source-local enumerator, not `RedResourceGoalRouter`.
Travel-capable capture and routed native evolution already exist. The failed local-menu check
did not establish that these capabilities are structurally location-incompatible. Reusing the
existing router is the redesign; another route engine or historical-menu search is unnecessary.
The historical catalog identity also does not by itself prove disjoint upstream lineage.

## Reviewer adjudication

Claude Opus4.8, High, completed a read-only CLI audit using the renewed subscription login.
Accepted: registered configuration hashes differ from raw profile hashes; routed bindings lack
the suffix the old join required; capture targets must respect actual provider demand/allowlists.
The replacement constructs bindings through the real router, rather than trusting arbitrary
callbacks with plausible names. Physical precursor presence is necessary, not sufficient:
native availability and routing must also succeed.

Rejected: Claude's proposed use of `policy.registered()` as local owned flags. That method
includes shared history. Actual local flags remain the sole local-completion authority.
Also rejected: interpreting a list of possible quota-one capture targets as a promise to catch
all of them. Actual execution and outcome accounting remain separate from menu eligibility.

Gemini3.8 Flash High completed a read-only test review. Accepted its legacy-serialization,
local/shared/physical separation, reserves, real-router and allowlist regression categories.
Its illustrative species names were not repository identifiers; fixtures use real adapters.
Neither headless CLI exposes remaining subscription percentages. Claude's follow-up delta audit
hit a session limit (429) after about272seconds, before returning a verdict; its message reported
reset at13:00 America/New_York on September14. Weekly quota is unavailable. The first audit
completed in about161seconds. No credit purchase, reset or billing change occurred; neither
reviewer remains running. Do not interpret the incomplete follow-up as approval.

## Verification and next decision

363 focused ROM-free tests and164 documentation/focus/roadmap tests passed. Repository Ruff and
configured mypy passed across497source files; source plus
the two changed scripts passed mypy across499files. An expanded non-CI sweep including all
historical scripts reported107errors in24files; that unrelated surface was not repaired here.
The full pytest suite was not repeated. Prospective collection metadata was regenerated and checked.

The real gate passed three times on one previously inspected saved state: first integration,
compatibility recheck, then the final single-choice/terminal contract. All had two acquisition
families, zero controller actions/frames
and unchanged save bytes. No additional historical state was searched. The real departure was
a field state; Center departure was qualified with ROM-free router/native-provider tests.
Input/tick/release were blocked by the existing read-only controller.

The new API constructs bindings through the live router, joins actual capture demand and
allowlists, preserves all original available goals, and shares a one-shot stale-departure guard
across them. The existing player observes full-local151 checkpoints and rewards. New policy and
checkpoint versions preserve global history; historical shared124 serialization remains unchanged.
Tests cover actual registry composition, missing routes/precursors/resources, protected stock,
stale state, single dispatch, scope tampering, local novelty over151, and durable recovery reload.

A final Codex integration check caught a lifecycle defect: rerunning the diversity gate during
the post-action observation could lose the terminal ledger after a successful goal removed one
family. The factory now allows one attempt across all preselection observations, then exposes
fresh read-only terminal observations with no second controller authority. Both success and
exception-after-partial-progress tests retain the new registration without re-gating.

Model121 remains121 examples/83 successes/86 local registrations. The diagnostic's18 local entries
belong to a separate save and must not replace that counter. There was no query, outcome, example,
fit, promotion, independence result, hack or Crystal access. The anti-drift alarm remains active.

Next: one60–90minute session connecting this qualified runtime to an eligible authenticated train
departure and the existing durable collector. Freeze one model choice, execute only that binding
under hard budgets, retain success/failure/costs and the fresh terminal ledger, then fit only the
measured outcome. The diagnostic supplies no train eligibility or consumed-trial retry authority.
Do not redesign another route engine or resume the Model121 resource-only loop.

Next-session recommendation: **GPT-5.6 Sol / High / Fast off**. This is now bounded execution and
outcome admission rather than broad architecture. The [official model guidance](https://developers.openai.com/api/docs/models/gpt-5.6-sol)
confirms Sol supports High; the task-specific recommendation is engineering judgment, not a setting
change or a claim about measured session savings.

[Path-free evidence](../evidence/red-full-pokedex-shared-departure-2026-09-14.json).
