# Complete evolution option and measured learning

## Mission check

- Capability: choose and complete useful collection development through bounded existing skills.
- Learned authority: the current history-capable model selects among actual supported goals; fit only its fresh executed outcomes, then test a post-fit continuation.
- Transfer test: ROM-free varied species/order/progress/budget cases; live same-lineage Red continuation is integration, not independent generalization or Crystal transfer.
- Cheapest falsifier: from the authenticated official saved endpoint, can the model-facing evolution option finish with exact collection preservation within measured feasible bounds?
- Time box: four hours for the first cycle, reassess after90 minutes without executable learning integration. Continue further cycles only after documented review toward the unchanged useful-play checklist.
- Stop condition: retain and inspect any failed episode before a successor; no consumed trial retry, diagnostic fitting, fabricated labels, sealed access or full replay. Stop the portion only at its unchanged checklist or a genuine unresolved blocker.

## Prospective sequence

1. Compose existing four-battle quanta inside one model-facing option, with bounded quanta,
   wall time, monotonic XP and exact collection/safety checks. Preserve the existing single-quantum
   component interface for diagnostics/tests. No new trainer or path script.
2. Declare an explicit completion-dose mode (30000 actions /3000000 frames per choice), sized
   from the observed runtime. Bind it in new training-plan schema and dataset admission; historical
   plans/rows retain6000/600000. No silent changed normalization or enlarged budget on old records.
3. Run one fresh model-selected training continuation from official checkpointb64023f4, not
   the diagnostic Rapidash save. Fixed seed2026090701, up to4 choices, max120000 actions /
   12000000 frames per episode. Keep all sampled failures; fit eligible observed outcomes only.
4. One short post-fit saved-state continuation (up to2 choices), then reorient on remaining
   useful-play checklist evidence. Additional work requires a written result and prospective
   next cycle, not automatic replay of failures.

Partial evolution has no XP reward in the current target. It remains incomplete/costly rather
than a positive completed-goal label. This session aims to supply completed options instead of
introducing a new reward feature. Independent learned advantage remains unproved by this loop.

## First lesson retained; reorientation before continuation

Episode A selected acquisition, then stopped after12 inputs/288 frames with unchanged collection.
Its read-only terminal audit proves the fully healed Center nurse farewell is still visible despite
ready movement flags. The existing farewell recovery was only connected to evolution, not other
routed goals. Reuse that exact bounded recovery at authenticated transport departure; preserve
the chosen goal, original route origin, all preparation costs and the final independent verifier.
Do not add a new walk string or force evolution.

The actual failed choice was admitted and fitted: model35→36, all previous rows retained,
zero counterfactual targets. A and its fit are closed. Prospectively run one post-fit training
continuation B from A's saved terminal with seed2026090702, up to2 choices, the same declared
completion dose, and no retry. If B creates eligible outcomes, retain and fit them before any
subsequent declared cycle. These are correlated training results, not independent evaluation.
