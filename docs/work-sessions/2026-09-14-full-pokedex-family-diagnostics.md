# Full-Pokédex acquisition-family diagnostics

## Result

The failed two-family menu now retains a structured, identity-free reason for both capture and
evolution. The contract distinguishes an inaccessible capture route, missing capture resources,
an absent evolution precursor, a protected precursor and an already-registered evolution target.
It also fails closed for missing declarations, non-solo catalog targets and no executable missing
target.

The diversity gate is unchanged. A valid proposal still requires both acquisition families, and
the families marked ready must exactly equal the candidates with executable bindings. The public
report contains only `acquire`/`evolve`, availability, portable reason enums and zero identity
fields—never species, encounter source, profile or binding identifiers.

The one-source inspector now serializes these reasons on failure. It remains read-only and verifies
unchanged save bytes, frame count, pressed buttons and action count. It does not construct a model
authority. This improves future evidence; it does not retroactively explain the third source,
whose frozen record lacks the new fields and remains closed.

## Mission check

- Reusable capability: explain why a same-departure acquisition family is unavailable without
  target-specific patches or another source inspection.
- Learned authority: unchanged. Model121 remains121 examples/83 successes/86 registrations.
- Transfer test: portable option and router reasons are title-neutral, but no cross-title result.
- Cheapest falsifier: each source-free failure fixture must produce its distinct reason while the
  other family remains ready, with no identity leak or game effect.
- Time box: one60–90 minute source-free session.
- Stop condition: any weakened diversity gate, indistinguishable failure, leaked identity,
  model query, input or frame. None occurred; stop before selecting a source.

This is maintenance for the named measured collection lesson. It adds no learning, authority,
registration or transfer result and cannot authorize a full run or later game.

## Verification

- Goal proposal and inspector:25 passed.
- Named non-consuming rehearsal:22 passed,11,699 deselected (previously17 cases).
- Related direct-profile, router, registration, training-plan, continuation and evolution suite:
  247 passed.
- Ruff passed the changed files; mypy passed499 source files.
- Prospective collection registry regenerated and checked: registry
  `9a415b54...2a25e215`, bundle `d0e25c27...955dadae`.
- Published source: `93ca9495d0abccf7511639af2abdc5cbbec41bb5`; exact-head
  CI34918614083/1 passed11,713 tests, with four skipped and one expected failure.

No source was selected or claimed. No private payload, ROM, registration session, model query,
controller action, emulator frame, outcome, example or fit occurred. Nine eligible sources remain.

## External review

Gemini3.8 Flash High completed a bounded read-only review and returned PASS with no P0/P1/P2
finding. It checked the strict two-family invariant, all five distinguished failure cases,
identity-free serialization, zero-effect boundary and regression coverage. Codex accepted no
changes because it reported no finding; the local tests remain the qualification evidence.

The first headless invocation was blocked from reading files and produced no review. A sandboxed
read-only retry completed. Claude was not used. Headless output exposed no current service quota
window or task cost; no Sol/Astra saving is claimed.

## Next bounded action

Exact-head CI passed. Next, design a practical prospective supply plan for exactly one measured
collection lesson. The plan must avoid spending a fresh source on another software check, retain
the model's choice and every outcome, and bind any permitted training reset in advance. This
qualification alone does not authorize a fourth source, replay V1–V3, or reconstruct V3.

Next session recommendation: **Sol / High / Fast off**, approximately60–90 minutes for the source
supply/admission decision and, only if separately justified, preparation for one measured lesson.
Use Astra if the admission contract itself needs a consequential redesign.

[Evidence](../evidence/red-full-pokedex-family-diagnostics-2026-09-14.json) ·
[Roadmap](../model-first-roadmap.md) ·
[Triggering failure](2026-09-14-third-source-roadmap-audit.md)
