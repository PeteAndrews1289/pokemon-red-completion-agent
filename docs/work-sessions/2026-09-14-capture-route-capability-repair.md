# Capture-route capability repair — September 14, 2026

## Working mission check

| Question | Answer |
| --- | --- |
| Reusable capability | Retarget a cartridge-derived wild-capture objective while retaining only the generic, previously declared travel and capture abilities that can legally reach it. |
| Learned authority | None in this maintenance session. The repair unblocks the already-named successor in which Model121 chooses between one executable capture and one executable evolution objective. |
| Transfer test | Retarget synthetic profiles to different cartridge-derived corridors and prove generic declarations survive without copying target identity, coordinates, encounter bounds, or unrelated parameters; then run one zero-input qualification against the authenticated Model121 terminal. |
| Cheapest falsifier | A ROM-free regression shows the declarations are lost or fabricated, or the exact read-only terminal still cannot bind a legal capture route after the scoped preservation rule. |
| Time box | One session, at most two hours. |
| Stop condition | Stop with no gameplay if strict capability preservation cannot make the exact capture family ready, if route feasibility needs a target-specific exception, or once the zero-input qualification proves the repair. Do not retry the consumed reset. |

This is maintenance, not a learning result. It may change no Model121 example, success,
registration, authority, or transfer counter. The correlated reset identity remains consumed;
no model query, controller input, emulator frame advance, protected-source scan, source-four
selection, fit, or full run is authorized here.

## Result

The ROM-free regression reproduced the defect: direct full-Pokédex retargeting discarded every
generic capture capability when it replaced the old source with a fresh cartridge-derived
corridor. Published source `239a7f74fd393a09efce5bcf49f9ec927ab739e3` now retains exactly eight
already-declared generic opt-ins: capture access/status support, Cut, Surf, Fly, indoor Fly
departure, travel capture and observed-local capture. It does not invent absent permissions or
copy the old target, source, coordinates, species allowlist or encounter route.

This repair is real but insufficient. One source-bound, action-free qualification reconstructed
the Model121 terminal and reused its authenticated registration policy. All eight declarations
survived exactly, yet capture still failed as `router_binding_unavailable / missing_capability`.
Evolution also reported the same failure, conflicting with the preceding session's legacy-profile
diagnostic that called evolution ready. The new profile is different, but this check does not prove
that the capture repair caused the evolution difference; no such causal claim is made.

The first qualification record rebuilt an overly conservative temporary reserve policy and is
superseded. V2 reused the authenticated policy and is authoritative; both records remain preserved.
No gameplay, model query, controller action, emulator frame, claim, outcome or fit occurred.

Focused route/profile/player integration passed386 tests in22.79 seconds. The first broad command
omitted `scripts` from `PYTHONPATH` and stopped during test collection; the corrected invocation
passed with no product-test failure. Registry regeneration/check, active-focus validation and diff
validation passed before source publication. The full suite was not repeated because the prior
session had just completed it with11744 passed, one known unchanged local-runtime fingerprint
failure and one expected failure.

Hosted CI for the source commit stopped at import sorting in the new test, before tests. Import-only
commit `1d56cdb46547e094f054de863bdd9e8c7a17b9c9` fixes it; full local Ruff and the nine direct-profile
tests pass. This changes neither runtime source nor the exact-terminal qualification.

Learning delta:0 examples,0 successes,0 fits and0 registrations. Model121 remains121 examples,
83 successes and86/151 registrations. The original collection checklist stays21/22, expanded25/26,
and the fresh-start Red gate remains0/5. All nine unused sources and the consumed reset remain
untouched. Gameplay is stopped.

[Path-free evidence](../evidence/red-capture-route-capability-repair-2026-09-14.json).

Next: **Sol / High / Fast off**. In one bounded session, qualify a generic route-feasible capture
candidate boundary and, only if it produces capture plus evolution without identity-bearing policy
features, prospectively declare one new collection lesson. Astra is unnecessary unless that
generic boundary exposes a genuine architecture or authority decision.
