# Direct catalog-origin full-local training bridge

## Result

The existing bounded Red player can now start a registered completion-dose training episode
directly from one authenticated training-catalog capture. The direct schema does not contain a
predecessor episode, restore profile or checkpoint digest. It retains the original catalog profile
digest separately from the derived execution profile, while preserving the catalog, assignment,
root lineage, exact state/envelope, model and current source identities.

A separate direct registration-session schema anchors the initial local-Red policy to that exact
training plan, catalog, context, state and envelope. Direct and continuation sessions reject one
another. Existing continuation plan/session schemas and bytes are unchanged, and their checkpoint
authentication remains mandatory.

The execution profile no longer requires caller-entered species or routes in full-local mode. A
read-only source observation chooses the first missing canonical level evolution backed by an
actual boxed precursor, chooses a missing ordinary wild source deterministically, and derives its
reversible encounter corridor from authenticated cartridge geometry. Full-local calls containing a
manual evolution target or wild-source transition now fail before source authentication.

## Mission check

- Reusable capability: direct catalog starts can enter the same registered player/fit pipeline as
  later continuations without fabricating history.
- Learned authority: unchanged. Model121 remains at 121 examples, 83 successes and 86 local
  registrations.
- Cheapest falsifier: after exact-source claim, the frozen state lacks a boxed canonical level
  evolution, a missing wild corridor or two actually executable routed bindings.
- Transfer relevance: the split between catalog origin, execution configuration and registration
  session is title-adapter work; no Crystal or cross-title result is claimed.
- Scope: engineering only until a measured model-selected outcome is durably retained and fit.
- Time box: one session; no substitute source, historical retry or full-game replay.

## Safety and compatibility

Profile derivation compares saved bytes, frame count and held buttons before and after observation,
and rejects battle/non-ready states. A missing two-family configuration is normalized to the
runner's fail-closed error contract. Dataset admission skips parent-checkpoint lookup only for the
explicit direct registered schema; legacy registered plans still require their exact terminal
checkpoint and train lineage. Direct session documents reject hybrid parent fields and state-anchor
swaps.

The direct runner requires a previously published account-wide root-pair claim before it resolves
or opens the ROM, saved state, envelope or profile. The claim binds this exact published commit,
runner bytes, logical root and physical state/envelope pair. Once the capture is opened, its
computed pair must match the consumed claim; mismatch fails before profile derivation, preflight or
model access. The claim digest is retained in both direct training-plan schemas.

The frozen selected source has not yet been claimed or opened. Publication and exact-head CI must
precede that irreversible claim. The subsequent live attempt must re-open only
`full-local-source-20260914-v1`, atomically claim its exact logical/physical root pair, derive and
preflight the profile, and stop without replacement on conflict or a one-family menu.

## Verification and review

Focused direct-plan, session, profile, dataset, proposal, continuation, outcome and economy tests
pass, including claim authentication and post-open byte-pair matching. Ruff and mypy pass for the
affected runner and all source modules. The hosted-equivalent suite passed 11,699 tests with one
expected failure. The sole excluded local-only test is an existing exact-Mac PyBoy package-metadata
fingerprint mismatch; hosted Linux CI skips that machine-specific qualification.

Claude Opus 4.8 High performed a read-only diff review. Its only concrete issue was inconsistent
error typing when inventory-derived configuration was unavailable; that path is now normalized and
covered by a regression test. Claude found the direct/legacy schema split, action-free checks,
origin anchors and continuation behavior sound. The review used $1.6006555 total after a bounded
inspection exhausted its first response budget and a report-only continuation recovered the
verdict. A final claim-order inspection used another $0.9096645 but exhausted its cap before a
second verdict, bringing recorded Claude use for this session to $2.51032.

Flash 3.8 High proposed hybrid-schema, component-anchor, exhausted-inventory, route-reachability,
tie-break, malformed-caller, catalog-replay and concurrent-session tests. Hybrid direct plans and
sessions, anchor swaps, exhausted boxed inventory and caller target injection are covered here.
Existing router preflight and pair-claim tests retain route and concurrency authority; a historical
Flash's final claim-order review found no concrete bug and suggested a direct missing-claim bailout
test; that test now proves the runner fails before resolving the ROM or saved payload.

## Next bounded action

Publish this bridge and require exact-head CI. Then use a controller-capable source-choice boundary
to create the claim required by this runner before the exact frozen payload is opened. If the two-family
preflight passes, freeze and execute one exploratory Model121 choice exactly once, retain actual
costs and terminal local flags, and fit only the measured outcome. Otherwise retain the claim/menu
failure and stop.

[Previous source selection](2026-09-14-full-local-training-source.md)
