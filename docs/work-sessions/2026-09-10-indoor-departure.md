# Retained collection learning and indoor departure

Three retained choices /three fits: Golbat and Raticate registered, then a failed Mansion destination attempt. Model26,51 registrations,46 specimens. Two native evolution choices and one learned regional source choice; capture preparation stopped at a Center before the destination. No indoor capture yet.

Batch duration 1369.84 seconds; 24684 actual actions and 2175041 frames. Actual final Center retains18 balls and3503 money. Its failed regional choice is fitted once; two unused source proposals from the evolution decisions are not labels. The new corpus has26 rows; historical114 is separate.

## Evidence and failure

Authenticated episode manifests match all three receipts. The third private diagnostic is RedCapturePartyError: capture preparation lost its selected source. Its observed start was Route11 and final endpoint is Vermilion Center. The existing Fly binder explicitly refuses indoor starts. A zero-input current restore passes, exposing six walking capture choices rather than the previous eight with flight. No capture at Mansion was verified.

## Mission check for the next repair

- Capability: compose indoor exit with the existing flight and selected collection destination.
- Learned authority: unchanged destination choice; maintenance unblocks its execution and adds no setup label.
- Transfer test: different exit maps, wrong actual boundary, absent flight capability and changed resources.
- Cheapest falsifier: unavailable exit emits zero input; actual exit must verify before fresh flight binding.
- Time box: one hour, then reassess or park rather than grow a walkthrough.
- Stop condition: wrong endpoint, silent destination substitution, lost resources, duplicate labels or consumed retry.

Flash High drafts only an isolated module and tests; Codex reviews, integrates and tests. Its party-only stone draft passes18 tests but is not integrated or emulator-proven. Latest allowance81.09% five-hour /83.73% weekly remaining. No exact usage-cost comparison is implied. Claude was not used in this section.

Next: Compose a verified indoor departure before requalifying the same Fly destination, then continue model26 from its actual terminal. Keep all failed costs and prior registrations; no retry or resource reset. Review the isolated stone-evolution capability without claiming it is live.

## Integration review

Flash supplied the isolated departure composer. Its initial fixtures offered a
flight back to the same town and seven tests failed before exercising departure.
Codex corrected those fixtures, retained route-drift exceptions, and added full
starting-observation checks, actual source-identity rejection and remaining-budget
propagation. Qualification uses a disposable projected availability check only;
the actual outdoor endpoint is independently verified before a fresh flight is
bound.202 focused tests pass, including existing Fly, capture preparation,
resource routing and documentation/focus tests.452 source files pass type checks.
This is engineering evidence, not proof of an actual indoor capture.

The first actual restore rejected the changed menu before input. The repair is
now a prospective `--indoor-fly-departure` profile transition, preserving the old
checkpoint's exact availability semantics. Historical flight profiles still
refuse indoor departures. New profiles explicitly retain the capability when
capture destinations change. This avoids weakening the checkpoint verifier.
The wider compatibility/script/profile suite passes315 tests before the added
retarget-retention regression. Latest Flash allowance77.27% five-hour and82.86%
weekly remaining; the CLI is closed. No live input was issued under the first
indoor-capable source because its old-menu restoration was correctly rejected.
