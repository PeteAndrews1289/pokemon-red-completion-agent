# Collection travel access

## Mission check

- Capability: reach useful collection and evolution destinations using observed legal Fly travel plus cartridge-derived walking, without per-species routes.
- Learned authority: broaden the existing goal/destination menu; Fly remains deterministic transport, not a reward-generating goal. Fit only actual eligible model choices.
- Transfer test: vary visited towns, carrier slots, landing coordinates, onward paths and stale observations in ROM-free tests. Red execution is one correlated lineage, not cross-title proof.
- Cheapest falsifier: from native110's retained Route22 endpoint, qualify a reachable evolution destination, then a short model-selected continuation. Preserve the ledger and resource costs.
- Time box: one session, two hours. Prefer existing controller/composer seams; stop architecture work once a bounded live opportunity can be tested.
- Stop condition: no guessing landings, trial confirmation, unsupported field mechanics, fabricated successful routes, lost specimens or consumed-trial replay. If travel fails, retain and diagnose that failure before continuing.

## Implementation plan

1. Decode Fly landing coordinates from the pinned cartridge table; do not transcribe town coordinates into a route.
2. Reuse the observed-menu Fly controller and routed-goal composer, with a checked landing followed by the computed walking plan. Keep the ordinary walking path preferred where available.
3. Add opt-in routing scope, action-free readiness and stale-state/budget tests. Then one published executable and short real continuation; no hosted-CI wait in the gameplay loop.
4. Audit outcomes, refresh the dashboard evidence and roadmap/handoffs/narratives, and select the next useful step from measured results.

The previous batch is closed: 110 examples, 40 specimens, 32 living species. It is not replayed.

## Local qualification

320 focused tests passed across the Fly controller/transport, resource router,
profiles, historical continuations, runners and route/destination composers.
Changed source modules passed typing and lint. The full suite was not run locally.
The new transport reuses the actual menu controller, a cartridge-derived landing,
an onward walking plan and the existing fresh-destination composer. A separate
256-dispatch ceiling bounds flight input within the runtime's existing hard frame
and episode budgets. Earlier profiles remain walking-only.

`--evolution-fly-transport` is an ordered future profile transition, not a rewrite
of ancestor settings. First scope is the current level-evolution objective;
Fly-assisted regional capture enumeration is not yet enabled.

## Measured continuation and reorientation

1 automatic steps, 1 fits: 110 to 111 examples; 17,523 actions / 1,549,746 frames; last fit settled at 4.4 minutes. 40 specimens and 33 living species retained; 0 failed steps. Model110 chose evolution, flew to the supported training region and evolved Drowzee13 into Hypno26. The next action-free menu had no supported goal; gameplay stopped before step2. Its orchestration exception is preserved, and a typed empty-menu stop is now qualified.

The native goal/destination model now has 111 examples; every intermediate model was carried into the next attempt. Only admitted actual outcomes were fitted; deterministic safety support receives no learned-choice credit. The final fitted model has not played. No learned combat, independent advantage or cross-title claim.

[Public evidence](../evidence/red-collection-fly-learning-2026-09-09.json). Final checkpoint: `1d2c828199a6222f471ed45d7868fbe2e8e27987e83e35e382c4d3992bc29e46`. Private continuation prefix `collection-fly-20260909-d` is consumed and must not rerun.

Next: Extend the qualified Fly transport to useful capture destinations, then a short model111 continuation from Route11. Preserve the useful Drowzee/Hypno pair; broaden generic evolution readiness afterward.

Keep the local Phase5 checklist2/3; full attainable-target coverage remains unfinished. No fresh-game, independent reliability or cross-title claim.

Next-session recommendation: Astra High, Fast off; bounded integration and measurement. OpenAI Docs verified supported effort levels; the recommendation is engineering judgment.

## Post-run audit and recovery

The successful episode preserved all 40 specimens, including the spare Drowzee,
and increased registered species from 36 to 37. Balls stayed at 11 and money at
20,913. The actual model110 choice produced one eligible example; model111 was
fitted afterward and has not yet controlled the game.

The batch did not finish normally: its second, action-free preflight raised an
empty-menu exception. There was no second episode or controller input. The private
batch summary was explicitly reconstructed from the original outcome, fit and
source receipts; its reconstruction flag and error remain in the public evidence.
The 266.341-second timing ends at the completed fit, not at the later exception.

The subsequent repair gives an empty menu a specific exception type and makes the
automatic loop save its completed work and stop normally in that case. Unrelated
planning errors still propagate. This repair has 336 passing targeted tests across
the affected stack, including no-second-input and unrelated-error regressions;
it was not retroactively used for the completed episode. No full-suite or external
agent audit is claimed.

The next development target is capture access, not a longer replay of this batch.
Extend the existing guarded transport to useful capture destinations, qualify it
locally, then test model111 from the retained Route11 checkpoint. Broader evolution
eligibility follows. The dashboard shows a saved state while stopped; a live game
feed exists only during an actual gameplay step.

Final closeout qualification: 354 targeted tests passed including dashboard and
roadmap checks; changed-code lint, targeted typing, public-artifact checks,
documentation/focus checks and regenerated registry verification passed. The
infographic was rendered and visually inspected. Hosted CI for the earlier
executable publication was still in progress at the closeout check; no hosted
success is assumed and gameplay did not wait for it.

Known presentation debt: the overview still uses a legacy “shadow-only” training
caption. The newly fitted model111 has not played, but its predecessor model110
did choose the completed evolution. Read the episode evidence and saved-state
panel for that distinction; this session does not claim the caption was fixed.
