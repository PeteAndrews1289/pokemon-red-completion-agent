# Full-Pokédex goal proposal reaches its action-free falsifier

## Outcome

The 151-entry Red inventory is now connected to authenticated goal proposal. The new boundary
admits only locally missing targets whose pinned acquisition method agrees with the live goal
mechanic, whose executor is bound to the exact profile declaration, and whose prerequisites are
physically present when required. It refuses to construct a proposal with fewer than two distinct
executable acquisition families.

The first real saved-state inspection failed that gate. Gameplay therefore remained stopped.

## Measured action-free result

The inspected historical train context is outside the Model106–121 checkpoint lineage. It had 18
local registrations, three missing species at its Mansion wild source, and a physical precursor
for a separate missing level evolution. The live menu nevertheless exposed only acquisition,
restoration and exploration. Capture was executable at the encounter source; the evolution skill
was unavailable because its supported departure boundary is a Pokémon Center.

- Proposed acquisition families: **1 wild / 2 required**.
- Controller actions, emulator frames and pressed buttons: **0 / 0 / 0**.
- Model queries, teacher labels, outcomes and new examples: **0 / 0 / 0 / 0**.
- Model121 remains **121 examples / 83 successes / 86 registrations**.
- No claim, fit, promotion, transfer result, ROM-hack work or Crystal work occurred.

This is a useful falsification but not a learning result. It is the second consecutive session
without measured learning output, so the North Star anti-drift alarm is now active.

## Implementation and verification

Commit `17eab16f` adds the private proposal candidates, policy-safe aggregate, strict profile and
catalog joins, and ROM-free coverage for local/shared/physical separation, precursor readiness,
identity mismatch and single-family rejection. The focused suite passed 21 tests; Ruff, mypy and
the regenerated collection-registry check passed. The complete suite finished with 11,619 passed,
3 skipped, 1 expected failure and 1 failure: the pre-existing exact local Mac PyBoy metadata-digest
qualification. All 150 documentation/focus tests and the documentation checks passed. None of these
checks count as product progress.

## External review

Gemini 3.8 Flash High proposed the narrow inventory-to-binding join and the fail-closed family,
identity and local-registration checks. Those were accepted. Its suggestion to use a private option
family hash as the acquisition-family label was rejected because it does not identify the actual
game mechanic.

The standalone Claude CLI binary is installed at version 2.1.197 but still reports that login is
required. Claude Sonnet 4.6 completed through `agy` after the implementation commit and agreed that
stopping before gameplay was correct. Its two structural alarms were rejected: profile validation
already forbids duplicate goal kinds, and Mansion species 77, 88 and 110 are in the pinned solo
catalog. Neither CLI exposes remaining quota percentages.

## Next bounded decision

Do not search another historical menu or add another inventory wrapper. Redesign one reusable
collection departure boundary so a travel-capable capture and native boxed evolution can both be
offered from the same authenticated Pokémon Center state. The cheapest falsifier is action-free:
if that shared-departure menu still cannot expose two independently verified acquisition families,
stop again without gameplay.

Next-session recommendation: **GPT-6 Astra, High effort, Fast off**. The evidence now requires an
authority and executor-boundary redesign, not ordinary planner wiring. Return to Sol High once the
shared-departure contract is settled and implementation becomes mechanical.
