# Red routed model-play session — September 6

## Mission check

1. **Capability:** choose and execute supported collection, party and resource goals from a
   common semantic decision point, retaining actual living-Pokédex outcomes.
2. **Learned authority:** the saved 29-example option-value model selects the learned arm's goal;
   deterministic skills implement it. The paired deterministic control remains a separate actor.
   This is bounded development, not a full-player promotion.
3. **Transfer test:** retain all four frozen development roots outside fitting and compare both
   actors from the same conditioned state. Treat four pairs as descriptive evidence; preserve
   identity-free semantic features for later Crystal work without running Crystal now.
4. **Cheapest falsifier:** one authenticated routed pair can reveal wrong menu binding, a stale
   choice, a skill failure or no collection benefit. Keep every frozen pair in the denominator.
5. **Time box:** two to four hours including implementation and the earliest safe bounded input;
   reassess before expanding architecture or repairing routes.
6. **Stop condition:** consumed input, changed recipes or denominator, a choice not durably
   committed before outcomes, unrecoverable diagnostics, unverified progress, or a teacher
   replacing the model. Never retry a claimed trial or fit development evidence.

## Starting checkpoint and scope

PR 231's eight-lesson collection and single fit are complete. The model contains 29 real train
examples. Six new factual rows and both setup censors are retained; no completed trial or fit may
run again. The spectator viewer and generic bounded-chain bridge are published in PR 232. Neither
is evidence that this fitted artifact has played a new live episode.

The four frozen development slots require their existing construction routes and routed provider
menus. Raw saves and singleton local menus are not substitutes. Reuse the production setup
resolver, validated origin capture, causal selected-arm executor and fixed utility. Keep the
existing train-only runner strict. Commit both policies before releasing either selected arm.
Then use fresh semantic state and living-ledger observations for the next bounded goal chain.

Ordinary engineering does not wait for repeated CI polling or advisory reviewers. Publication
qualification remains distinct from learner progress. No full replay, sealed Red, Crystal,
development fitting or public hosting is in scope.

## Implementation and bounded verification

The paired journal reuses the strict saved-model commitment, causal runtime, independent outcome
observer and fixed nine-component utility. The completion-first control reconstructs the original
nine need pressures rather than inverting the lossy seven-pressure model projection. Both see the
same offered goals and provider effort/risk estimates; the scorer also retains its original
travel-effort feature. Neither sees private bindings as policy features.

The Red consumer preserves the eight train terminals, exact four paired assignments and reserve
exclusion. Setup retains a validated shared origin and all provider proofs. The selected-arm
executor restores that origin for each actor. Durable attempts make setup/arm interruption
terminal; completed pairs recover without more predictions or game input. Private diagnostics
preserve exception class, message, exact-message digest and source frame locations. Path-bearing
messages are withheld under the artifact store's path-free contract; private diagnostics are
excluded from public receipts.

The command uses development-tier qualification: clean published code, exact source bundle,
authenticated frozen producer/plan and fitted record, unchanged ROM/runtime/registry, then a
200,000-action / 20,000,000-frame campaign ceiling. Local verification precedes publication;
another exact-main CI result is not a development prerequisite. This does not alter an old
benchmark or one-shot contract. There is no fitting entry point.

The spectator producer distinguishes deterministic setup, model-selected goals and the separate
control. It reports real meter costs, receipt-backed training, selected semantic goals, observed
collection counts and terminal progress. Frame failures disable the view without changing play.
The persistent overview stays on 8768 and the producer uses 8769.

The integration fixtures initially offered storage/supply despite empty boxes and full supplies.
They now have a nearly full active box and genuinely low balls while recovery stock is adequate.
This corrects test preconditions rather than weakening production offer checks. Both actual
selected-arm seams are exercised; skill success is not fabricated.

**104 focused tests passed**, covering routed setup, two selected arms, durable ordering, exact
recovery, forbidden train/reserve access, fresh pressure binding, failure diagnostics, viewer
isolation and existing shared viewer behavior. Full source typing and lint passed earlier in the
session; final qualification must check the publication tree again. No external auditor was used.

The prior spectator PR failed after 6,880 CI passes because its existing Crystal snapshot test
omitted the new `observed: true` field. A one-line schema correction was pushed as `e24d0b0b`;
15 targeted dashboard tests passed. No Crystal gameplay was used.

At this checkpoint there is no new real-game result or fit. The 29-example artifact is unchanged.
The next entry must record the actual preflight and four-root execution, not a projected outcome.

Final publication-tree verification: the 104-test group passed again, all 389 source files passed
typing, and full lint, documentation, active-state and public-artifact checks passed. The path-free
[engineering receipt](../evidence/red-routed-paired-engineering-2026-09-06.json) binds the source
bundle without claiming gameplay or a full local-suite result.
