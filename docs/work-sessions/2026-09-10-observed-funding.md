# Observed-map funding repair

## Mission check

- Capability: legitimate resource replenishment through routes that respect observed terrain.
- Learned authority: preserve the model's resupply choice and the quoted trainer; only the deterministic approach is requalified. Maintenance unblocking fresh model 38 collection.
- Transfer test: ROM-free changed-passage, alternate-path and state-identity variants; no cross-title claim.
- Cheapest falsifier: a static shortcut is closed in observed blocks, while an alternate route to the same trainer exists.
- Time box: 60–90 minutes including targeted tests and a first bounded continuation, subject to the active overnight deadline.
- Stop: unsafe or mismatched observation, changed trainer/quote, no safe route, or failed continuation authentication. Never replay a consumed choice or reset resources.

## Compatibility decision

Keep historical candidate enumeration unchanged so older saved menus remain reproducible.
After the model chooses funding, requalify the approach using current observed blocks,
without changing the quoted trainer or expected reward. Preserve all other trainer
bodies, sight lanes, story requirements and route limits. If no route to that same
trainer remains, stop before any new input rather than silently switch objectives.
This changes skill execution, not learner authority or historical outcome labels.

## Verified implementation and actual-state result

The funding executor now requalifies the selected trainer against observed current-map
blocks before the escort swap or any route input. It keeps the trainer and reward
identical, validates stable state and blocks, and uses the newly computed approach.
Historical enumeration remains unchanged. Missing routes and invalid observations
fail closed; no larger retry budget or Mansion-specific routing was introduced.

168 focused funding/planner/battle tests passed. The changed source file passed its
type check; lint and documentation checks passed. Tests cover a closed shortcut with
a safe detour, no route, changed map/battle/state/blocks, malformed terrain, trainer
or reward substitution, actual use of the revised plan and rejection before input.
These are targeted results, not a full-suite or independent generalization claim.

The actual retained model 38 state was checked with zero controller actions, zero
advanced frames, zero predictions and zero fits. Its sole statically quoted trainer
has **no observed-terrain approach**. The repair therefore rejects this option before
input. This verifies safe rejection, not successful funding or new collection.

No fresh gameplay batch was started: repeating a predictably impossible funding
choice would not establish useful competence. Model 38, 53 registrations, 47 specimens,
six balls and 548 money remain unchanged. The prior failed choice remains preserved.

## Next bounded session

Add prospective observed-terrain funding eligibility to new decision menus, with an
explicit versioned execution flag. Reconstruct old menus with their recorded flag
values; do not silently change saved checkpoint identities. Hide unreachable funding
options before selection while retaining other genuine alternatives. Authenticate
the actual model 38 endpoint, then perform a short fresh learning batch if at least
one useful executable alternative exists. If none does, report the specific missing
departure/puzzle capability rather than launch another known-failing choice.

This is a refinement of the same resource/collection lane, not a new phase or an
increase in learned authority. Budget 45–90 minutes for menu compatibility, focused
tests and one bounded outcome; the estimate is conditional on an executable option.
Flash and Claude were not needed for this small repair. Recommended next session:
Astra, High, Fast off; prioritize the integration and actual learner outcome.
