# Registered collection: capture-helper recovery

## Mission check

1. Capability: plan capture support from actual move, PP, HP and status readiness;
   recover through legitimate mechanics without losing physical collection or registration.
2. Learned authority: preserve goal/destination selection; unblock a new model3 lesson,
   not another teacher route or learned-control claim.
3. Transfer test: capability-based synthetic helpers and changed resource/status cases;
   subsequent Red continuation is correlated training, not cross-game validation.
4. Cheapest falsifier: inspect the retained failure without input and reproduce the
   mismatch; test the repaired readiness/recovery boundary before controller input.
5. Time box: one bounded session, up to two hours; stop expanding the repair after
   the actual failure is qualified and collect a short measured lesson.
6. Stop: unverifiable terminal, specimen loss, unsafe readiness, changed actor goal,
   retried consumed attempt or mixed-objective fitting. Preserve all prior outcomes.

## Division of work

Flash3.8 High drafts readiness tests and diagnostics in an isolated worktree from
209558a8. Only the named capture-party source, a new regression-test file and a
short audit memo are editable. No private artifacts, ROM, gameplay, fitting,
shell commands, publication or dependency installation. Codex inspects the actual
saved failure, independently tests the draft, adjudicates and alone continues play.

## Diagnosis and integrated draft

Read-only restoration of the exact failed terminal found a full-HP helper with
usable Sing PP but persistent sleep. Field control was settled at the Center PC;
no diagnostic controller input or frames occurred. Box capability is not readiness.

Flash's single194.97-second draft supplied diagnostics and18 new parametrized
regression cases;25 tests passed with the existing capture-party suite. Codex
fixed its nullable typing, removed broad exception swallowing and preserved all
original checks. The draft's unverified coordinates and proposed boxed HP-ratio
filter are advisory, not accepted execution facts.

Codex connected an explicit optional restoration callback to the existing routed
Center skill. It must execute and verify, recheck actual helper readiness and
preserve inventory/resources. All restoration actions/frames are part of the
selected capture goal and receive no extra training label. Ready helpers do not
trigger healing. Already being inside a Center avoids planning remote Center
routes. No safety threshold, original failure or learned-goal identity is changed.

Gemini quota after the draft:95.97%five-hour and91.85%weekly remaining; displayed
resets approximately4h54m and48h25m. The initial panel refreshed from cached values;
these are account-wide readings, not isolated token savings. Claude not used.

Before live continuation:218 targeted tests passed; four-module typing, lint,
documentation and public-artifact checks passed. The prospective continuation
uses the actual final model3 checkpoint, at most four new decisions and a
20-minute between-episode budget. It stops on a new non-search failure; it does
not replay the prior failed preparation or fit any synthetic recovery checks.
