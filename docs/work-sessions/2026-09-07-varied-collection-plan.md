# Next session — varied, resource-aware collection from model59

Start from S's actual terminal and fitted model59. Mixed collection/evolution5/5
is closed; sustained Red remains the current stage. Preserve the original living
specimens and all59examples. No successor has been declared or played.

## Mission check

1. **Capability:** compose collection development with legitimate supplies and PC
   capacity, across a different specimen rather than replaying successful Abra.
2. **Learned authority:** after necessary safety support, expose real competing
   acquisition/evolution opportunities. Keep deterministic resupply and source
   proposals out of learning counts; retain the actual chosen outcome and costs.
3. **Transfer test:** use a different observed non-attacking precursor (the stored
   duplicate cocoon lines are candidates) or another supported acquisition need.
   Same-lineage variation is not independent generalization or Crystal transfer.
4. **Cheapest falsifier:** restore S/model59, inspect actual affordability, storage
   and candidate reachability, then one bounded useful choice with retained fitting.
5. **Time box:** one two-hour session; reassess at90minutes without useful learner
   output. Reuse existing mechanics and reject another documentation/CI-only cycle.
6. **Stop condition:** no honest learned alternative after support; exhausted
   resources; unhandled full-box state; loss of a retained specimen; no recipient
   XP; repeated nonproductive recovery. No reset, sealed access or full replay.

## Sequence

Prospective local checklist, separate from the closed mixed-goal5/5:

1. Legitimate required supply support settled and retained, with model59 unchanged
   and zero-row support carried to the next fit.
2. Storage-aware capture composition qualified without hiding needed precursors,
   changing the chosen source or consuming the final active slot unprepared.
3. One varied useful native choice actually played and fitted with all59 prior
   examples and any intervening support results retained.

These are three local acceptance items, not a fraction of Red completion. Supply
and storage engineering are prerequisites; only item3 adds a learned outcome.

Session resumed September7 at14:50UTC; reassess16:20UTC, stop/reorient16:50UTC.
First allow at most20minutes for the repeated CI source-compatibility failure:
maintenance only, unblocking this named varied-collection continuation. Review
the default-disabled shared-XP branch against the previously accepted training
routine; preserve rejection of unreviewed changes and historical receipts. No
learning counters advance for this repair. If semantics cannot be justified,
stop the compatibility repair rather than widening its waiver blindly.

1. Exact next continuation is already action-free verified:23living/25specimens,
   Kadabra16 and retained Abra10; Route11,one ball309currency,one immediate boxslot.
   The current resupply/capture menu selects **deterministic safety resupply**.
   Do not report that forthcoming support as learned authority or a training row.
2. Carry the real supply outcome/checkpoint forward. Retain any zero-row support
   result alongside the next eligible result for `fit_incremental_goal_results`.
3. Inspect cartridge-derived unmet evolution requirements. Append a prospective
   target only after historical profiles; do not overwrite S's declared Abra goal.
   One of the duplicate Harden-only cocoon lines could test a different recipient.
   Read-only cartridge decoding on September7 confirms Metapod national11/internal124
   evolves toButterfree12 atlevel10; Kakuna14/internal113 evolves toBeedrill15 atlevel10.
   The saved inventory has two of each. Choose a prospective curriculum target
   before play; do not claim that manually declaring this nested target is learned
   species selection. The native model still must choose the actual high-level task.
4. Before consuming the final box slot, qualify existing PC box-switch/deposit/
   withdrawal composition where required. Do not pretend unused total box capacity
   is immediately reachable, and do not catch another specimen into a dead end.
   Inspection found `RedBoxSwitchGoalProvider` and `switch_box` already preserve
   all boxes, but the regional router does not connect storage to reachable PCs.
   The duplicate cocoon candidates are in the current box; blindly switching to
   an empty box first would make current-box-only evolution unavailable. Prefer
   a bounded storage preparation attached to an actually selected capture, with
   capture-helper retrieval before box rotation, fresh source-preserving rebind,
   and all setup costs charged to that outcome. Alternatively qualify retrieval
   across boxes before exposing a standalone rotation. Do not label preparation
   as a second learned choice or silently replace the selected objective.
5. Execute one real model choice; verify species preservation, XP/goal completion,
   all earlier rows and the actual model/save chain. Then decide whether another
   useful bounded step exists. Resource recovery must use game mechanics, not resets.
6. Add compact quantum battle/heal/PP/XP summaries if the existing retention hook can
   carry them with a small change. Do not rerun S to generate prettier telemetry.
7. Audit/reorient and refresh the roadmap, saved-view dashboard, handoffs and narrative.

Longer-term work remains dynamic completion-task selection, renewable resources,
general storage access, varied evaluation, model-led Red story/living collection,
then a compatible unfamiliar Red modification and finally Crystal. A single successful
choice is evidence that this loop works, not that those stages are already solved.
