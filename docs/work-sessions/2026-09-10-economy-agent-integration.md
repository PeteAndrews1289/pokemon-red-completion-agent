# Delegated economy integration

Status: implementation/review complete; prospective extension remains unactivated.
User priority is conserving Codex weekly usage, not minimum latency.
Use isolated Flash implementation and one Claude review; Codex owns integration,
verification, publication and live-game authority.

## Mission check

1. Capability: use cash shortfall and measured income to plan sustainable collection.
2. Learned authority: an explicitly versioned, opt-in extension lets future native
   choices distinguish earning from purchasing. No historical re-labeling or claim
   that forced support teaches choosing an income method.
3. Transfer test: identity-free rich/poor and income/cost contrasts; old models,
   projections, outcomes and replay probabilities remain unchanged.
4. Cheapest falsifier: the same historical model/row stays byte-identical while a
   prospective earned-income choice has distinguishable features and actual targets.
5. Time box: one isolated implementation/revision cycle and targeted review; stop
   expanding infrastructure if it cannot connect to the next played earning lesson.
6. Stop: unresolved historical compatibility, fabricated outcome labels, permission
   failures outside the allowed tree, or a proposal requiring a full replay.

## Delegation boundary

Flash may edit the named economy/model integration source and regression tests in
its separate checkout. Accepted-edit mode grants scoped file work, not unrestricted
shell, private artifact access or gameplay. Claude reads public source only and
reviews the approach/patch. Neither may push, merge, fit actual models, read ROMs,
edit saves, change credentials or run a collection campaign. Codex verifies the
diff and tests before integrating. A branch alone is not a filesystem sandbox.

AM01/model65 remain the live continuation; no learning counters change for drafts.
The first implementation target is learning **whether** to earn. Choosing among
multiple qualified income methods remains unproved until real alternatives exist.

## Independent work and review

Antigravity CLI advertised and ran Gemini3.8 Flash High with sandbox/accepted edits
in the isolated tree. Its refreshed initial Gemini quota was100%five-hour and
79.12%weekly remaining, with the weekly reset displayed about22h11m away. These
are shared account windows, not measured task savings. No global permission bypass
or credential change was made.

Claude's subscribed CLI completed its public-base design review in154.952seconds,
using Opus4.8 High (provider metadata confirms the model). It had only Read/Glob/Grep
tools, no shell/write capability, and reported no permission denials. The inspected
checkout was still unchanged when its review completed; Flash's edits followed.
Claude's interactive quota display stopped at first-run login selection despite
working print-mode subscription authentication. Five-hour/weekly quota is unavailable;
API-equivalent cost/token telemetry is not substituted for subscription percentages.

Accepted: cash budgets need provenance in missing supplies and priced offers, not
an arbitrary reward target. Codex added a budget derived from coherent bag stock,
declared capture reserve, bounded purchase quantity, stack headroom and the existing
provider's price. Unsupported/full-bag cases do not fabricate a funding opportunity.
This does not prove the chosen shop is reachable or globally cheapest.

Rejected as written: Claude recommended modifying the older goal-manager matrix,
but the current native model uses the living-Dex option-value path. It also said a
linear model can infer a product interaction from the separate inputs; that is
incorrect without an explicit nonlinear feature or different model architecture.
Retain the tested shortfall-income interaction and version the active learner.

Codex's separate authenticated AM inspection advanced zero frames/actions and made
zero policy predictions. All four Elite Four defeated flags and the Champion flag
are clear in the current save. The existing party-preparation screen finds offensive
coverage for all four rosters; it favors Blastoise as opening lead for Lorelei,
Bruno and Lance, and Dugtrio for Agatha. This is not a survival, rematch-entry or
profit guarantee. The exact save/model remain untouched.

## Review corrections and remaining connection

Flash's first draft touched seven source/test files. Codex's first targeted run
had110passes and3failures: a non-idempotent model upgrade, an invented interruption
enum value, and the wrong expected decoder exception. Codex also found fabricated
zero-cash fallback observations, insufficiently explicit availability filtering,
and a self-comparison advertised as a frozen compatibility test. The unavailable
quote concern was already blocked by the upstream typed contract; the policy's
explicit available-quote check is defense in depth, not a newly discovered live
exploit. A bounded Flash correction pass repaired the main draft. Codex corrected
remaining fixtures/type errors and removed item identities from policy serialization.
Independent Codex regression fingerprints come
from the pre-change executable, not the draft under test.

The key semantic finding is that attaching an economy outcome does not make it a
learning target. The existing nine-output objective ignores that attachment. A
synthetic fit against hand-authored legacy targets demonstrates representational
capacity only, not income learning. Keep the prospective extension unactivated.

Next connected slice, in order:

1. Add explicit useful-liquidity gain and cash-loss learning outputs with declared
   utility weights. Retain registration, action/frame, party and inventory feedback;
   do not relabel cash as a registration or story unlock. Historical rows lacking
   economy observations must be masked for these new outputs, not assigned zero.
   Test changed measured income with otherwise identical labels, and unchanged
   historical predictions before any prospective fitting.
2. Record a fresh supply-derived budget and observed cash with each actual choice.
   Wire executor events, sampling replay and dataset reconstruction together. Forced
   support/singleton menus remain excluded; quotes never substitute for outcomes.
3. Continue the exact AM terminal through bounded Center departure/League access,
   retaining earned cash, items, actions, frames and party changes. Offensive
   matchup screening is not a solo, survival or net-profit guarantee. Stop safely
   on failed access or unsupported semantics; do not replay an old attempt.

This is one income-learning connection, not a new phase or a full-game teacher.
Flash can own the first implementation draft; Codex reviews the target semantics
and integration. The goal manager currently permits one option per goal kind, so
this slice supports deciding whether to earn, not choosing among multiple income
methods. That latter choice needs a qualified method-level menu. Claude is optional
for a narrow independent check, not a gate.

## Delegation results and usage

Both external jobs finished. Flash supplied the seven-file cross-module draft and
one correction pass; Codex retained its local draft at7d356006 and integrated the
reviewed version, adding real-budget and independent legacy/identity/bridge tests.
No permission bypass was necessary. Flash's response was not accepted as test
evidence: Codex ran the suites and corrected failures before publication.

The refreshed Antigravity display showed76.79%weekly and86.11%five-hour remaining
after the work, versus79.12% and100% before. These are account-wide quota readings,
not an exact task attribution or a measured Codex saving. Claude five-hour/weekly
remaining is unavailable through the working subscribed print CLI; its interactive
usage path required login setup. No API-cost estimate is substituted. Quota-only
interactive processes were closed; neither external assistant is left running.

Verified:258targeted regression tests, including the real registered-outcome bridge
with synthetic observations, legacy compatibility, policy identity, recovery and
history. Repository-wide Ruff and mypy across476source files pass. This is not a
full-suite or hosted-CI result. The12documentation-surface tests, documentation
links, product-focus check, public-artifact check and regenerated prospective
registry freshness check also pass before the single publication batch.
Actual game/model delta remains zero:65examples,64registrations,54specimens;
AM01 is stopped at Fuchsia Center. No income has been earned by this session.
