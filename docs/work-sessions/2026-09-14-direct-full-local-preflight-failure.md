# Direct full-local source consumed before preflight

## Result

The exact frozen catalog source was claimed once after its direct-origin bridge passed exact-head
CI. The first preparation attempt failed before opening the selected payload because the command
parser represented an omitted regional-transition list as `null`. One crash-recovery preparation
attempt reused the same claim and source; it authenticated and opened that source, then failed while
building the base training plan because Model121's feature version selected the economy behavior
before the economy schema and supply fields existed.

Both failures are retained in sealed private records. No action-free preflight completed. There was
no controller input, advanced emulator frame, model query, registration session, registration
observation, outcome, training example or fit. Read-only checks confirmed that the named
registration session and run do not exist. The one recovery reused the consumed claim during
preparation; it was not a gameplay retry. No further attempt or replacement is allowed.

## Mission check

- Reusable capability: future direct catalog origins accept an omitted regional-transition list,
  preserve legacy wild-source callers and let a feature-version-4 model enter the ordinary base
  training contract before any separately qualified economy promotion.
- Learned authority: unchanged. Model121 remains at 121 examples, 83 successes and 86 local
  registrations; no model choice occurred.
- Transfer test: ROM-free parser, legacy namespace and feature-version tests exercise the boundary
  independently of the consumed save. No cross-title transfer result is claimed.
- Cheapest falsifier: parser-created direct arguments must reach source authentication, a v4 base
  declaration must use optional-recovery behavior, and feature version 5 must still fail closed.
- Time box: one maintenance session, ending after focused/full verification and publication.
- Stop condition: do not reopen this source, create another claim or select a replacement. End after
  the generic repair is green; a new source requires a separately frozen future session.

This is maintenance, not learning. It unblocks the already named experiment: one future
prospectively frozen direct-origin source exposing two executable acquisition families for one
measured Model121 choice.

## Repair

The runner now treats a missing parser list as empty while retaining an explicitly supplied legacy
`wild_source`. Base training declaration maps only supported feature version 4 to the existing
optional-recovery behavior; versions above 4 still fail closed. Economy plans still require the
registered continuation schema, a v4 model and complete supply fields before they receive the
economy behavior ID.

Two new ROM-free runner tests cover the parser default and legacy fallback; the existing plan test
matrix now includes supported v4 and rejected v5. The focused plan/continuation suite passes 129
tests. Repository-wide Ruff and mypy pass across 499 source files. The hosted-equivalent ROM-free
suite passes 11,710 tests with one expected failure. The existing exact-Mac PyBoy metadata
fingerprint check is excluded locally; hosted Linux CI uses its standard platform path. Exact-head
CI remains the publication gate.

## External review

Gemini 3.8 Flash High completed a bounded tool-free review. Its unsupported-version finding and
legacy-fallback finding were accepted and fixed. Its claimed feature-version metadata skew was
rejected because the base training-plan document contains no feature-version field. The first
tool-assisted Flash attempt was permission-denied and produced no review.

Claude Opus4.8 High then completed a read-only evidence/code audit for $1.4518645 and returned PASS
with no P0/P1 finding. Its clarity notes led to the explicit same-claim-preparation and test-count
wording above. Headless output exposed no five-hour or weekly quota percentages/reset times for
either service. Reviewer access does not authorize another source or gameplay attempt.

## Next bounded action

Publish and require exact-head CI for the generic repairs. In a later session, freeze a distinct
unused source prospectively, claim it once under the new exact source, and stop action-free unless
it exposes two executable acquisition families. The full fresh-start Red final exam remains closed.

[Evidence](../evidence/red-direct-full-local-preflight-failure-2026-09-14.json) ·
[Previous bridge](2026-09-14-direct-catalog-origin-bridge.md)
