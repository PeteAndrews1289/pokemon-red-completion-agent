# Model121 full-local collector stops on ineligible provenance

## Outcome

The qualified full-local151 proposal is now connected to the existing durable Red player. The
opt-in path requires a local-Red registration session, offers the real routed capture and native
evolution candidates, shares one episode-scoped execution authority across fresh observation
gates, carries the mode through terminal checkpoints, and uses the existing write-ahead decision
record before a selected binding can receive controller authority. Historical modes remain off by
default.

The real Model121 departure did not proceed to gameplay. Its immutable checkpoint is partitioned
as train but explicitly records `training_eligible: false`, and its original state has zero matches
in the frozen training catalog. The registered collector correctly refused to create a new local
session without a catalog-authenticated training plan. No historical state was substituted, no
diagnostic was relabeled, and no consumed assignment was reopened.

## Mission check

- Capability: one model choice across executable capture and evolution families, retaining a
  fresh local151 terminal and actual costs.
- Learned authority: unchanged; Model121 remains the current bounded-development model.
- Transfer test: local/shared/physical separation and legacy checkpoint compatibility pass;
  unseen-lineage and cross-title transfer remain untested.
- Cheapest falsifier: the exact departure lacks eligible catalog-backed training provenance.
- Time box: one bounded session; the falsifier fired before gameplay.
- Stop: zero controller actions, frames, model queries, teacher labels, registration sessions,
  outcomes, fits, promotions, full replays, ROM-hack access or Crystal access.

## Verification

The main integration surface passed 194 focused tests. After hosted CI found one older false-mode
test double receiving new optional keywords, the compatibility repair and a prepare-time scope
guard passed the related 144-test surface. Ruff, configured mypy, and collection-registry regeneration and
checking pass. Hosted CI run `34871389806` documents the caught compatibility failure. Replacement
run `34873249583` passed the exact final implementation commit: 11,675 tests passed, 4 skipped and
1 expected failure, alongside documentation, artifact, registry, Ruff and mypy checks.

Claude Opus completed a read-only delta review with GO and no P0/P1 findings. Its prepare-time
scope-guard suggestion was accepted; its checkpoint-schema note is unreachable under the explicit
completion-dose requirement, and the collection metadata was regenerated rather than hand-edited.
Gemini3.8 Flash High's first repository review timed out, then a tool-free contract review returned
STOP. Its findings were rejected against the concrete implementation: durable episode identity and
the pre-execution trajectory record prevent cross-process replay; goal kind and typed outcomes keep
capture/evolution attribution separate; and catalog validation runs before registration-session
publication, as the verified zero-session stop demonstrates.

Model121 remains at **121 examples / 83 successes / 86 local registrations**, with 66 living
species, 70 specimens, four capture items and cash706. This session changes engineering capability,
not learned competence or the Red completion gate.

## Next bounded objective

Provision one prospectively eligible, catalog-backed train departure for the already qualified
full-local collector. It must expose at least two executable acquisition families without searching
or replaying consumed historical states. Then freeze one exploratory model choice, execute only its
binding, retain success or failure and the fresh terminal ledger, and fit only that measured result.
If such a departure cannot be provisioned without weakening provenance, stop and redesign the
training-supply contract; do not run Model121 deterministically or return to resupply/restoration.

Next-session recommendation: **GPT-6 Astra / High / Fast off**. The remaining problem is now
training-provenance and authority design, not ordinary bounded execution. Sol High remains suitable
after an eligible departure is frozen.

[Evidence](../evidence/red-model121-full-local-collector-stop-2026-09-14.json)
