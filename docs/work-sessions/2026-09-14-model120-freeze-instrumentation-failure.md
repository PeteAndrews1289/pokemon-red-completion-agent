# Model120 freeze stops after an unretained sample

## Outcome

The exact Model120 successor was authenticated against green CI and reconstructed action-free. It
still contained routed full-team restoration and trainer-funded resupply, with no fishing
supplement. The wrapper invoked Model120 once under seed `120091401`, then failed before preserving
the answer because it read a nonexistent choice attribute. The selected index and kind are unknown
and are not inferred.

This identity is consumed. There was no redraw, retry, gameplay, controller input, emulator frame,
teacher label or fit. Model120 remains at **120 settled examples / 82 successes**; the save remains
at **86 registrations / 66 living species / 70 specimens**, cash **346** and four capture items.

## Durable failure and repair

The action-free observation and a separate failure receipt retain the exact source, model,
checkpoint, state, menu and consumed seed. The receipt records one model-query attempt and no
recoverable choice. It authorizes neither fitting nor execution.

A repaired V2 freeze was prepared but deliberately not run. It uses a new identity and seed. Before
selection, it exclusively writes and fsyncs a query-intent tombstone; an interruption from that
point forward therefore consumes the attempt instead of permitting an accidental requery. A
successful selection is then written ahead of later wrapper validation with the public choice,
private binding reference and a content hash of the selected option. Recovery can rebuild the exact
menu from a receipt without another model query, or finalize directly when both the receipt and
action-free observation exist.

## External contribution

Claude Sonnet High was reachable directly through the reenabled CLI, returned
`CLAUDE_CLI_READY`, and completed a no-tools review. It agreed that the lost selection must not be
redrawn and identified two hardening needs: structurally gate the query before invocation and hash
the selected option rather than trust an index alone.

Gemini 3.8 Flash High, invoked through `agy`, independently approved the stop and identified the
same crash interval between query processing and receipt durability. Both findings were accepted in
the unexecuted V2 script. Neither model edited the repository or touched gameplay.

At closeout, Antigravity reported **100% five-hour / 65% weekly** Gemini quota remaining and
**100% five-hour / 100% weekly** for its separate Claude/GPT pool. Claude print mode worked, but
its interactive usage view requested a fresh login, so no direct Claude percentage is claimed.

The focused product/roadmap slice passed 152 tests. The complete local gate reached 11,592 passes,
three skips and one expected xfail. It reported a stale strategic-registry golden, which was updated
from the exact regenerated registry and passed its focused rerun, plus the known Mac-only PyBoy
metadata identity mismatch. That host-environment check had already been absent from the exact
starting-head hosted CI failure surface; all 11,590 hosted tests passed there.

## Product gate and next session

The complete-Red requirement is unchanged: one authenticated non-deterministic model-directed run
must finish the story and full local Red Pokédex before any ROM hack. Version, trade and event
dependencies remain explicit; Crystal and later games remain out of scope.

Next, start a new session and run the repaired V2 freeze once. If it retains a valid choice, execute
only that binding and fit only an eligible measured result. Stop on an ambiguous tombstone, any
identity mismatch, teacher fallback, redraw or retry. Estimate: one bounded session, 60–120 minutes.

Recommended model: **GPT-5.6 Sol, High effort, Fast off**. The next work is the established bounded
freeze/execute/fit loop. Use Astra High only if the task expands into fresh-run authority,
dependency-resolution architecture or a changed admission contract.

[Evidence](../evidence/red-model120-freeze-instrumentation-failure-2026-09-14.json) ·
[Prior measured session](2026-09-13-model120-frozen-field-restore.md)
