# Trainer transition repair from the actual retained endpoint

1. **Capability:** recognize ordinary trainer dialogue/battle transitions and full
   cartridge event offsets, allowing earned income to unblock living collection.
2. **Learned authority:** maintenance for model75 resource-backed collection;
   forced support earns no labels and does not promote battle authority.
3. **Transfer test:** independently varied pending/closed/wrong-identity transitions
   and event offsets on both sides of the byte boundary; not a cross-game claim.
4. **Cheapest falsifier:** inspect the exact retained funding01 endpoint without
   input, establish its actual transition state, then test that observation before
   changing controller behavior. A ready movement latch alone is insufficient.
5. **Time box:** one bounded session, at most two hours. Flash gives a small
   independent decoder/test analysis while Codex diagnoses the observed transition.
   No broad audit, catalog extension or new CI workflow.
6. **Stop:** ambiguous or unsupported transition, unsafe party, unexpected battle,
   missing durable terminal or no causal explanation. Preserve all prior failures;
   never replay funding01 or restore its older origin. Any later input must be a
   separately bounded continuation from actual checkpoint3fe01202/statef08d4b65.

The current session first performs read-only diagnosis and ROM-free repairs. Its
scope does not include sealed data, Crystal, a full replay or protected-item sales.
Codex alone integrates, publishes and admits live results. Flash works read-only
in its existing isolated tree; Claude is reserved for a consequential finding.

## Diagnosis and repair

A zero-input reload of state `f08d4b65` reports pending trainer `(201, 10)`,
current script 2, battle mode 0, ready movement and no bottom text frame. State
bytes and frame count remain unchanged. The cartridge's
[StartTrainerBattle](https://github.com/pret/pokered/blob/master/home/trainers.asm)
arms the trainer-start latch before battle mode changes. Thus the previous guard
confused an armed transition with settled overworld. It was not a failed trainer
selection, a defeated trainer, or insufficient level.

The observer now distinguishes this latch from stale class/set fields. The battle
adapter waits without buttons while the expected transition is pending; mismatches
and timeouts still stop. A retained pending continuation skips escort preparation,
travel, facing changes and a second interaction. Event decoding gains an explicit
full-offset mode while historical menus retain legacy semantics. Fresh target
verification uses the corrected mode, so an aliased old quote stops before input.
This does not claim that historical route hazards have all migrated.

Flash High returned a small compatibility/test proposal, conversation
`0ebd019a-978d-4410-aa8b-82e43d797671`. Accept explicit legacy/cartridge separation;
reject the invented D5AB event-region examples and incorrect range endpoints.
Codex supplied actual independently pinned D747/D886 boundary fixtures. No Flash
test execution is claimed. Latest service quota at14:34 UTC: 91.9998% five-hour,
93.1392% weekly remaining; account readings do not measure this job's isolated cost.
Claude was not called for this routine repair; its last quota remains unavailable.

The first zero-input native preflight exposed a second integration seam: talking
turns the NPC toward the player, putting the saved square inside its sight lane.
Normal approach planning correctly refuses that origin, but must not be invoked
to resume an already-armed adjacent battle. Candidate recovery now requires the
unique visible, undefeated, matching class/set immediately in the player's facing
direction and creates a zero-step continuation. Six additional regression cases
check this real candidate-builder path. No movement hazard is removed, no route
is replayed, and a preflight offering only healing is prohibited from execution.

The next read-only preflight correctly rejected retrospective menu changes: the
same state bytes now exposed a resumable funding option absent from the saved
terminal's semantic hash. `--trainer-pending-recovery` therefore explicitly
separates historical restore (old header defaults false) from new execution;
new headers retain the mode and reject rollback. The checkpoint comparison stays
intact. Both rejected preflights sent zero controller input and claimed no episode.

## Actual continuation and second diagnosis

Funding02 consumed 9 actions / 996 frames. Its first action was WAIT; it entered
the intended battle and stopped at the first MAIN menu, before any move or HP/PP
loss. Exact failure `1fdce0fc` and failed manifest `5a015dfa` are retained; this is
not an admitted safe checkpoint or a fit row. Both funding01 and02 stay consumed.

Read-only reload and cartridge WRAM definitions establish that the field
engagement pair aliases enemy Special/stat-modifier storage in battle: `(201,10)`
became `(19,7)`. Stable opponent/class/trainer-number reads remain `(201,1,10)`.
The prior assertion that all four fields remain stable was wrong. Claude's older
specific explanation was inaccurate, but dismissing the entire lifetime concern
was too strong; this measured result supersedes that adjudication.

The battle guard now uses stable combat identity. An explicit active-MAIN-menu
recovery option reuses the existing failed-state support recorder, not a new
native model choice. It may finish only the unique adjacent cartridge trainer,
verify exact payout, preserve the collection and publish a zero-label recovery
checkpoint. No replay of either consumed funding attempt, party preparation,
healing detour, protected-item use or new model fitting is part of this recovery.
