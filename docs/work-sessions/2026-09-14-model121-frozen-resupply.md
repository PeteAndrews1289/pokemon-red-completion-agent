# Model121 learns a verified finite-income resupply

## Outcome

The repaired Model120 V2 freeze ran once under seed `120091402`. It wrote a durable query-intent
tombstone before sampling and a decision receipt before wrapper validation. Model120 selected the
trainer-funded resupply candidate at probability `0.4072490891` from an exact two-option menu of
routed restoration and resupply. The retained menu, model, state, option content and private binding
all matched before controller input.

The selected binding then executed exactly once. It defeated the quoted three-level-24 trainer in
**153 actions / 11,461 frames**. Cash increased from **346 to 706**, exactly matching the ordinary
victory payout of **360** with zero Pay Day money. Inventory, four capture items and all **86**
registrations were unchanged. There were no execution-time model queries, teacher labels or retries.

## Learning and publication

The first fit preflight stopped without publishing because the new write-ahead declaration schema
was not yet recognized by the learner. The original receipt was not rewritten. A narrow public
admission change requires the exact older frozen-resupply fields plus all three lowercase SHA-256
receipt identities. It preserves the resupply-only binding, qualified-CI, zero-query and frozen
behavior-replay rules. Focused tests passed **110** cases; the related learning surface passed
**158** cases. The corrected source `ee8f402f` passed hosted CI run `34843054840` in full.

The zero-input preflight then independently reconstructed both observations and verified the
measured 360 cash gain, unchanged inventory and unchanged Pokédex. One training-only row was
admitted at importance weight `2.4554996604`. Model121 now has **121 settled examples / 83
successes / 42 economy-qualified rows / 17 measured-source rows**. Weighted in-sample loss changed
from `0.0308927525` to `0.0118511614`; this is not independent evaluation.

Model121 was published with bounded-development authority only. Its terminal checkpoint reopened
with **86 registrations / 66 living species / 70 specimens**, cash **706**, and no publication
actions or frames. There was no promotion, sealed evaluation, full replay, ROM hack or Crystal use.

## External contribution

Claude Sonnet and Gemini 3.8 Flash High through `agy` each passed the exact-once execution contract.
Both also passed the new fail-closed learning admission: exact key set, three receipt hashes,
qualified source, frozen behavior replay and no redraw. Neither model touched gameplay or private
state.

## Product gate and next session

The complete-Red requirement is unchanged. Before any ROM hack, one authenticated fresh-start,
non-deterministic model-directed Red run must finish the story and full local Red Pokédex. Version,
trade and event dependencies remain explicit; the old reduced native-availability count is not the
gate. Crystal and later titles remain out of scope until Red passes.

Next, inspect Model121's new terminal action-free and freeze its exact menu once. If a decision is
durably retained, execute only that binding and fit only an eligible measured Red result. Stop on
any identity mismatch, controller activity during inspection/freeze, redraw, retry or teacher
fallback. Estimate: one bounded session, 60–120 minutes.

Recommended model while capacity notices persist: **GPT-5.6 Terra, High effort, Fast off**. Use
**GPT-5.6 Sol, High** when available for the same bounded loop. Reserve Astra High for fresh-run
authority, dependency-resolution architecture or another admission redesign.

[Evidence](../evidence/red-model121-frozen-resupply-2026-09-14.json) ·
[Prior failure session](2026-09-14-model120-freeze-instrumentation-failure.md)
