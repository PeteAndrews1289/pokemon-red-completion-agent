# Model116: explain Pay Day, retain another failure

## Outcome

The hidden58 from Model114 is now explained from the exact pinned Red source. Route13's
level29 opponent Meowth knew Pay Day. Red adds twice the move user's level to
`wTotalPayDayMoney`, regardless of which side used it, then credits that accumulator after
victory. The ordinary trainer quote was2030 and Pay Day added58, exactly explaining the
observed2088 increase from58 to2146.

Model114's outcome remains a failure. This diagnosis does not relabel history, loosen its frozen
quote or fabricate success after seeing the result.

## Exact Model115 execution

The exact frozen Model115 resupply choice ran once. It used **27 actions / 864 frames** to reach
the selected trainer boundary, then stopped before interaction because the new verifier initially
required a zero Pay Day accumulator. The retained Model114 value58 was still present. Cash stayed
2146; registrations stayed84, living species64 and specimens68. The attempt is consumed and was
not retried.

Pinned cartridge source resolved the lifetime rule: `EndOfBattle` credits but does not clear the
accumulator; `InitBattleVariables` clears the whole miscellaneous battle-data block when the next
battle starts. The final contract therefore permits a stale pre-battle value, requires exact zero
after the selected new battle becomes active, and verifies terminal cash as initial cash plus the
ordinary trainer quote plus the new battle's persistent Pay Day accumulator. Direct player Pay Day
remains outside the ordinary trainer-funding policy.

## Learning and continuation

The actual failed Model115 outcome became row116. Model116 has **116 settled examples / 78
successes** and preserves all115 earlier rows. It received zero success, completion, dependency or
useful-liquidity credit. There were no teacher labels, retries, execution-time policy queries,
independent evaluation or authority promotions.

The terminal was published as an action-free restart. Model116 then inspected a six-choice menu
across three goal families and selected candidate2, an acquisition, with probability
0.0856079343148439. The choice remains frozen and unexecuted. It is the next bounded gameplay
objective; this session does not redraw or execute it.

## External contribution

Flash3.8 High completed a read-only review and correctly pressed on ambiguous terminal sampling,
which motivated adversarial tests. Its assumption that Pay Day clears at battle end was corrected
against pinned cartridge source; the useful race concern was retained in the battle-init proof.
The refreshed shared Gemini display showed96.69% five-hour and69.30% weekly remaining; these are
account-group counters, not a measured Astra saving.

Three bounded Claude CLI review attempts returned no review before being interrupted. Claude made
no contribution this session, and its subscription quota is unavailable. External review did not
replace Codex's source analysis or tests.

## Verification boundary

The final accounting and execution implementation passed393 focused ROM-free tests. The new
declaration admission passed252 targeted tests. Ruff passed, and mypy passed across495 source
files. An intermediate source revision passed11,560 tests with one expected failure; source changed
after that run, so it is not claimed as a final-HEAD broad-suite result. Hosted CI run34770365977
is the final authority and was still pending when this report was drafted.

## Product gate

The Red-first requirement is unchanged and unmet: one authenticated non-deterministic,
model-directed fresh start-to-finish Red run must show Champion/Hall of Fame and the full local Red
Pokédex before any ROM hack. The existing40-entry remainder is only the124-entry native scope;
version, trade and event dependencies remain explicit. No ROM hack or Crystal was opened.

## Next session

Execute Model116's exact frozen acquisition once, retain its actual result, fit only eligible
measured evidence, publish the terminal and rebuild the next menu. Estimate: one bounded session,
60–120minutes. Stop on resampling, a consumed Model115 retry, a parent/source mismatch, teacher
labels or an unverifiable collection transition.

Recommended model: **GPT-5.6 Sol, High effort, Fast off**. This is a bounded execution and
integration task. Reserve Astra for fresh-run authority design, unfamiliar-state generalization or
a cross-system failure that survives focused diagnosis. This recommendation does not change the
currently selected model.

[Evidence](../evidence/red-model116-pay-day-accounting-2026-09-13.json) ·
[Roadmap decision](../roadmap-decisions.md)
