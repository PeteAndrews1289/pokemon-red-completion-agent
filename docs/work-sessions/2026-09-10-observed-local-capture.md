# Capture from the room actually reached

BatchJ retained one failed learned source choice and fitted it once: model27,
51 registrations and46 specimens. Actual214 actions /14568 frames moved from
Vermilion Center into Mansion1F;18 capture balls and3503 money remain. The indoor
departure and flight worked. The subsequent fixed capture lane was unreachable.

A zero-input saved-state comparison found three current block changes closing
doors. Cartridge-initial geometry offers17 steps to the old lane; actual geometry
rejects it. The current accessible component contains395 land cells and an
alternative reversible encounter pair with a43-step approach. This is an available
candidate, not a performed walk or capture.

## Mission check

- Capability: find a reachable encounter patch within the model-selected current
  area, using actual terrain rather than its original cartridge layout.
- Learned authority: the actor still chooses source/goal; local path selection is
  deterministic support, not another label or a new learned policy.
- Transfer test: changed doors, disconnected encounter patches, occupancy,
  directional edges and different maps; no cross-title result is claimed.
- Cheapest falsifier: the blocked canonical pair is excluded while a genuinely
  reachable reversible pair qualifies with zero controller input.
- Time box: one hour, then reassess practical collection throughput.
- Stop condition: changing the selected source, mutating the declared historical
  profile, inventing reachability, unbounded search, or replaying consumed attempts.

Flash drafts only an isolated helper and tests; Codex owns the prospective profile
flag, router integration and verification. Remote maps retain their existing
qualification; this change does not solve arbitrary door puzzles or predict
unobserved layouts. Dynamic execution profiles are local to the skill; the
declared parent profile remains stable and is reconstructed from saved state.

## Authenticated batch closeout

## Integration review

The prospective `observed-local-capture` transition is integrated. Historical
profiles keep their prior meaning; capture retargeting preserves the new flag.
The helper uses actual blocks, land reachability, occupancy, object/warp exclusions
and reversible encounter edges. An unavailable actual patch cannot fall back to
the stale static patch. It rejects changed observations/blocks before input and
delegates the existing execution/verification and measured costs without adding labels.

Codex replaced Flash's defective test draft and added active-state/source guards.
311 relevant tests passed, including real-router replacement and ordered-profile
regressions. Type checking passed for467 source files; local docs/focus checks
passed. These are engineering results, not a new capture or training outcome.
Flash's latest displayed remaining quotas were73.44% five-hour and81.79% weekly;
Claude was not used. The five-hour display was at its reset boundary.

Next: authenticate the unchanged batchJ endpoint, qualify the new local binding
with zero input, then launch a fresh bounded continuation from model27. Do not
retry J or claim current-room support solves remote doors or general puzzles.

One retained failed source choice /one fit: model27,51 registrations,46 specimens. Indoor departure and Fly reached Mansion1F, but closed doors blocked the old capture lane. 214 actions /14568 frames;18 balls and3503 money retained. No indoor capture yet.

Next: Qualify a reachable encounter patch from actual current-map blocks within the same selected area, then continue model27 from its retained Mansion endpoint. No source substitution, consumed retry or resource reset. Keep stone evolution parked until useful collection resumes.
