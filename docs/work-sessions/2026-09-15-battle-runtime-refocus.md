# Battle runtime refocus — September 15

User-directed maintenance session: audit the repeated failure/repair loop and qualify
the battle execution contract before further collection gameplay.

Status: ROM-free audit/hardening complete; disposable cartridge qualification remains open.

## Mission check and scope

- Capability: execute a chosen battle turn with explicit command, PP and settlement
  evidence; preserve useful diagnostics when a bounded transition fails.
- Learned authority: unchanged. This maintenance unblocks the named Model121
  heterogeneous collection lesson; executor tests are not training examples.
- Transfer test: varied ROM-free semantic battle sequences, including delayed effects,
  no-effect turns, status suppression, stale menus and resource-accounting violations.
  These test executor generality, not learned transfer or cartridge reliability.
- Cheapest falsifier: one spent selected PP accompanied by another PP change must
  fail even when an HP change or battle exit is simultaneously observable.
- Time box: one session, two hours; prioritize a testable contract and diagnostics.
- Stop condition: stop expansion if it requires a new observation-adapter fact or
  unexplained weakening of an existing safety check. Record a bounded integration
  gate before any future disposable emulator campaign.

The prior evolution and recovery identities stay consumed. Recovery succeeded;
the retained save is stable with 86/151 registrations and Model121 at 121 examples /
83 successes. The original exception message and traceback were not retained, so
its exact cause remains unknown. Pending effects are a hypothesis, not a diagnosis.

## Working plan

1. Audit turn admission, PP proof, effect settlement and diagnostic retention.
2. Obtain bounded read-only Flash/Claude criticism where available.
3. Implement the smallest shared contract justified by discriminating tests.
4. Run focused regression and failure-injection checks; specify the cartridge soak
   gate separately from synthetic coverage.
5. Refresh the active roadmap and handoffs with measured results and remaining gaps.

Gameplay is paused for this session. No model query, fit or promotion is part of
the runtime qualification.

## Verified result

Architecture decision: retain the existing semantic executor and centralize its selected-turn
resource contract. Command admission, resource proof, effect observation and caller settlement
are distinct responsibilities. Explicit diagnostic phases make failures at those boundaries
visible. A full replacement of the battle state machine is not justified by the lost historical
exception; the disposable campaign will test whether deeper settlement changes are needed.

Nine adversarial cases ran against the original code: seven reproduced false success,
two were already rejected. The repaired shared contract rejects all nine. It verifies
the original battler's complete PP vector before accepting any effect, battle exit or
no-effect menu return, and a forced switch cannot stand in for move learning.
The final observation after the last allowed pulse is checked without adding an input.

The 108-case matrix spans four slots, three delays, three menu phases and three outcomes
(damage, no observable effect, exit). It rejects extra confirmations on stale MAIN/MOVE
menus. The affected battle, training, evolution, trajectory, schedule, route and recovery
suite passes 604 tests, one skipped. Ruff and mypy pass. This is ROM-free qualification.

The first full local suite stopped after2212 passes on
`test_exact_local_mac_runtime_identity_qualification`. Baseline bf241d19 reproduces the
same preflight metadata-hash rejection against the same installed dependency directory.
The test and authentication checks remain unchanged. A second broad run explicitly excludes
that one existing local-environment failure; this must not be reported as an unqualified
full-suite pass. Hosted Linux CI exercises the unchanged normal suite.

The broader run also detected the expected whole-module battle fingerprint change in
the venue-prior golden test. Its explicit expected digest was updated after reviewing the
contract changes; historical receipts, compatibility rules and timing limits were not changed.

Final broad local result: **11875 passed,1 xfailed,1 warning in1076.63 seconds**,
with integration tests outside scope and the one baseline-reproduced Mac test explicitly
excluded. Public-artifact checks, documentation/product focus, regenerated collection registry,
Ruff, configured mypy (501 source files) and whitespace checks also passed. The generated
roadmap and its changed footer were visually inspected. Hosted CI is a separate verification.

The diagnostic scope records explicit runtime phases and keeps the latest 128 events,
the latched selection and original PP, and a bounded exception chain with trusted package
function names/line numbers. It observes existing reads only. Attempted and completed
actions are distinct, and no message text, paths, frame locals, ROMs or saves enter it.

An episode writer can bind `bind_battle_runtime_failure_sink` to persist the record before
an outer handler discards the exception. Ordinary recorder/writer errors remain counted
and do not replace actor errors. Process interrupts propagate with the actor error as
context. A prospective runner must test its actual durable writer; the consumed private
evolution runner was not changed or re-executed.

## Review adjudication

Claude Opus 4.8 High completed two read-only reviews through the standalone CLI.
The first accepted PP-vector criticism was independently reproduced. Its proposed slot0
example was corrected to this API's one-based slots. The speculative identity-remapping
claim was rejected: the actual helper uses party index, not move-identity matching.
The second review found no new PP-proof hole and identified two diagnostic issues:
external traceback function names are now excluded, and process-interrupt behavior is
explicitly documented and tested. The small post-review changes were verified locally,
not submitted to a third review. No reviewer operated a ROM or edited the worktree.

Flash 3.8 High was reached through `agy`. Its first no-tools response disclosed missing
stdin and offered inapplicable engine/late-generation advice; none was accepted. A second
request embedded the actual source, but timed out after90 seconds without a verdict.
Do not represent either response as a completed source audit or a measured usage saving.

Service quota refreshed September15 around13:00UTC: standalone Claude reports32% used
in the current session (68% remaining), reset11:00 America/New_York; weekly73% used
(27% remaining), reset September18 at10:00 America/New_York. Gemini reports99% five-hour
remaining, reset14:33:11UTC September15, and58% weekly remaining, reset23:22:45UTC
September18. These are shared account readings, not isolated task costs or savings.

## Remaining limitations

- The historical exception's exact cause cannot be reconstructed from its error type.
- Legacy same-battler move replacement still infers execution after the original PP was
  overwritten. It is not exact proof and must not support a new authority claim.
- Off-slot move learning can legitimately change another PP slot; the current strict
  vector guard rejects that change. Qualify this transition before using the contract
  for extended training, and do not misreport such a stop as extra move execution.
- An observed effect does not prove all turn dialogue/animations have settled. The narrow
  turn primitive intentionally returns at effect; full-battle boundary behavior still
  needs cartridge qualification.
- A trace is a bounded diagnostic tail, not a full gameplay recording. Binding a writer
  is required for persistence; attaching evidence to an exception alone is insufficient.
- No cartridge soak, model query, training example, fit, promotion or registration occurred.

## Next session: bounded disposable cartridge qualification

Use Sol / High / Fast off, one session / at most two hours. This maintenance serves the
named Model121 heterogeneous collection lesson. Begin with a synthetic forced failure
through the actual prospective runner and verify its diagnostic can be reopened.

Authenticate the already-named stable recovered terminal and make an isolated disposable
copy with explicit test provenance. Never overwrite the retained source or replay either
consumed evolution/recovery identity. Do not search protected roots or use source four.
The test copy is correlated development material; it supplies no learning/evaluation credit.

Prospectively declare at most12 short battle cases, with at most2000 controller actions
and200000 frames per case, at most12000 actions/2000000 frames overall. These are caps,
not targets. Use only legal cartridge inputs, fixed existing slot policies, no model calls,
no RAM edits, and no long evolution grind. Exercise varied timing and available move/status
conditions; unexercised mechanics stay visibly uncovered. Retain every case and its actual
cost. Repeatability of disposable development tests does not reopen a consumed experiment.
Include move-learning and forced-switch boundaries explicitly in coverage. An unavailable
case is a remaining qualification gap, not permission to declare extended training reliable.

Pass only if every executed case stays within bounds, attributes resource use correctly,
reaches its declared settled boundary or honest expected failure, and reopens its diagnostic
on failure. Unexpected extra PP, an unsupported transition, missing durable evidence or a
hard budget stops the campaign. Retain that first failure and reassess the shared contract;
do not chain live patches and retries or claim success from a partial campaign.

After qualification, the next product task is the heterogeneous collection lesson. The
current restoration/resupply menu alone does not satisfy its mixed-acquisition gate. Do not
spend another session fitting resource-only rows or infer independence from timing variants.

[Qualification evidence](../evidence/red-battle-runtime-refocus-2026-09-15.json) ·
[Roadmap](../model-first-roadmap.md)

Next-session recommendation: **Sol / High / Fast off**. The contract and falsifiers are
specified; the next work is bounded integration and measurement. Revisit Astra if actual
evidence requires a new observation or execution contract.
