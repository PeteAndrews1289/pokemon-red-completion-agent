# Model112 dialogue recovery and automatic menu

## Outcome

The exact recovery-required model112 checkpoint regained input-ready field control through the
generic scripted-dialogue handler in **8 controller actions / 576 emulator frames**. Four bounded
cancel pulses closed the interruption. Registrations remained **83**, with **63 living species**
and **67 specimens**. The failed fishing acquisition was not retried, the model was not queried
during recovery, and the support step added no training example.

The recovered state was published as checkpoint
`793d9e9dc9c5b38d0b21de48c31285aed32ebeaacc19d435801d94cf5e77800a`, then reopened and inspected
without input. Its automatic mixed menu contains one restore goal and four cartridge-derived,
reachable fishing destinations. Public policy features contain no map, coordinate or species
identity. Model112 selected restore from the five choices by exploration with probability
`0.599148676088116`. That selection is frozen but has not been executed.

## Audit and qualification

Gemini 3.8 Flash High independently read the exact repository and private recovery scripts,
recomputed the source bundle and returned **GO after exact-head CI**. It confirmed the no-retry,
zero-label, single-policy-query, model/checkpoint and collection-preservation boundaries. It warned
that private module rebinding was brittle. The first publication assembly did encounter that alias
collision and stopped before writing; the parent was then resolved before rebinding and the
zero-input publication completed. A later menu assembly omitted one checkpoint lineage link and
failed closed before emulator input; adding the exact parent restored the authenticated chain.

GitHub CI run `34743224160` passed source
`5dcdce45737538e2ce4fdabceab4574949614896` and bundle
`de8c1320250030c842c884fb89f58fe26504cc9038cbe85e18b6aa8bbb49899e` in 30m37s. The recovery and
menu were both bound to that exact qualification.

## Mission check

| Question | Answer |
| --- | --- |
| Reusable capability | Recover an unexpected dialogue without fabricating a label, publish the true terminal and resume automatic goal discovery from it. |
| Learned authority | Model112 made the next five-way strategic selection; deterministic code still owns dialogue, navigation, healing, fishing and capture mechanics. |
| Product relevance | Long autonomous runs must survive ordinary interruptions and continue choosing from the actual save instead of resetting to a walkthrough checkpoint. |
| Limitation | This is one same-lineage Red recovery and one unexecuted choice, not evidence of fresh-game reliability, full Pokédex completion or transfer. |
| Next falsifier | The exact selected restore cannot execute and verify safely, or the resulting model113 state cannot expose another useful automatic choice. |
| Stop condition | No replay of the consumed acquisition, no second query for this menu, no teacher label, no invented outcome and no Blue/Crystal execution. |

## Next session

Bind the exact menu, selection, model, checkpoint and source once; execute the selected restore
within the existing bounded router; retain success or failure; fit only that observed outcome; then
publish and rebuild the next automatic menu without input.

[Evidence](../evidence/red-model112-dialogue-recovery-and-menu-2026-09-13.json)
