# Faster learning loop and a verified Horsea capture

The session removed two duplicated pre-input passes from the bounded Red learning loop, then ran three actual model-facing decisions. All three succeeded. The registered-objective model advanced from87 to90 fitted examples and the shared collection advanced from68 to69 registrations.

[Path-free evidence](../evidence/red-bq-horsea-learning-2026-09-11.json).

## What changed

Each regional learning step had been authenticating and preparing the same continuation more than once, then rebuilding the same regional candidate inventory in both the cycle and its child runner. The execution path now prepares each episode once and passes that authenticated readiness and immutable inventory into the child. Validation boundaries remain intact; later steps still rebuild live feasibility from their newly retained checkpoint.

An input-free measurement over226 continuation ancestors separated the remaining cost: authentication took39.135seconds, while the fresh regional inventory took252.24seconds. The repairs remove one complete duplicate preparation/inventory pass per step. The remaining51-source route scan is now the clear performance target.

## What ran

- BQ01 gave the model a real resource decision. It selected earning, defeated an available trainer and increased cash443→1,143. One settled example was fitted.
- BQ02 again exposed real alternatives. The model selected a one-ball purchase, changing cash1,143→543 and capture stock3→4. One settled example was fitted.
- BQ03 exposed six cartridge-derived regional sources. The highest model score belonged to SeafoamB4F, while the declared exploration policy sampled SeafoamB1F. That source choice was committed before input. Deterministic mechanics then traveled with Surf, used verified paralysis support and registered Horsea with one ball. Capture stock ended at3. The regional choice and observed success were fitted.

The three episodes used1,018 controller actions and60,805 emulator frames over1,143.875seconds. The terminal save is input-ready at SeafoamB1F with543money, three capture items,69 registrations and a full-HP party.

## What this demonstrates

The model made high-level economic and destination decisions and updated from their real outcomes. It did not receive a copied teacher label or a hidden retry. Horsea was verified from the retained terminal state, and every action/frame/model/checkpoint digest in the public receipt was checked independently without replaying gameplay.

This is not a fresh-game autonomous player. Navigation, menus, battle support and capture execution remain deterministic skills. It is not an independent evaluation, a Crystal result, a ROM-hack result or proof of arbitrary-seed reliability.

## Next decision

Continue only from BQ03/model90. Reduce the fresh regional-inventory cost by precomputing or caching cartridge-static source work while preserving live blocker, party, inventory and route feasibility checks. Require identical candidate menus and hashes before using the faster path in another bounded collection episode. Do not replay BQ or count the mechanics layer as learned authority.
