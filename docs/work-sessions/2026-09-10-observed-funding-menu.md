# Observed funding eligibility

## Mission check

- Capability: expose resource-recovery choices that respect currently observed terrain.
- Learned authority: the learner still chooses goals; remove mechanically unreachable offers rather than force a destination. Maintenance unblocking the model 38 continuation.
- Transfer test: changed-terrain menus and old/new checkpoint mode combinations; no cross-title result implied.
- Cheapest falsifier: an old static funding offer remains reproducible during restore, but is absent from a new observed-mode menu when its route is closed.
- Time box: 45–90 minutes for versioned integration, focused tests and a bounded useful outcome if available.
- Stop: checkpoint mismatch, lost provenance, no executable alternative or uncertain terminal. No replay of consumed attempts, refunds or invented fits.

The explicit menu mode is recorded separately from historical modes. Execution
continues to recheck the selected trainer against current terrain before input.
No new gameplay or learning is claimed until its actual result is recorded here.

## Implementation verification

`--observed-trainer-funding` is explicit, requires trainer funding and is retained
in new episode metadata. Restore uses the parent's recorded mode (absent means false),
not the successor's setting; a successor cannot roll the mode back. Recovery and
recorded-support paths preserve it. The generic historical forward-goal flag set
and its hashes are unchanged.

New menus build current-map geometry before enumerating funding. They validate map,
position, readiness and stable observations. Other goals are not force-selected.
Execution still independently requalifies the same trainer before input.

297 focused menu, script, recovery, support and forward-goal tests passed. All 468
source files passed the type check; lint, registry and documentation checks passed.
The actual model 38 checkpoint authenticated with historical mode false and new
mode true. Its new menu retained capture and removed unreachable funding. No input,
frame advance, predictions or fits were used for that compatibility check.

## Actual bounded result

Observed funding menus authenticated against the actual prior checkpoint. 1/2 new steps succeeded; 2 fits, model 40, 54 registrations and 48 specimens. Batch stop: step_limit. Failed costs remain recorded; singleton mechanics are not learned goal choices.

Next: Continue a fresh four-choice collection/resource batch from the input-ready model 40 Rock Tunnel checkpoint. Expose legitimate resupply, capture and evolution alternatives as actually available; preserve search costs and shared registrations. No consumed retry, reset, forced destination, sealed evaluation or Crystal.

[Path-free receipt](../evidence/red-observed-menu-learning-2026-09-10.json). Gameplay stopped at the recorded terminal. No learned combat, independent reliability or transfer claim.

## Outcome audit and reorientation

- R01: the model chose Mt. Moon B1F among eight destinations. The sole capture goal
  was a forced singleton, but the destination decision was genuine. Geodude (074)
  was caught: 53 to 54 registrations and 47 to 48 specimens. One destination fit
  advanced model 38 to 39. Cost: 763 actions and 50,521 frames.
- R02: model 39 chose acquisition among two goals and searched Rock Tunnel B1F.
  Five encounters yielded no new capture; the declared survey limit ended the
  attempt. This negative goal outcome was retained and fitted, producing model 40.
  The source proposal itself was not a fitted destination choice. Cost: 786 actions
  and 52,706 frames. No routing crash or specimen loss occurred.
- The two-step batch stopped at its declared step limit after 439.835 seconds,
  excluding its preceding read-only preparation. Total: 1,549 actions and 103,227
  frames, two fits and one new registration. One success out of two is a tiny
  same-lineage development sample, not a reliability estimate.

The final recorded trajectory snapshot was still a battle observation. It is not
the later saved terminal. A zero-input, byte-preserving read of the exact checkpoint
confirmed field state, no pending trainer, and input-ready controls. The closeout's
saved observation comes from that checkpoint, not the older battle snapshot. No
neutral settling, healing trip or reset was necessary. Resources: three balls and
1,073 money. All six party members remain alive and free of status conditions.

Next session: four fresh useful choices from model 40, with the same bounds and
observed menu mode; let the model consider legitimate resources, capture and
evolution. Budget 30–60 minutes for preparation, the small batch and its audit,
conditional on the choices' duration. Prefer this data-producing continuation to
adding another architecture layer. No independent learned advantage is claimed.

Flash and Claude were not used; there is no new external-agent quota reading.
Recommended next session: Astra, High, Fast off. This is execution and outcome
analysis, with higher effort reserved for a concrete unresolved failure.

## Hosted CI follow-up

CI run 34477318504 stopped on a continuation-test mock that lacked the new observer
keyword. The actual runtime restoration had passed, but the local targeted set had
omitted this test file. The mock now accepts and asserts the historical false mode
even when the successor requests true. Added a separate parent-mode/rollback test.
The expanded continuation/menu/recovery/support/forward set passed **372 tests**;
the 112 dashboard/roadmap tests also passed. No runtime behavior or learner result
was changed by this test repair. Hosted validation of the closeout remains pending;
the unchanged failing run was not manually retried.
