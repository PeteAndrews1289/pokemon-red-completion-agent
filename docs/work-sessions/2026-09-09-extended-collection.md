# Longer automatic collection training

## Working mission check

- Capability: sustain useful collection and legitimate supply/recovery over a longer automatic run, preserving living specimens and resource costs.
- Learned authority: actual native goal or regional destination choices, with eligible outcomes fitted and the next model loaded after each step. Deterministic mechanics remain disclosed.
- Transfer test: changed step/time budgets in ROM-free tests; live choices encounter changing inventory, party resources and search history. This remains one correlated Red lineage, not independent or cross-title evidence.
- Cheapest falsifier: continue native95's actual endpoint for at most16 steps, with a60-minute between-step deadline; stop early at the first unexpected failure. Measure captures, evolutions, new living species, supply costs and elapsed time.
- Time box: two hours for bounded integration, execution and closeout; expected gameplay/fit batch30–45 minutes if all16 steps remain feasible.
- Stop condition: no retries of consumed trials. Stop on unverifiable outcomes, unexpected execution failure, specimen loss, fitting failure, unsupported goal menus, the step limit or the elapsed-time boundary. Preserve an in-flight bounded episode rather than interrupt it just to meet a clock limit.

## Plan

1. Extend the existing runner's maximum declared step budget, add a monotonic between-step time boundary and explicit per-step progress. Keep historical defaults and existing guards.
2. Qualify the bounds and unchanged continuation semantics, then publish one executable change.
3. Run a newly declared continuation from native95 and its authentic Route1 endpoint. Do not edit the executable during the run. Inspect whether evolution options are genuinely available; do not count useful duplicate stock as completed living species.
4. Record results and remaining blockers in the dashboard, roadmap, handoffs and narratives. No new framework, full replay or sealed evaluation.

## Implementation qualification

119 focused cycle/goal/source/continuation tests passed, plus lint. The extended
16-step test verifies every intervening model and checkpoint is retained. Deadline
tests expire during initial inventory, source inspection and fitting: no subsequent
controller attempt begins, while an already completed outcome and fit remain saved.
Invalid or missing extended-run time bounds reject before preparation. Existing
defaults remain two steps without a new time gate. This is not a full-suite result.

## Completed live measurement

16 automatic steps, 15 model updates: 95 to 110 examples; 4,875 actions and 304,644 frames. 7 useful specimens, 3 new living species; 40 specimens and 32 living species retained. 0 unexpected runtime failures.

Actual native goals and regional destinations fitted from 95 to 110 examples, retaining prior rows. Forced support receives no invented training credit. Native110 has not yet played; no independent advantage or learned combat.

[Public evidence](../evidence/red-extended-collection-learning-2026-09-09.json). Final checkpoint: `02711287344cd37f18f420fbd6e41fded202e5167b4c208e30694bcb410def41`. Private resume prefix: `extended-collection-20260909-c`. Never rerun the consumed batch.

Elapsed: 36.7 minutes; stop: `step_limit`. Seven capture, seven recovery and two resupply steps. Step 13 used deterministic safety recovery, with zero model decisions and no admitted row; the other 15 steps produced actual sampled-choice examples. Capture-item counts: 15 to 11, with observed per-episode increases of 20 and decreases of 24. Money: 24,913 to 20,913. These are endpoint resource differences, not an inferred catch-efficiency rate.

## Closeout audit

- Every intermediate model was used by the next step, prior rows remained intact, and the final model contains exactly 95 + 15 = 110 examples. Native110 is saved but has not played.
- Observed ball and money balances match across every episode boundary. All completed steps succeeded; the specimen ledger records zero undeclared losses.
- Nine of 16 steps were support, with zero evolutions. New living-species gain was three; the remaining four catches preserve useful extra stock. This supports broader access work, not a claim of efficient or complete Pokédex autonomy.
- The executable passed GitHub CI run 34386559299. Local qualification: 119 cycle/goal/source/continuation tests and 18 dashboard/roadmap tests, plus lint, public-artifact, documentation and product-focus checks. The full suite was not run locally.
- No external-agent review was requested this session. The existing mission and stage exits remain unchanged; model fitting alone does not establish independent policy advantage.

A zero-action, zero-frame diagnostic of the starting checkpoint found two Drowzee and a missing Hypno: stock and the evolution mechanic were ready, but the walking-only planner could not compose a route to either supported Center. This is a supported routing limitation, not proof that the game itself has no path. The diagnostic left the saved state unchanged.

## Reorientation

The starting collection had executable Drowzee evolution stock but no permitted route to its supported training Centers. The current collection router is walking-only; the existing observed-menu Fly controller is not integrated. Broader access is the next capability, not merely a larger step budget.

Connect existing observed-menu Fly travel to bounded collection/evolution routing; qualify visited-town/Fly-holder prerequisites and destination/resource conservation, then let the model choose a useful evolution or newly reachable capture. Do not repeat the same local-only batch.

Keep the local Phase5 checklist2/3; full attainability coverage is unfinished. Genuine strategic-model fitting is already running. The next milestone is broader useful access and a model-selected evolution, not the first optimizer update or learned low-level combat. The small goal/destination scorer does not yet demonstrate independent advantage or end-to-end control.

Presentation follow-up: the overview still uses the legacy `shadow_only` component classification. Native sampled gameplay has bounded development authority, not independent evaluation authority; align that label without implying an authority promotion.

## Next bounded session

- Reuse `Gen1FieldMovePort` in `gen1_field_moves.py`; do not reuse the older trial-landing teacher helper. Observe the visited-town menu and a living Fly holder before advertising transport.
- Add Fly as transport for a semantic collection/evolution destination, not as a separate reward-generating goal. Re-read arrival and bind the real skill there; a proposed route is never a successful outcome.
- Test missing carrier/badge/visited destination, blocked indoor departure, stale menu/arrival, changed resources, and action/frame limits with ROM-free fixtures. Keep forbidden Surf/Cut/Strength transport unavailable until separately supported.
- Qualify a short actual-endpoint continuation and retain every outcome. Let the model choose among genuinely executable alternatives; fit only eligible actual choices. Stop on unexplained failure or specimen loss; no replay to erase costs.
- Measure one useful evolution or newly reachable capture before scaling again. Then broaden evolution stock eligibility and remaining attainable-target coverage. Time-box the integration to one session and reorient if it produces no new useful access.

Next session: Astra High, Fast off. This is bounded integration and throughput measurement; the batch runs locally and does not wait for hosted CI. The OpenAI Docs skill verified supported effort settings; this task recommendation is engineering judgment.
