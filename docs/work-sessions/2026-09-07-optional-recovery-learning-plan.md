# Next session — learn optional recovery decisions

Start from V/model60, not U. V is consumed zero-row support: storage retrieval
worked; Metapod4 is now in party at5/18HP, Route11, all25specimens retained.
The native whole-party Center repair is published as74a95fed and passed154 checks.
It has not received live input. The W-labelled inspection is action-free only;
no W proposal, declaration, episode, controller input or fit exists.

## Mission check

1. **Capability:** choose between optional recovery and productive collection
   work while skills enforce hard health, resource and specimen safety.
2. **Learned authority:** let the scorer rank supported recovery alternatives;
   the present menu exposes restore/evolve but drops restore before scoring,
   leaving one supported candidate and deterministic_unsupported. Do not execute
   this fallback as if it were model play.
3. **Transfer test:** ROM-free changed HP, PP, item quantity, money and goal order;
   no title/species/location identifiers in policy features. Retain mandatory
   recovery as a deterministic safety boundary. No Crystal execution.
4. **Cheapest falsifier:** authenticated V must produce two actual model scores
   with unchanged action-free enumeration and safety gates. A two-entry game
   menu alone is insufficient. Old recorded decisions and model outputs must
   continue to replay exactly under their original feature versions.
5. **Time box:** one session/two hours; reassess after60minutes if the compatible
   projection is not working. No further PC/teacher/economy expansion first.
6. **Stop condition:** reinterpreted old features or labels, weakened safety,
   private identity features, unsupported fallback passed off as a model choice,
   another reset/consumed replay, or no additional learned authority.

## Ordered implementation

1. Freeze the old kind/feature layouts explicitly. The current kind one-hot list
   is generated from the enum; simply adding RESTORE would silently widen older
   vectors. Do not alias healing to resupply merely to bypass that boundary.
2. Add a prospective, versioned optional-recovery representation. Preserve old
   model predictions and old trajectory replay exactly. New feature weights are
   initialization, not evidence of learned recovery competence. Do not turn V or
   other old nonexploratory episodes into training targets after the change.
3. Propagate the declared version through the live exploratory actor, trajectory,
   dataset reader and incremental fitter. Qualify sampling/selected-arm attribution,
   legacy exclusions, real resource costs and unchanged hard recovery rules.
4. Inspect V/model60's compatible successor without input. Only if two supported
   genuine choices are scored, declare one fresh bounded native choice. Do not
   require that it chooses evolution; a safe healing choice is a real observation.
5. Retain the actual outcome and V's zero-row support in the next fit. Verify all
   60prior rows survive, report recovery calibration honestly, and inspect the
   saved successor before any further input. No source-proposal double credit.
6. Audit/reorient. The current2/3 storage-safe varied-collection checklist does
   not close merely because a recovery model was initialized or one extra row
   was fitted. Require the original useful varied outcome criterion.

## Secondary findings, not prerequisites for another process campaign

- Capture quotes currently cover Mart purchases, not capture-item scarcity.
  The selected acquisition advertised zero resource cost. Plan an explicit
  compatible correction; do not reinterpret old rows as having better features.
- Storage subtotals survive normal returns, but a later exception loses that
  subsection. V still retains total costs and its exact private failure trace.
  Improve failure-stage observability alongside the next concrete mechanic change.
- A broader read-only architecture review is useful when the new authority and
  migration design is ready. Routine work does not wait for external agents.
- Preserve Red-first progression, the compatible unfamiliar Red hack test and
  later Crystal. No new teacher, full replay or benchmark gate is needed here.
