# Current development handoff

Read [MISSION.md](MISSION.md), [NORTH_STAR.md](NORTH_STAR.md) and
[ACTIVE_PRODUCT_STATE.md](ACTIVE_PRODUCT_STATE.md). Updated September 14, 2026.

## Direct catalog start qualified; claim-first live boundary remains

The full-local151 player now accepts an authenticated catalog origin without inventing a predecessor
episode. Separate direct completion/registered plan schemas bind the source catalog, exact bytes,
lineage, original and derived profiles, model and current code. A direct registration session binds
that plan/catalog/context/state/envelope. Legacy continuation schemas and parent checks are intact.

A metadata-only supply selector now excludes nontrain/ambiguous states and claimed roots,
then chooses by fixed slot order before any saved-state or menu inspection. The authenticated
catalog has 54 train entries: 42 claimed and 12 unclaimed at the recorded census. The earlier
18-registration diagnostic is claimed. It remains diagnostic evidence only.

Private record `full-local-source-20260914-v1` freezes the first unclaimed slot under a shared
account-registry lease. Its digest is 628732d3752e00900c3bdf99a8b247a28e237813bb68665749910023c9c3e1cb.
The selection is not an execution reservation; recheck and claim that exact pair before opening
it. No replacement after inspection or a claim conflict.

Full-local execution now derives one missing boxed canonical level evolution and one missing ordinary
wild source from a read-only semantic observation, then derives the encounter corridor from cartridge
geometry. Caller species/routes are rejected. The direct runner refuses all game payload access
until an already-published account-wide claim matches the exact code and selected root pair, and it
retains that claim in the plan. The actual selected save remains unopened, so its two-family
gameplay feasibility is still unknown.

## Next bounded implementation

Publish the direct-origin bridge described in the
[current session](docs/work-sessions/2026-09-14-direct-catalog-origin-bridge.md) and require exact-head
CI. Then use a controller-capable source-choice boundary to atomically claim the frozen source and
pass that claim to the runner before its payload is opened; open only it and require two actual
executable families.
Only after that gate: freeze one exploratory model choice, execute once, retain real costs and
the local 151 terminal, and fit only the measured outcome. A claim or menu failure stops without
another source. This work is bounded to 90–150 minutes before reassessment.

## Learning and completion status

Model121 remains 121 examples, 83 successes and 86 local registrations. The bridge session opened
no selected ROM/save/model, issued no input/query, created no root claim or registration session,
and added no outcome or fit. Gameplay is stopped; the anti-drift alarm remains active.

The Model121 terminal itself remains ineligible for the standard collector because its original
state is not in the frozen catalog. Never relabel it, replay consumed trials or weaken provenance.
Full fresh-start model-directed Red story plus all 151 local flags still precedes any ROM hack,
then Crystal and at least Emerald. Version, trade, supporting-save and event requirements remain.

## Verification and reviewers

Claude Opus4.8 High found one error-contract inconsistency in the direct bridge; it is fixed and
covered. Flash3.8 High supplied hybrid-schema, anchor-swap, exhaustion and caller-injection cases,
now covered or mapped to existing route/claim tests. Neither review accessed private artifacts.

Next-session recommendation: **GPT-5.6 Sol / High / Fast off**. The next work is a bounded
claim/preflight/execution integration, not a new architecture search; reserve Astra for an unresolved
authority decision.

[Session](docs/work-sessions/2026-09-14-direct-catalog-origin-bridge.md) ·
[Evidence](docs/evidence/red-direct-catalog-origin-bridge-2026-09-14.json)
