# Current development handoff

Read [MISSION.md](MISSION.md), [NORTH_STAR.md](NORTH_STAR.md) and
[ACTIVE_PRODUCT_STATE.md](ACTIVE_PRODUCT_STATE.md). Updated September 14, 2026.

## One source frozen; direct-start and acquisition configuration remain

The full-local151 player is integrated with the existing travel router, native evolution,
one-shot execution, write-ahead selection and durable terminal observations. Its prior final
commit 063ad931 passed hosted CI 34876713998.

A metadata-only supply selector now excludes nontrain/ambiguous states and claimed roots,
then chooses by fixed slot order before any saved-state or menu inspection. The authenticated
catalog has 54 train entries: 42 claimed and 12 unclaimed at the recorded census. The earlier
18-registration diagnostic is claimed. It remains diagnostic evidence only.

Private record `full-local-source-20260914-v1` freezes the first unclaimed slot under a shared
account-registry lease. Its digest is 628732d3752e00900c3bdf99a8b247a28e237813bb68665749910023c9c3e1cb.
The selection is not an execution reservation; recheck and claim that exact pair before opening
it. No replacement after inspection or a claim conflict.

The selected configuration contains only story, restoration and control recovery. It has no
declared capture corridor or native evolution provider. The actual save was not opened, so its
gameplay feasibility is unknown. Separately, the collector's completion dose and registration
anchor require an existing continuation. A new catalog start cannot use that path directly.

## Next bounded implementation

Implement the direct catalog-origin training/session contract described in the
[current session](docs/work-sessions/2026-09-14-full-local-training-source.md).
Bind the existing catalog, exact source bytes, lineage, model and new execution configuration.
Keep old continuation schemas and bytes valid. Do not create a fictitious predecessor episode.

Use semantic inventory and existing cartridge adapters to derive acquisition declarations.
Then atomically claim the frozen source, open only it and require two actual executable families.
Only after that gate: freeze one exploratory model choice, execute once, retain real costs and
the local 151 terminal, and fit only the measured outcome. A claim or menu failure stops without
another source. This work is bounded to 90–150 minutes before reassessment.

## Learning and completion status

Model121 remains 121 examples, 83 successes and 86 local registrations. This session opened no
ROM, saved payload or model, issued no input/query, created no root claim or registration session,
and added no outcome or fit. Gameplay is stopped; the anti-drift alarm remains active.

The Model121 terminal itself remains ineligible for the standard collector because its original
state is not in the frozen catalog. Never relabel it, replay consumed trials or weaken provenance.
Full fresh-start model-directed Red story plus all 151 local flags still precedes any ROM hack,
then Crystal and at least Emerald. Version, trade, supporting-save and event requirements remain.

## Verification and reviewers

The source selector and adjacent regression tests are recorded in the session evidence.
Claude Opus4.8 High completed a read-only source/test review with no concrete finding.
Flash3.8 High supplied useful partition, deterministic-order and claim-race test categories.
Its historical-menu-as-executability assumption was rejected. Subscription percentages/reset
windows are unavailable from both headless interfaces. No external reviewer remains running.

Next-session recommendation: **GPT-5.6 Sol / High / Fast off**. The remaining interfaces and
invariants are specified; reserve Astra for an unresolved authority decision.

[Session](docs/work-sessions/2026-09-14-full-local-training-source.md) ·
[Evidence](docs/evidence/red-full-local-training-source-2026-09-14.json)
